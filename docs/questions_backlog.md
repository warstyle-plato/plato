# Задачи и вопросы

Единственное место, где ведутся текущие задачи и открытые вопросы. CLAUDE.md
держит правила, выведенные из поломок, и устройство — список дел там не живёт,
там только ссылка сюда: копию негде обновлять, потому что копии нет.

Три раздела и разница между ними жёсткая:

- **В работе** — есть ветка или PR. Строка снимается, когда PR слит или закрыт.
- **Очередь** — решение делать принято, работа не начата.
- **Вопросы** — решение не принято. Это не задачи: пока на вопрос нет ответа
  владельца или разбора, брать в работу нечего.

Как добавлять: дата постановки, сам вопрос своими словами, затем — что по
этому поводу **уже есть в коде** (файлы и функции, чтобы через месяц не искать
заново) и что именно неясно. Отвеченный вопрос не стирается: под ним пишется
решение и дата, а сама работа переезжает в «Очередь». Сделанное, из которого
вышло правило, уезжает в CLAUDE.md.

---

## В работе

**Сделано 26.08.2026 — свод продаж переделан по восьми пунктам владельца.**
Плашки называют долю от проекта: продано столько-то лотов, метров, машино-мест
и процентов ожидаемой выручки — база берётся из плана финмодели и книги, а не
заводится третьей. Динамика — один график с переключателем (₽, м², лоты, ₽/м²),
таблица под раскрытием. Появилась квартирография: пул проекта, продажи и
остаток витрины тремя полосами, с вымыванием по полосам книги. Факт против
обоих планов — на одном графике по кварталам. Каналы уехали в конец, список
брокеров свёрнут. Под каждым блоком — вывод словами, считает его сервер.
Сверху — навигация по блокам. Источники лежат на ядре и переживают закрытие
вкладки: файл грузится один раз, книга финмодели — отдельно и позже.

Что при этом поймано: `Number(null)` — это ноль, и он проходит
`Number.isFinite`, поэтому линия плана банка ползла по нулю там, где плана нет
вовсе; «6 из 0 лотов» показывалось вместо «пул лотов неизвестен»; хвост плана
банка в вопросе Платону оставлял 2029 год без факта вместо кварталов, с
которыми сравнивают.

**Сделано там же 26.08.2026 — спрос против витрины и достаточность эскроу.**
Из 573 сделок CRM разобран 101 запрос по площади и 106 по бюджету; ответ на
«почему не покупают» собран из разрыва: 34,7% запросов на полосу 28,3–40 м²,
где осталось 15,2% витрины. Причину отказа не считаем — поля стадии в выгрузке
нет, а «отказ» в комментарии почти всегда про контакты и звонки.
Достаточность эскроу: план с листа «КРЕДИТЫ» даёт покрытие 0,95× к раскрытию,
факт на 12,6% ниже плана, а темп плана в 3,8 раза выше нынешнего — при
нынешнем темпе покрытие 0,36×.

Осталось по кабинету: PDF свода продаж и калибровка баллов лотов по реестру
владельца на 372 сделки. Читатели площадок банкротства ждут живых ответов
пробы с прода.

## Прежнее

Состояние на 23.08.2026.

**Сделано 23.08.2026 — анкеты и пользователи (0.19.54–0.19.56).** Анкета лежит
своим файлом в `data/surveys` на ядре и переживает подметание журнала и потерю
контейнера; свод объединяет хранилище с журналом, снимая дубли. Анкеты из бота
доезжают через `/internal/survey/save`, люди — через `/internal/user/touch`,
оба с подписью общим токеном. Реестр людей пишется в `usage_track` — там, где
сходятся все обращения. История бота восстанавливается с ядра на старте
(`/internal/users/list` + `restore_from_core`), поэтому `/users` после выкатки
показывает всех, а не «с четверга». `/agent/chat` наконец пишет `chat_id`.
Завершение анкеты идемпотентно: владелец получил подряд «Оценок записано: 7» и
«Ни одной оценки», а при двух воркерах та же гонка удвоила бы средние. Списки
админов сведены: `TELEGRAM_ADMIN_IDS` складывается с `DEVELOPAID_ADMIN_IDS`.
Тринадцать тестов.

Осталось из того же гнезда: контекст Платона (`data/platon_state`) и тексты
сообщений журнала бота по-прежнему живут на Render и умирают с выкаткой; гость
по админскому ключу по-прежнему безымянен — ключ пускает, но личности не несёт.

**Взято владельцем 23.08.2026** — девять пунктов ушли в работу одним заходом.
Отмечены как взятые, а не как сделанные: подтверждения в коде у меня нет, и
ставить «готово» со слов нельзя — иначе список перестанет отличаться от
намерений. Как что-то доедет, скажите, перенесу в закрытые с проверкой.

| Пункт | Где лежит |
| --- | --- |
| Классы — что задаёт уровень и вторая копия ставок у бота | вопрос 7 |
| «ГНС проекта» с подземной частью: восемь ставок на полную ГНС | очередь 7 |
| Три норматива приобъектной парковки в одном коде | очередь 10 |
| Погашение БРИДЖа: шаблон против движка | очередь 1 |
| Опция «собственный капитал вместо БРИДЖа» | очередь 2 |
| Чистка шаблона | очередь 3 |
| Инструмент чувствительности для Платона | очередь 4 |
| Четыре округа Подмосковья в Таблице 3 | очередь 6 |
| Правило ±1 на уровне бизнеса | вопрос 4 |

**Открытые PR** — по состоянию на 22.08.2026.

| PR | Что делает | Состояние |
| --- | --- | --- |
| [#204](https://github.com/warstyle-plato/plato/pull/204) `fix/monitor-readable-forecast` | Monitor: читаемый forecast, корпуса и финансовые полосы | черновик |
| [#183](https://github.com/warstyle-plato/plato/pull/183) `agent/pf-rate-tep-ui` | Ставка ПФ, пропорции ТЭП и нормальный ТЭП в Excel | черновик |
| [#171](https://github.com/warstyle-plato/plato/pull/171) `agent/market-statistics` | Market preview + statistics | черновик |
| [#51](https://github.com/warstyle-plato/plato/pull/51) `feature/mpt-calculator-v0.1` | Калькулятор МПТ отдельным сервисом | открыт с 07.08.2026, давно без движения — решить, доводить или закрывать |

Таблица собирается руками и потому врёт ровно настолько, насколько давно её не
трогали. Перед тем как на неё опереться, сверить с открытыми PR.

## Список от 22.08.2026 — сверка с кодом

Пятнадцать пунктов из ChatGPT, проверенных по коду 22.08.2026. Каждый пункт
несёт не мнение, а место в коде или прогон движка. Закрытые оставлены здесь
строкой, чтобы их не спрашивали заново; открытые расписаны ниже в «Очереди».

| № | Пункт | Состояние |
| --- | --- | --- |
| 1 | Дефолт ПФ в разделе «Результат» | частично — см. очередь |
| 2 | Лесенки процентов | **закрыто** |
| 3 | ТЭП очередей в обе стороны | частично — см. очередь |
| 4 | Коэффициенты МПТ | **закрыто**, но открыт PR #51 |
| 5 | Спрятать самостоятельный расчёт ВРИ | **закрыто** |
| 6 | Пропорции ТЭП одиночного проекта | **закрыто** |
| 7 | Отражение изменений в Excel | не проверено — нужен Excel |
| 8 | Распределение финансирования по очередям | **закрыто** |
| 9 | ПИРы в очередях | не воспроизвелось — нужен пример |
| 10 | Румянцево: ДОУ и СОШ в ТЭП | подтверждено, причина найдена |
| 11 | Поиск по КРТ с классом и ценой по округам | частично |
| 12 | Зона КРТ в градпроработке | открыто |
| 13 | Слои: ЛЭП по краю участка | частично, и главная половина не сделана |
| 14 | Блок `/statistics` | причина найдена — см. вопрос 6 |
| 15 | Статистика по пользователям и анкетам | причина найдена — см. вопрос 6 |

Что именно проверено по закрытым:

- **2. Лесенки процентов.** Поле `pf_special_steps` есть во вводных и в
  `FIELD_GROUPS`, ступени считает движок, действовавшую ступень называют отчёт,
  PDF (`_pdf_pf_step_rows`) и книга; `tests/test_escrow_rate_steps.py` зелёный.
- **4. Коэффициенты МПТ.** Кзатр 166,23078 с 01.01.2026 по приказу
  ДИПП-ПР-34/20 в редакции 10.03.2026, с квартальной индексацией и разговором
  о смене квартала; `test_mpt_audit_fixes`, `test_kzatr_quarterly_index`,
  `test_reference_freshness` зелёные. Отдельно висит PR #51 «MPT calculator
  v0.1 as isolated service» — с 07.08.2026 без движения, решить его судьбу.
- **6. Пропорции одиночного ТЭП.** `tepCellChanged`: любое из трёх чисел
  ведущее, два других считаются по пропорциям (`tepFillByRatios`); сами
  пропорции правятся и сбрасываются (`tepRatioSet`, `tepRatioReset`) и берутся
  из движкового `TEP_RATIOS` подстановкой, а не копией.
- **5. Самостоятельный расчёт ВРИ.** Уже убран решением владельца от
  20.08.2026: карточки на странице нет, её работу делает «Пересчитать под
  фактический ТЭП». Формула города осталась в движке (`vri_manual_payment`) —
  ею пересчёт подтверждает свою пропорцию, — и `/vri/manual` отвечает
  по-прежнему. Прятать больше нечего.
- **8. Финансирование очередей.** `phasing.financing_strategy` с двумя
  режимами — «независимый» и «единый поток проекта», — таблица «Финансирование
  очередей» и `tests/test_phase_financing_strategy.py`.

## Очередь

**Книга «Полная модель» (Румянцево) расходится с движком — разбор 27.08.2026,
чинить книгу (сборщик у сессии «Сводного чата», в репозитории его нет).**
Сверка постатейная и помесячная на восстановленных из книги вводных: выручка и
коммерческие расходы сходятся до копейки, финансирование — нет. Четыре причины,
движок по трём прав (его методика сверена с договорами НКЛ Сбера):
1. Книга платит проценты ПФ живыми деньгами ежемесячно (О1: уплачено 9 343 млн
   в ходе стройки) и затыкает кассу «вкладом собственного капитала» 8 686 млн,
   которого в проекте нет. По методике движка проценты до раскрытия эскроу
   отсрочены и платятся разом в РВЭ из раскрытия — вклад тогда не нужен и
   исчезает сам. Это не «вклад с эскроу»: эскроу в книге учтён отдельными
   строками, вклад — внешние деньги, следствие неверного графика уплаты.
2. Книга начисляет проценты на непогашенный остаток до РВЭ + 24 мес; горизонт
   движка — РВЭ + 12. Лишний год × 13,5% × ~6,4 млрд ≈ +0,86 млрд процентов.
   Правило уже записано: «формула книги живёт в горизонте движка».
3. Соцстройка в книге без индексации (2 584,9 номиналом), движок индексирует
   строительную часть к РнС очереди (+117 млн).
4. Садик 530 мест: книга ставит в О1, движок разложил «поздним умолчанием» в
   О2 — расчёт не передавал явного размещения. Умолчание движка НЕ менять
   («их же много, как можно говорить обо всех объектах в 1 очереди» —
   владелец, 27.08.2026): в расчёте Румянцево задать садику очередь явно,
   реестром `social_objects`. Открытая идея для движка: умолчание «по
   населению» — объект встаёт в очередь, при заселении которой накопленное
   население впервые требует его мощность по нормативам (садик рано, большая
   школа позже); ждёт решения владельца.
Плюс: вшить в книгу лист сверки с движком (как `audit`/`_parity_mismatch` у
v4 и ПЛАТО), чтобы следующее расхождение называло себя само. Вердикт у обеих
поверхностей уже совпадает — проект в дефолте по долгу, LLCR ~1,0; расходятся
величины, не вывод.

Пункты 1–6 достались из CLAUDE.md, 8 и 9 пришли с выгрузки ГлавАПУ,
остальные — из списка от 22.08.2026.

1. **Логика погашения БРИДЖа** в шаблоне и движке разная: в шаблоне остаток
   падает до нуля на четвёртый месяц (пик 700,8 млн ₽), в расчёте пик 1,00 млрд ₽.
2. **Опция «собственный капитал вместо БРИДЖа»** на входе в проект.
3. **Чистка шаблона** от листов и строк, не участвующих в расчёте (лист
   «Расчет ВРИ (ТЭП)» — остаток прежней методики). Выпиливать только после
   разбора зависимостей: на него ссылаются формулы.
4. **Инструмент чувствительности для Платона Сергеевича** — читать последний
   результат `/analysis/sensitivity` или запускать его. Числа считает движок,
   не модель.
5. **Градостроительный скрининг участка** — полная архитектура владельца в
   `docs/land_screening_architecture.md` (runtime-источники против локальной
   нормативной базы, слой конфигурации layer_id с canary, статусы чеков,
   запрет «участок подходит», MVP). Ниже — краткая выжимка. (Решение, 16.08.2026 —
   «это уже девелоперский сервис, а не просто финмодель»). Первичная
   град-проработка из открытых данных, красные флаги до экономики:
   - Этап 1: слои ЗОУИТ НСПД по bbox участка (тот же WMS, что подложка карты;
     id слоёв сверять живой пробой в духе `/land/map-probe`, не вслепую) →
     зоны с процентом накрытия, строимое пятно = контур минус ЗОУИТ; доля
     границы с ИЖС по ВРИ соседей (точечный поиск уже умеет); «физика
     посадки» — этажность/высота из СПП ГлавАПУ и пятна (пример владельца:
     22 тыс. продаваемых на пятне 0,3 га ≈ 28 этажей ≈ 100+ м).
   - Этап 2: подзоны ПЗЗ Москвы (открытые данные: плотность тыс. м²/га,
     высота в метрах, процент застройки, индекс «Ф») → автосверка
     нормативного ТЭП с пределами.
   - Этап 3: МО через РГИС; красные линии — только где внесены в ЕГРН
     (полнота фрагментарна — говорить об этом вслух).
   Места в интерфейсе (утверждены владельцем): блок в разделе «Участок» под
   карточкой участка (зоны поверх нашей карты); в отчёте раздел
   «Реализуемость посадки» ПЕРЕД «Оценкой целесообразности покупки»
   (`_PdfSection` — порядок штатный); главный флаг поднимается в карточку
   решения рядом с финвердиктом. Честные границы метода — в оговорке блока:
   геоподоснова 1:500 закрыта, сервитуты и чужие ГПЗУ не видны.
6. Четыре округа Подмосковья отсутствуют в Таблице 3 (Дзержинский, Протвино,
   Пущино, Электрогорск); справочник `vri_mo_ranges` не заполнен.
7. **Скрининг спрашивает точку, а не участок.** `_land_screen_findings(lat, lng)`
   опрашивает шестьдесят слоёв НСПД в одном пикселе — центре участка. Охранная
   зона ЛЭП, идущая полосой вдоль границы, туда не попадает, слой отвечает
   пусто, и вердикт говорит «критических ограничений не обнаружено» (кейс
   Коммунарки, владелец 22.08.2026). Прежняя правка про долю накрытия
   (`_land_coverage_shares`) чинила вторую половину вопроса — как измерить
   найденную зону; первую, как её найти, не трогала: геометрия берётся из того
   же ответа GetFeatureInfo. Чинить опросом по BBOX контура (или сеткой точек
   как запасным путём) и называть метод в ответе: «опрошен контур» против
   «опрошена одна точка» — это и есть разница между «проверено» и «похоже,
   чисто». Пока опрос точечный, зелёный вердикт проверкой участка не является,
   и в оговорке блока это должно стоять прямо. Место правки — вызов
   `_land_screen_findings(center["lat"], center["lng"], geometry)`: контур туда
   уже приходит, но идёт только в долю; сам запрос строит
   `_nspd_getfeatureinfo` одним пикселем тайла zoom 24.

8. **Пункты ниже пришли из main 24.08.2026 и заменяют прежние формулировки.**
   Соседняя сессия переписала их по тексту свода: «три норматива парковки» было
   неверно (формула у 945-ПП одна, различаются X2 по приложениям), а у ГНС дело
   не в подписи, а в том, что надземную ГНС и строительный объём надо развести.
   Мой прежний пункт про восемь удельных ставок снят — он про то же, но хуже.

9. **«ГНС проекта» в движке включает подземную часть.** `project_gns_sqm`
   суммирует `gns` по всем строкам ТЭП вместе с `underground_parking`, и на
   этой базе считаются все `*_per_gns_th` — в отчёте, в PDF, в книге и на
   странице. По методике подземная в ГНС не входит (владелец, 23.08.2026), она
   в общей площади. Чинить не переименованием одной подписи: надо развести
   **надземную ГНС** (база для сравнения со сметами и с чужими проектами) и
   **строительный объём** (надземная плюс подземная — база, на которой сейчас
   всё посчитано), и печатать обе рядом, как уже сделано с парой «на метр ГНС /
   на метр продаваемой». Пока не разведено, любое «тыс ₽/м² ГНС» из наших
   поверхностей завышено примерно на пятую часть относительно того же
   показателя, посчитанного по методике.

10. **Норматив приобъектной парковки решён выгрузкой — свести код к одному
   числу.** Выгрузка ГлавАПУ по кварталу 77:05:0004001 (22.08.2026) отдала
   собственный ответ калькулятора, и наши формулы воспроизвели его до единицы:
   постоянные места жилья 470 по 2118-ПП, квартир 588, всего мест МКД 519
   (470 + 47 гостевых + 2 кратковременных), приобъектные встроенных 15 по
   **НП / 90 × К1 × К2**, всего встроенных 19 (15 + 4 кратковременных).
   Приложение 6 (офис 63, ТЦ 54) даёт на тех же метрах 22 и 25, пресетные
   100 — 14. Значит верна строка «90 × К1 × К2», а `MOSCOW_ATTACHED_PARKING_X2`
   и `project_preset.OFFICE_SQM_PER_SPACE` — два лишних мнения; свести к
   одному месту. Проверено на встроенно-пристроенных: в этой выгрузке СПП
   нежилых зданий нулевая, поэтому для отдельно стоящих офисов и ТЦ формула
   ещё не сверена — нужна выгрузка квартала с ними.

   Оттуда же: **площадь квартир = население × 33**, а не «доля жилой СПП»
   (на выгрузке 40 689 м² при СПП жилой 62 596 — отношение 0,65 вышло
   совпадением). И **кратковременные места считаются у обоих**: жильё
   `ceil(площадь квартир / 22 100)`, нежилое `min(4, ceil(НП / 450))` — их у
   нас не было вовсе. Коэффициенты квартала: К1 0,75, К2 0,5 (вне ТТК),
   аренда 0,1184, район Нагатино-Садовники, зона 1.

11. **Приобъектная парковка: формула одна, X2 разные, коэффициентов у пресета
   нет.** Формулировка «три норматива» была неверной и однажды спровоцирует
   ошибку — исправлено 23.08.2026 по тексту свода. Формула у 945-ПП **одна**:
   `Nв = X / X2 × К1 × К2`. Разложено по приложениям так (уточнено 24.08.2026):
   **приложение № 1** — таблица X2 по ВРИ (офис 63, ТЦ 54, магазин 63, общепит
   54, гостиница 300, спорт 200 м²/место), **приложение № 3** — К2 по 132
   районам Москвы, **приложение № 6** — сама формула и К1 (0,75 при доступности
   рельсового каркаса до 1200 м, 0,9 до 2200 м, 1,0 дальше — по пешеходным путям
   до входа на станцию, не радиусом). Прежде мы приписывали таблицу X2
   приложению № 6: в споре с городом неверный реквизит стоит дороже неточного
   числа. Значит 63, 54 и 90 — не конкурирующие правила, а разные строки одной
   таблицы. Сноска <2> приложения № 1 называет базу прямо: **«используется
   нежилая наземная площадь»**.
   **Загадка «63 против 90» закрыта 24.08.2026: 90 — это норма.** Приложение
   № 6 прямо устанавливает для встроенно-пристроенных нежилых помещений жилой
   застройки 90 кв. м ННП на место. Полгода это число жило у нас как «практика
   города, восстановленная по выгрузке» — мы считали правильно и не знали, на
   чём стоим. **Число не менялось, появилось основание, и это разные вещи:** в
   споре с городом «так считает их калькулятор» и «так написано в приложении 6»
   весят по-разному. Отсюда общее: **воспроизведение чужого ответа — не замена
   основанию, а повод его искать.** К2 при этом сошёлся: приложение № 3 даёт Нагатино-Садовникам 0,5,
   и ровно 0,5 несёт наша выгрузка по 77:05:0004001.
   Состояние кода: строка 90 (`tep_derived_norms`, `vri_tep_quick`) работает и
   сверена выгрузкой до единицы — но на **встроенных** помещениях, для отдельно
   стоящих офисов и ТЦ она не проверялась; `MOSCOW_ATTACHED_PARKING_X2` (63/54)
   объявлена, но **нигде не вызывается** — её читают только тесты;
   `project_preset.OFFICE_SQM_PER_SPACE` (100 м²/место) работает и считает мимо
   формулы — без К1 и К2 вовсе.
   **Менять пресет на 63 нельзя, и это решение владельца (23.08.2026):** у
   `project_preset` нет ни К1, ни К2 — пресет собирается из извещения КРТ, а
   коэффициенты приходят с выгрузкой ГлавАПУ по кварталу. На 92 845 м² офисов
   выходит 929 мест при нынешних 100 м²/место, 1 474 при 63 с допущением
   К1=К2=1 и 221 при настоящих К1=0,75 К2=0,2 — разброс в шесть с половиной раз.
   Норматив без своих коэффициентов выглядит нормативно и врёт кратно; честные
   100 м²/место с пометкой «наше допущение» лучше. Чинить — когда у пресета
   появятся К1 и К2, а не раньше. Полной таблицы приложения 1 у нас нет: свод
   перечисляет четыре функции, строки для встроенных помещений в нём нет, и
   откуда взялось 90 — из практики города на одном квартале, а не из документа.
   2118-ПП переписал приложение 5 (жильё), приложение 6 не трогал — свод велит
   не переписывать их вместе.
   **В Московской области приобъектных стоянок нежилья у нас нет вовсе**:
   нормативы РНГП МО в движке считают паркинг только от населения, и офисник
   или ТЦ в подмосковном проекте получает ноль мест. Это пробел, а не
   допущение. Норматив есть — приложение № 10 к 713/30 (введено 1480/45,
   правила п. 5.12 в редакции 774-ПП от 02.07.2026): офис 1 м/м на 50–60 м²
   **общей** площади, ТЦ на 40–50, рестораны и кафе на 4–5 посадочных мест,
   функция вне таблицы — 1 на 50 м². Аналогов К1 и К2 в МО нет; переносить туда
   московские нельзя. Таблица снята со вторичного источника и лежит в
   `docs/normative/parking_sources_20260824.md` — в расчёт не заведена.
   Там же: **СП 42.13330.2016 не действует с 12.07.2026**, заменён
   СП 42.13330.2026 (приказ Минстроя № 368/пр от 11.06.2026).

12. **Непогашенный ПФ на экране не назван дефолтом.** Плашка про РВЭ на
   странице есть (`pfRveWarning`: «Эскроу не погашает ПФ полностью»), а долг на
   конец проекта стоит нейтральной плиткой «Остаток ПФ» среди ставок и лимитов.
   Текст «Долг ПФ не погашается к концу проекта — для банка это риск дефолта»
   живёт только в карточке бота и строкой в PDF. Прибыль при этом
   положительная и стоит выше — на экране это читается как хороший проект.
13. **ТЭП очередей: правка одного поля не двигает связанные.** Абсолютные
   значения и доли правятся, потолок исходного ТЭП держится
   (`phaseProductTepLimit` и предупреждение «Значение ограничено до …:
   доступный остаток исходного ТЭП»), — эта половина сделана. Не сделана
   вторая: `phaseProductDerived` считает каждое поле от своей доли независимо,
   поэтому правка ГНС очереди не пересчитывает продаваемую, как это устроено в
   одиночном ТЭП. Колонки «общая площадь» в таблице очередей нет вовсе — цепочка
   ГНС → общая → продаваемая там неполна по составу.
14. **Соцобъекты не видны в ТЭП, потому что у них нет ГНС.** Подтверждено на
    Румянцеве без скриншотов: после импорта пресета ДОУ несёт `total_area`
    4 764,9 м², СОШ — 9 265,1 м², а `gns` и `saleable` у обоих нули. Обе
    таблицы — вкладка «Результат» (`reportTep`) и ТЭП очередей (`phaseTepBody`) —
    печатают только ГНС · продаваемая · шт., поэтому строки выглядят пустыми,
    хотя объекты строятся. Рядом второе: у ДОУ `transfer` остался 3 000 м² из
    `TEP_DEFAULT` при площади 4 764,9 — импорт поле не перезаписал (то самое
    «поле, которого нет в карте записи, молча остаётся мусором из шаблона»).
15. **Поиск по КРТ с классом и ценой по округам.** Сейчас есть фильтр по
    округам, статусу и назначению и скоринг `krtFit` по ТЭП; класс и цена
    приходят только по одному выбранному проекту кнопкой «Получить маркетинг и
    прогнать модель». Массового отбора «наш класс и наша цена по округу» нет.
16. **Зона КРТ в градпроработке.** В `_NSPD_SCREEN_LAYERS` только слои НСПД;
    krt.mos.ru со скринингом участка не связан. Показывать, что участок входит
    в утверждённую зону КРТ или граничит с ней.
17. **Прочитать заполненные анкеты нельзя тремя разными способами.** Читалка
    есть и она полная: `/survey` в боте печатает воронку, средние по разделам,
    свободные тексты целиком и ответы поимённо, а половину с сайта подтягивает
    с ядра отдельным блоком. Невидимой анкета становится по трём причинам, и
    все три чинятся. Первая: без `DEVELOPAID_ADMIN_IDS` команда отвечает «Свод
    закрыт» — владелец сервиса не может прочитать ответы, пока не впишет свой
    номер в переменную. Вторая: окно свода по умолчанию тридцать дней, а журнал
    хранит сто восемьдесят, — «Анкет заполнено» показывает только последний
    месяц, и заполненная в июне в ответе не появится, пока не набрать
    `/survey 180`. Для `/stats` это правило уже выведено («свод читает журнал за
    всё хранение»), до анкет оно не дошло. Третья: не ответило ядро — тогда
    видна половина бота, и об этом есть строка. Пока держится любая из трёх,
    просить людей заполнять анкету бессмысленно: ответ уходит туда, откуда его
    не достают.

18. **Кабинет должен разбирать банковскую выгрузку проекта, а не только
    узнавать её.** Файл «Кутузов Сити» на 18.08.2026 (владелец, 23.08.2026) —
    это не отчёт о продажах, а весь мониторинг проекта одной книгой,
    пятнадцать листов: `ТЭП` (объекты и площади), `Дашборд` (план-факт на
    дату), `Продажи П-Ф` (план, факт и прогноз по эскроу, метрам, договорам и
    цене — отдельно по квартирам, кладовым, машино-местам и коммерции, плюс
    блок «по месяцам»), `ЭСКРОУ П-Ф` (помесячные периоды с номером квартала),
    `План продаж УТВ` (по очередям), `Бюджет (РСС)` (сводный сметный расчёт,
    237 колонок), `Чек лист` готовности конструктива, реестры договоров (283
    строки), актов (516) и платежей (2 209), `Сводная` и лист `списки` со
    справочниками — статьи ССР, источники и признаки платежа, класс,
    конструктив, этажность.

    Проверено на этом файле, а не предположено. **РСС читается существующим
    кодом целиком** — мешает только имя листа: `developaid_actuals.read_estimate`
    ищет «Расчет стоимости строительства», а в книге лист называется «Бюджет
    (РСС)». Переименовал копию — и разобрались 61 статья и итог: смета
    6 804,4 млн ₽, заключено 461,9, оплачено 455,1, выполнено по КС 425,3.
    Оплачено сходится с «Дашбордом» книги (455 068 049,24) до копейки, колонки
    угадывать не пришлось. **Продажи, наоборот, не разбираются вовсе**:
    `_sales_snapshot` в мониторе только проверяет, есть ли лист «Продажи П-Ф»,
    и возвращает его имя — ни одного числа оттуда не берётся, хотя там лежит
    ровно то, чего монитору не хватает: факт по эскроу 1 356 740,7 тыс ₽ против
    плана 1 476 461,1, цена квартир 660,6 против плановых 663,5 тыс ₽/м², темп
    августа отдельным блоком.

    Работа: узнавать такую книгу по составу листов, а не по имени файла;
    искать РСС по нескольким именам листа (и по подписи «Всего инвестиционные
    расходы» внутри, если имя незнакомо); достать план-факт продаж по четырём
    продуктам; сказать вслух, что распозналось, а что нет — по правилу
    «пустой результат проверки — не „чисто“», молчаливо пропущенный лист
    выглядит как отсутствующий в файле раздел.

    Отдельно — где держать эталон. Румянцево лежит файлом в `presets/` именно
    затем, чтобы разбор проверялся на настоящем документе, и такой же эталон
    нужен здесь. Но эта книга несёт живые данные проекта: подрядчиков поимённо,
    суммы договоров, даты и суммы платежей. Класть её в репозиторий — решение
    владельца, а не моё; альтернатива — обезличенная копия (реестры урезаны,
    контрагенты заменены) плюс контрольные числа, на которых проверяется
    разбор: смета 6 804,4 млн, оплачено 455,1, эскроу-факт 1 356 740,7 тыс ₽.

19. **Проверить, что изменения доехали в Excel.** В песочнице не проверяется:
    openpyxl формулы не считает, LibreOffice сломан. Нужен прогон
    `audit_plato_workbook` на книге, пересчитанной Excel и сохранённой. Отдельно
    сверить то, что появилось последним: ТЭП очередей и лестницу ставок.

## Вопросы

### 1. Ориентир максимальной цены покупки на аукционе КРТ (22.08.2026)

**Вопрос.** В блоке КРТ, наверное, стоит давать ориентир максимальной цены
покупки на аукционе — сколько можно поднять ставку, пока проект остаётся
проходным.

**Что уже есть.** `auction_search/krt_screening.py` считает `entry_capacity`
через `_goal_seek_entry_capacity`: подбор `purchase_price_mln` при целевом
LLCR 1,20x (`TARGET_LLCR`), на слабейшей очереди в фазированном режиме. То есть
число уже считается движком, а не рядом с ним. Наружу оно выходит как «Резерв
при LLCR 1,20x» в карточке лота (`auction_search/ui.py`) с подписью
«предельный резерв по переменной "цена входа"».

**Что неясно.**
- Резерв при LLCR 1,20x — это ещё не предельная ставка на торгах. Из него не
  вычтены обязательства КРТ, которых в каталоге нет: плата за ВРИ, переселение,
  соцобъекты, внеплощадочные сети (они перечислены в `exclusions` того же
  расчёта). Назвать резерв «максимальной ценой» — значит показать заведомо
  завышенный потолок под уверенным именем. Решить: показываем потолок только
  когда обязательства оцифрованы, или показываем всегда с явным
  «до обязательств».
- По какому критерию считать потолок. LLCR 1,20x — банковский порог
  проходимости; у покупателя на торгах критерием может быть маржа или прибыль.
  Возможно, нужны две линии: «выше этого банк не финансирует» и «выше этого
  проект не окупается».
- Один потолок или лестница. Аукцион идёт шагами, и полезнее видеть, что даёт
  каждый шаг: при такой ставке LLCR такой, при следующей — такой.

### 2. Стартовая цена аукциона из документов КРТ (22.08.2026)

**Вопрос.** Нужна цифра старта аукциона из документов КРТ.

**Что уже есть.** У лота ЭТП поле есть — `AuctionLot.start_price_rub`
(`auction_search/models.py`), оно доезжает до пресета и до карточки
(`preset_mapper.py`, `developaid_mapper.py`). А у проекта из официального
каталога krt.mos.ru его нет: `auction_search/krt.py` вынимает из документов
программу (`extract_krt_program`) и обязательства (`extract_krt_obligations`),
цену — нет. Поэтому скрининг КРТ и стартует с `purchase_price_mln: 0.0`.

**Что неясно.**
- Где эта цифра лежит в документах КРТ и одна ли она: начальная цена права,
  задаток, шаг аукциона — разные числа, и брать «первое похожее на деньги»
  нельзя. Нужна пара живых комплектов документов, чтобы увидеть формулировки,
  а не гадать по шаблону.
- Что делать, когда цифры в документах нет. По правилу «пустой результат
  проверки — не "чисто"»: отсутствие стартовой цены должно быть видно как
  «не нашли», а не как ноль, иначе оно неотличимо от бесплатного входа — ровно
  то, чем сейчас является `purchase_price_mln: 0.0`.

**Осторожно с именем.** В коде уже два разных «старта»:
`AuctionLot.start_price_rub` — начальная цена лота на торгах (₽ за лот), а
`start_price` в `krt_screening.py` — стартовая цена продажи квартир (₽/м²,
приходит из маркетинга), в ответе она лежит как `market.start_price_rub_sqm`.
Третье поле с корнем «start» без второй базы в имени читаться не будет —
правило про `*_per_gns_th` / `*_per_saleable_th` здесь то же самое.

### 3. Классы в DevelopAid — отложено 18.08.2026, «возможно, потом»

Сейчас `PROJECT_CLASS_PRESETS` знает три уровня (комфорт, бизнес, элитный), у
«Пульса» их пять, и премиума между бизнесом и элитой у нас нет. Пока это не
мешает: кнопка «Рекомендация DevelopAid» шлёт только адрес и класс не передаёт
вовсе — он голосуется по соседям, то есть приходит от «Пульса», как и решено
для конструктора. Цена отсрочки в другом: выбранный в карточке класс на
ориентир не влияет, и для участка среди бизнеса ориентир будет бизнес-ценой,
даже когда в проекте выставлен «Элитный». Вводить четвёртый уровень — вместе с
решением, кто главнее: метка «Пульса» по месту или класс, заявленный в проекте.

### 4. Правило ±1 на уровне бизнеса

Соседний класс принят 10.08.2026 на элитном конце. У бизнеса соседи — комфорт
и премиум, выборка Кутузов Сити выходит 268 800…1 254 077 ₽/м². Сейчас общая
медиана и медиана своего класса печатаются рядом; не решено, оставлять так или
сузить правило до своего класса везде ниже премиума.

### 5. ПИРы в очередях: где именно неверно (22.08.2026)

**Вопрос.** «Почему совершенно неверно считаются ПИРы в очередях». По коду и
прогону движка расхождения не видно — нужен ваш пример.

**Что проверено.** ИРД, стадии П и РД считаются как ГНС × ставка × индекс
инфляции очереди. Румянцево одним проектом: 325 005 м² ГНС → стадия П
1 950,0 млн ₽ (ровно 6 тыс ₽/м²). Он же двумя очередями: 178 762 + 146 244 м²
→ 1 072,5 и 984,9 млн ₽ при индексе второй очереди ×1,122 — сходится до
0,1 млн. На умолчаниях та же картина. Итог по очередям выше одиночного расчёта
на 5,5% — это индексация второй очереди, а не ошибка.

**Что неясно.** Что именно считается неверным: сумма, разнесение по очередям
или срок. Есть содержательная развилка, которую стоит решить отдельно: ИРД и
стадия П — во многом разовые проектные затраты, а движок делит их между
очередями пропорционально метрам и заново начисляет каждой. Если ошибка в
этом, лечится не арифметикой, а тем, что часть ПИР уходит в общие затраты
проекта (`phasing.shared`), а не в очередь.

### 6. Статистики посещений и анкет нет по сути (22.08.2026)

**Вопрос.** «Проверить работу /statistics», «статистика не работает вообще ни
по пользователям, ни по анкетам, их нет по сути». Проверено — и это не
поломка: их действительно почти нечем показать.

**Посещений не существует как события.** Ни один заход на сайт в журнал не
пишется: `index()` отдаёт `PAGE` и ничего не учитывает, middleware в приложении
нет вовсе. Журнал знает только действия — `land` (поиск участка), `calc`,
`pdf`, `question`, `survey`, `invite` и обращения в бот. На вопрос «сколько
человек зашло» ответить нечем по устройству, а не по сбою.

**У не вошедшего не остаётся ни следа, ни анкеты.** Расчёт за входом (решение
владельца 18.08.2026), поэтому `usage_track("calc")` для него не случится. И
анкету он не увидит: `feedbackMaybeAsk` показывает её только после хотя бы
одного расчёта и шестидесяти секунд чтения отчёта (`FEEDBACK_READ_SECONDS`).
Кто пришёл, посмотрел участок и ушёл, для статистики не существовал.

**Что при этом работает.** Журнал ядра выкатку переживает — том
`$ROOT/data:/app/data` в `deploy-developaid.sh`, — а журнал бота на Render нет,
это уже записано правилом. Анкета сайта ложится в тот же журнал
(`usage_track("survey", surface="site")`), свод собирает обе половины и
подписывает, чьих строк сколько.

**Первое, что проверить на стенде.** `DEVELOPAID_ADMIN_IDS`: без него `/stats`
и `/survey` отвечают «Статистика закрыта» — снаружи неотличимо от «не
работает». Второе — ответило ли ядро на `/internal/usage/summary`.

**Решение владельца 23.08.2026: считаем зарегистрированных через телеграм.**
Анонимных идентификаторов браузера не заводим — значит политику и согласие
трогать не надо, а «сколько человек» отвечается по аккаунтам бота. Открытия
страницы всё равно писать стоит, но как отдельную величину: «сколько раз
открыли» и «сколько людей» — разные числа, и складывать их нельзя. Второе
требование того же решения: анкеты должны читаться, если их кто-то заполнил.

**И тут вскрывается главное: реестр пользователей уже есть, но лежит не там.**
`telegram_user_registry` держит sqlite со всеми, кто писал боту: user_id,
chat_id, username, имя, первый и последний визит, счётчики сообщений, кнопок,
обращений к Платону и выгрузок модели; поверх него команды `/users`,
`/users_today`, `/users_week`, `/user <id>`. Проблема в месте: реестр пишется
там, где обрабатывается вебхук, то есть на Render, в файл
`data/telegram_users.sqlite3` внутри контейнера. Диска там нет — **каждая
выкатка обнуляет ответ на вопрос «сколько зарегистрировалось»**. Профили
знакомства при этом целы: они лежат на ядре, в смонтированном томе.

**Что показали данные с ядра 23.08.2026.** Журнал с 6 по 23 августа: расчётов
711, поисков участка 271, вопросов Платону 74, PDF 28. Профилей знакомства —
**2**, анкет — **1**. Расчёты распались на два chat_id: 381 у одного (владелец)
и **330 под нулём**. Ноль — это не «никого не было»: `_web_identity_chat_id`
отдаёт ноль и когда сессии нет, и когда подпись не сошлась, и когда вошли
админским ключом (`DEVELOPAID_ADMIN_KEY` на ядре задан, как и
`TELEGRAM_BOT_TOKEN`, то есть гейт включён и расчёт без пропуска невозможен).
Владелец подтверждает: людей было минимум четверо. Значит они в этих 330 и
неотличимы друг от друга — **личность теряется в момент записи**.

Единственная анкета от 18.08.2026 тоже под нулём, роль «Девелопер». Оценки
в основном 4–5, провалы конкретны: **правка ТЭП участка — 2**, пресеты
вводных — 3. Отзыв, ради которого анкету заводили, лежал непрочитанным.

**Журнал прочитан 23.08.2026, и он оказался материалом, а не учётом.**

Нули по дням: 18.08 — 134 при 32 вошедших, 19.08 — 76 при 110, 20.08 — 91 при
111, 21.08 — 29 при 55, а с 22.08 нулей нет вовсе. Значит это не «до гейта»
(его включили 18.08): чужие считали 18–21 августа, и 22-го поток прекратился.
Заодно видно, что `calc` пишется только с 18.08 — раньше расчёты не
журналировались, и 711 это не «за всё время».

**Вопросы Платону обезличены не сессией, а кодом.** `usage_track("question")`
в `/agent/chat` вызывается **без `chat_id`** — соседний `/calculate` его
передаёт, а этот нет. Поэтому все 74 вопроса лежат под нулём при любой
исправной сессии. Правка однострочная.

**Что люди на самом деле спрашивают.** Из 74 вопросов подавляющее большинство
об одном: «за сколько максимум можно купить», «при какой цене проект
рентабелен», «подбери целевую цену покупки», «максимальная цена приобретения»,
«за сколько торгуемся». Это и есть работа, ради которой приходят, — и она уже
умеет считаться подбором параметра. Второй пласт — беспомощность в интерфейсе:
«как работать с сайтом», «как ввести ТЭП», «а где кнопка применить в модель»
(спрошено дважды, разными днями), «можешь как-то внести правки в расчёт?».
Это ровно то, что единственная анкета оценила двойкой за правку ТЭП и тройкой
за пресеты. Два независимых источника сошлись на одном.

**`TELEGRAM_ADMIN_IDS` не задан** — команда `/users` заперта, реестр
пользователей владельцу не виден.

**Решение владельца 23.08.2026: анкеты и пользователи — условие продажи.**
Порядок поэтому такой, и он не тот, что был записан выше.

**Работа, в порядке.**
1. **Перестать терять личность.** Писать в журнал способ входа (`session`,
   `key`, `none`) — три разных нуля перестанут быть одним. Показ по админскому
   ключу для чужих не годится: сколько бы человек им ни пришло, все они один
   ноль. Гостю — именной вход через бота или одноразовая ссылка с меткой, кому
   выдана.
2. **Спрашивать анкету в боте, а не только на сайте.** Сейчас её видит тот,
   кто вошёл, посчитал и минуту читал отчёт: одна анкета на 711 расчётов.
   У нас есть chat_id и канал — после первого расчёта бот может спросить сам.
3. **Короткая анкета.** `FEEDBACK_GROUPS` даёт 23 подпункта; это анкета на
   пять минут, и отвечает на неё один из сотни. Ключевой вопрос один —
   «посчитали бы этим настоящую сделку» — плюс свободный текст; подробные 23
   оставить для тех, кто готов.

4. Перенести реестр пользователей на ядро — туда же, где профили и проекты.
   Бот пишет в него внутренним путём с подписью токеном, как уже устроены
   `/internal/usage/summary` и `/internal/profile/announcements`. Пока реестр
   на Render, любое число из него живёт до следующей выкатки.
5. Свести два списка администраторов в один. Сейчас `/users` открывается по
   `TELEGRAM_ADMIN_IDS`, а `/stats`, `/survey`, ежедневная сводка и хранилище
   проектов — по `DEVELOPAID_ADMIN_IDS`. Одна роль, две переменные: заполнишь
   одну — половина статистики останется запертой, и не поймёшь почему.
6. Назвать вслух две разные величины. `/stats` считает «Зарегистрировано» по
   профилям знакомства, `/users` — по реестру бота; это «заполнил анкету
   знакомства» против «нажал Start», и разница между ними как раз и есть
   воронка. Показывать оба числа рядом, а не одно под общим словом.
7. Свод анкет по умолчанию за всё хранение, а не за тридцать дней (для
   `/stats` правило уже выведено, до анкет не дошло).
8. Событие визита в `index()` — отдельной величиной, без попытки посчитать по
   нему людей.

### 7. Класс проекта задаёт две ставки себестоимости, и одна из них живёт в двух местах (22.08.2026)

**Вопрос.** Настройки классов по себестоимости.

**Что есть.** `PROJECT_CLASS_PRESETS` — три уровня и пять чисел в каждом: цена
квартир, цена коммерции, цена машино-места и две ставки СМР
(`main_above_th_per_sqm`, `main_under_th_per_sqm`: 110 / 190 / 300). Всё
остальное строительство — ИРД, П, РД, подготовка, сети, благоустройство, сдача,
содержание площадки, вознаграждение ГП, технадзор, управление, резерв — общее
для всех классов из `DEFAULT_INPUTS`. То есть элитный проект отличается от
комфорта только основным строительством; проектирование у них по 6,0 и 8,5
тыс ₽/м² одинаково.

**Рядом вторая копия.** Бот берёт СМР класса не из пресета, а из своего
`_TELEGRAM_CLASS_SMR_PRESETS` (110 / 190 / 300). Сегодня числа совпадают, но
это два места на одну величину — та же болезнь, что была у `VERSION`: поправят
пресет, а бот останется на прежнем, и экспресс-расчёт разойдётся с сайтом
молча.

**Решение владельца 22.08.2026: у элиты отличается только благоустройство.**
Проектирование и наружные сети остаются общими для всех классов — разводить их
не надо. Значит работа такая: `landscaping_th_per_sqm` переезжает в
`PROJECT_CLASS_PRESETS` четвёртым числом класса, а `_TELEGRAM_CLASS_SMR_PRESETS`
исчезает — бот берёт ставки из пресета, как страница.

**Что осталось открытым.** Сама ставка благоустройства для элиты: сейчас у всех
11,5 тыс ₽/м² ГНС, и число для элитного уровня нужно от вас — придумывать его
нельзя. Заодно решить, различается ли благоустройство у бизнеса или ступеней
только две (комфорт и бизнес по 11,5, элита выше).

Рядом, но не то же самое: вопрос 3 — сколько уровней и кто главнее при
расхождении с «Пульсом»; здесь — что именно уровень задаёт внутри.

### 8. Сценарии и класс не знают локации, хотя региональные данные у нас есть (22.08.2026)

**Вопрос.** Настройки сценариев надо привязывать к локации; статистика по
регионам у нас есть.

**Что сейчас.** `SCENARIOS` — три жёстких набора на весь сервис:
консервативный 0,90 к выручке и 1,10 к затратам, базовый 1,00/1,00,
оптимистичный 1,10/0,90. Проект в Хамовниках и площадка в дальнем Подмосковье
получают один и тот же коридор ±10%. То же у класса: `PROJECT_CLASS_PRESETS`
не знает, где стоит проект, — «Комфорт» это 350 тыс ₽/м² и СМР 110 и на
Саввинской набережной, и в Балашихе, где официальная средняя 189 045 ₽/м².
Сценарий ключевой ставки (`rate_scenario`, 11/9/7%) к локации привязывать не
надо — он макро, и это верно как есть.

**Данные, которые уже лежат в проекте и никем для этого не используются.**
`data/mo_market_price.csv` — средняя рыночная стоимость 1 м² по муниципалитетам
Московской области с периодом и реквизитами распоряжения Комитета по ценам и
тарифам; `data/upks_oks_quarters.csv.gz` — УПКС по кадастровым кварталам;
«Пульс» — класс, прайсы и темп ДДУ соседних проектов с районом как жёсткой
отсечкой сопоставимости; `official_price` — официальная средняя из помесячного
отчёта о продажах. То есть спрашивать «а есть ли у нас цифры по региону»
не нужно — они есть, вопрос в том, что именно из них брать.

**Решение владельца 22.08.2026: привязывать себестоимость.** Не стартовую цену
и не ширину сценарного коридора — ставку СМР. И тут же вскрывается неприятное:
региональной статистики **по себестоимости** у нас как раз нет. Всё, что лежит
в проекте, — про цену продажи и про кадастр: `mo_market_price.csv` это средняя
рыночная стоимость жилья по муниципалитетам, УПКС — кадастровая стоимость,
«Пульс» и `official_price` — прайсы и сделки. Себестоимость строительства по
регионам ни один из них не даёт, и выводить её из цены продажи нельзя: это
разные величины, и в дорогом районе высокая цена значит не дорогую стройку, а
дорогую землю. Значит первый шаг — источник: укрупнённые нормативы цены
строительства Минстроя (приказы по субъектам, индексы пересчёта по кварталам),
свои замеры по проектам или прямая экспертная таблица от владельца. До этого
привязывать нечего.

**Что решать дальше.**
- Откуда брать разброс. Наблюдаемый по району (сколько на деле расходятся
  прайсы соседей) честнее экспертных ±10%, но на районе с двумя проектами он
  сам по себе шум — нужен порог по числу наблюдений, как у средних в своде.
- Как называть источник. Региональная поправка обязана быть подписана в отчёте
  тем же способом, что плата за ВРИ: муниципалитет, число, документ и период.
  Иначе она неотличима от нашей выдумки.
- Что делать, когда района нет в справочнике. Подставлять среднее по области
  молча нельзя — по тому же правилу, что «пустой результат проверки не значит
  чисто»: отсутствие данных по локации должно быть видно, а не заменяться
  средним.

### 9. Закрывать ли репозиторий (23.08.2026)

**Проверено:** `warstyle-plato/plato` сейчас **публичный** (`visibility: public`),
форков 0, звёзд 0, семь открытых issue.

**Что лежит открытым.** Не методика — с ней решать вам, — а чужие данные.
`presets/Румянцево.json` это весь проект целиком: сделка, экономика, ВРИ, МПТ,
техприсоединение, контрольные числа. Рядом `КРТ_Нагатино.json`,
`КРТ_Варшавское_Нагатинская.json`, `Мишина_ТЭП.xlsx`, `Мытищи_ТЭП.xlsx`.
В CLAUDE.md — номера пяти кредитных договоров Сбербанка с их условиями:
ставки, спреды, ступени лестницы, диапазоны комиссий. Последнее — чужая
коммерческая информация, и она там не под нашим решением.

**Секретов не нашёл.** По файлам искал ключи OpenAI и токены Telegram по их
шаблонам — только `.env.example`. Это не гарантия: по всей истории коммитов
сканером не гонял. Если закрывать именно из-за утечки — сначала прогнать
историю, потому что закрытие уже утёкший ключ не отзывает, отзывает ротация.

**Что закрытие не чинит.** Опубликованное остаётся у тех, кто успел склонить,
и в кэшах. Форков ноль — скорее всего, никто и не смотрел, но полагаться на
это нельзя.

**Что сломается — проверено по файлам.** Сборка ходит в Yandex Cloud по OIDC
(`build-yandex.yml`: `id-token: write`, аудитория
`https://github.com/warstyle-plato`, обмен токена GitHub на IAM), а образ
выкатка тянет из `cr.yandex`. Ни то ни другое от видимости не зависит: имя
репозитория и издатель токена не меняются, привязка федерации остаётся той же.
Ссылок на `raw.githubusercontent.com` и анонимных `git clone` в коде и
скриптах нет — искал.

Реальных рисков два, и оба не в коде. Первый: **минуты Actions становятся
платными** — на публичном репозитории они бесплатны, на приватном идут из
квоты, а среди девяти workflow есть сборка образа с Chromium. Перед закрытием
решить, что оставить включённым. Второй: **всё, что подключено к GitHub
снаружи** — Render, если он собирает бота из репозитория, и разрешение Claude
на доступ. Приложению GitHub нужно право на приватные репозитории; обычно оно
переживает смену видимости, но проверять это надо сразу после переключения, а
не через неделю. И третье, мелкое: если на ядре рабочая копия клонирована по
https без токена, `git pull` там перестанет работать — понадобится deploy key
или PAT.

**Рекомендация.** Закрыть. Причина не секреты, а чужие данные — договоры банка
и проекты клиентов. Отдельно от закрытия стоит вынести пресеты живых проектов
из репозитория (в том `data/`, как эталоны), иначе тот же вопрос вернётся с
каждым новым файлом вроде «Кутузов Сити».

