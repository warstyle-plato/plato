# Задачи и вопросы

Единственное место, где ведутся текущие задачи и открытые вопросы. CLAUDE.md
держит правила, выведенные из поломок, и устройство — список дел там не живёт,
там только ссылка сюда: копию негде обновлять, потому что копии нет.

Три раздела и разница между ними жёсткая:

- **В работе** — есть ветка или PR. Строка снимается, когда PR слит или закрыт.
- **Очередь** — решение делать принято, работа не начата.
- **Вопросы** — решение не принято. Это не задачи: пока на вопрос нет ответа
  владельца или разбора, брать в работу нечего.

Как добавлять: дата постановки, сам вопрос своими словами, затем — что по
этому поводу **уже есть в коде** (файлы и функции, чтобы через месяц не искать
заново) и что именно неясно. Отвеченный вопрос не стирается: под ним пишется
решение и дата, а сама работа переезжает в «Очередь». Сделанное, из которого
вышло правило, уезжает в CLAUDE.md.

---

## В работе

Состояние на 22.08.2026 — по открытым PR репозитория.

| PR | Что делает | Состояние |
| --- | --- | --- |
| [#204](https://github.com/warstyle-plato/plato/pull/204) `fix/monitor-readable-forecast` | Monitor: читаемый forecast, корпуса и финансовые полосы | черновик |
| [#183](https://github.com/warstyle-plato/plato/pull/183) `agent/pf-rate-tep-ui` | Ставка ПФ, пропорции ТЭП и нормальный ТЭП в Excel | черновик |
| [#171](https://github.com/warstyle-plato/plato/pull/171) `agent/market-statistics` | Market preview + statistics | черновик |
| [#51](https://github.com/warstyle-plato/plato/pull/51) `feature/mpt-calculator-v0.1` | Калькулятор МПТ отдельным сервисом | открыт с 07.08.2026, давно без движения — решить, доводить или закрывать |

Таблица собирается руками и потому врёт ровно настолько, насколько давно её не
трогали. Перед тем как на неё опереться, сверить с открытыми PR.

## Список от 22.08.2026 — сверка с кодом

Пятнадцать пунктов из ChatGPT, проверенных по коду 22.08.2026. Каждый пункт
несёт не мнение, а место в коде или прогон движка. Закрытые оставлены здесь
строкой, чтобы их не спрашивали заново; открытые расписаны ниже в «Очереди».

| № | Пункт | Состояние |
| --- | --- | --- |
| 1 | Дефолт ПФ в разделе «Результат» | частично — см. очередь |
| 2 | Лесенки процентов | **закрыто** |
| 3 | ТЭП очередей в обе стороны | частично — см. очередь |
| 4 | Коэффициенты МПТ | **закрыто**, но открыт PR #51 |
| 5 | Спрятать самостоятельный расчёт ВРИ | **закрыто** |
| 6 | Пропорции ТЭП одиночного проекта | **закрыто** |
| 7 | Отражение изменений в Excel | не проверено — нужен Excel |
| 8 | Распределение финансирования по очередям | **закрыто** |
| 9 | ПИРы в очередях | не воспроизвелось — нужен пример |
| 10 | Румянцево: ДОУ и СОШ в ТЭП | подтверждено, причина найдена |
| 11 | Поиск по КРТ с классом и ценой по округам | частично |
| 12 | Зона КРТ в градпроработке | открыто |
| 13 | Слои: ЛЭП по краю участка | частично, и главная половина не сделана |
| 14 | Блок `/statistics` | причина найдена — см. вопрос 6 |
| 15 | Статистика по пользователям и анкетам | причина найдена — см. вопрос 6 |

Что именно проверено по закрытым:

- **2. Лесенки процентов.** Поле `pf_special_steps` есть во вводных и в
  `FIELD_GROUPS`, ступени считает движок, действовавшую ступень называют отчёт,
  PDF (`_pdf_pf_step_rows`) и книга; `tests/test_escrow_rate_steps.py` зелёный.
- **4. Коэффициенты МПТ.** Кзатр 166,23078 с 01.01.2026 по приказу
  ДИПП-ПР-34/20 в редакции 10.03.2026, с квартальной индексацией и разговором
  о смене квартала; `test_mpt_audit_fixes`, `test_kzatr_quarterly_index`,
  `test_reference_freshness` зелёные. Отдельно висит PR #51 «MPT calculator
  v0.1 as isolated service» — с 07.08.2026 без движения, решить его судьбу.
- **6. Пропорции одиночного ТЭП.** `tepCellChanged`: любое из трёх чисел
  ведущее, два других считаются по пропорциям (`tepFillByRatios`); сами
  пропорции правятся и сбрасываются (`tepRatioSet`, `tepRatioReset`) и берутся
  из движкового `TEP_RATIOS` подстановкой, а не копией.
- **5. Самостоятельный расчёт ВРИ.** Уже убран решением владельца от
  20.08.2026: карточки на странице нет, её работу делает «Пересчитать под
  фактический ТЭП». Формула города осталась в движке (`vri_manual_payment`) —
  ею пересчёт подтверждает свою пропорцию, — и `/vri/manual` отвечает
  по-прежнему. Прятать больше нечего.
- **8. Финансирование очередей.** `phasing.financing_strategy` с двумя
  режимами — «независимый» и «единый поток проекта», — таблица «Финансирование
  очередей» и `tests/test_phase_financing_strategy.py`.

## Очередь

Пункты 1–6 достались из CLAUDE.md, 8 и 9 пришли с выгрузки ГлавАПУ,
остальные — из списка от 22.08.2026.

1. **Логика погашения БРИДЖа** в шаблоне и движке разная: в шаблоне остаток
   падает до нуля на четвёртый месяц (пик 700,8 млн ₽), в расчёте пик 1,00 млрд ₽.
2. **Опция «собственный капитал вместо БРИДЖа»** на входе в проект.
3. **Чистка шаблона** от листов и строк, не участвующих в расчёте (лист
   «Расчет ВРИ (ТЭП)» — остаток прежней методики). Выпиливать только после
   разбора зависимостей: на него ссылаются формулы.
4. **Инструмент чувствительности для Платона Сергеевича** — читать последний
   результат `/analysis/sensitivity` или запускать его. Числа считает движок,
   не модель.
5. **Градостроительный скрининг участка** — полная архитектура владельца в
   `docs/land_screening_architecture.md` (runtime-источники против локальной
   нормативной базы, слой конфигурации layer_id с canary, статусы чеков,
   запрет «участок подходит», MVP). Ниже — краткая выжимка. (Решение, 16.08.2026 —
   «это уже девелоперский сервис, а не просто финмодель»). Первичная
   град-проработка из открытых данных, красные флаги до экономики:
   - Этап 1: слои ЗОУИТ НСПД по bbox участка (тот же WMS, что подложка карты;
     id слоёв сверять живой пробой в духе `/land/map-probe`, не вслепую) →
     зоны с процентом накрытия, строимое пятно = контур минус ЗОУИТ; доля
     границы с ИЖС по ВРИ соседей (точечный поиск уже умеет); «физика
     посадки» — этажность/высота из СПП ГлавАПУ и пятна (пример владельца:
     22 тыс. продаваемых на пятне 0,3 га ≈ 28 этажей ≈ 100+ м).
   - Этап 2: подзоны ПЗЗ Москвы (открытые данные: плотность тыс. м²/га,
     высота в метрах, процент застройки, индекс «Ф») → автосверка
     нормативного ТЭП с пределами.
   - Этап 3: МО через РГИС; красные линии — только где внесены в ЕГРН
     (полнота фрагментарна — говорить об этом вслух).
   Места в интерфейсе (утверждены владельцем): блок в разделе «Участок» под
   карточкой участка (зоны поверх нашей карты); в отчёте раздел
   «Реализуемость посадки» ПЕРЕД «Оценкой целесообразности покупки»
   (`_PdfSection` — порядок штатный); главный флаг поднимается в карточку
   решения рядом с финвердиктом. Честные границы метода — в оговорке блока:
   геоподоснова 1:500 закрыта, сервитуты и чужие ГПЗУ не видны.
6. Четыре округа Подмосковья отсутствуют в Таблице 3 (Дзержинский, Протвино,
   Пущино, Электрогорск); справочник `vri_mo_ranges` не заполнен.
7. **Скрининг спрашивает точку, а не участок.** `_land_screen_findings(lat, lng)`
   опрашивает шестьдесят слоёв НСПД в одном пикселе — центре участка. Охранная
   зона ЛЭП, идущая полосой вдоль границы, туда не попадает, слой отвечает
   пусто, и вердикт говорит «критических ограничений не обнаружено» (кейс
   Коммунарки, владелец 22.08.2026). Прежняя правка про долю накрытия
   (`_land_coverage_shares`) чинила вторую половину вопроса — как измерить
   найденную зону; первую, как её найти, не трогала: геометрия берётся из того
   же ответа GetFeatureInfo. Чинить опросом по BBOX контура (или сеткой точек
   как запасным путём) и называть метод в ответе: «опрошен контур» против
   «опрошена одна точка» — это и есть разница между «проверено» и «похоже,
   чисто». Пока опрос точечный, зелёный вердикт проверкой участка не является,
   и в оговорке блока это должно стоять прямо. Место правки — вызов
   `_land_screen_findings(center["lat"], center["lng"], geometry)`: контур туда
   уже приходит, но идёт только в долю; сам запрос строит
   `_nspd_getfeatureinfo` одним пикселем тайла zoom 24.

8. **Норматив приобъектной парковки решён выгрузкой — свести код к одному
   числу.** Выгрузка ГлавАПУ по кварталу 77:05:0004001 (22.08.2026) отдала
   собственный ответ калькулятора, и наши формулы воспроизвели его до единицы:
   постоянные места жилья 470 по 2118-ПП, квартир 588, всего мест МКД 519
   (470 + 47 гостевых + 2 кратковременных), приобъектные встроенных 15 по
   **НП / 90 × К1 × К2**, всего встроенных 19 (15 + 4 кратковременных).
   Приложение 6 (офис 63, ТЦ 54) даёт на тех же метрах 22 и 25, пресетные
   100 — 14. Значит верна строка «90 × К1 × К2», а `MOSCOW_ATTACHED_PARKING_X2`
   и `project_preset.OFFICE_SQM_PER_SPACE` — два лишних мнения; свести к
   одному месту. Проверено на встроенно-пристроенных: в этой выгрузке СПП
   нежилых зданий нулевая, поэтому для отдельно стоящих офисов и ТЦ формула
   ещё не сверена — нужна выгрузка квартала с ними.

   Оттуда же: **площадь квартир = население × 33**, а не «доля жилой СПП»
   (на выгрузке 40 689 м² при СПП жилой 62 596 — отношение 0,65 вышло
   совпадением). И **кратковременные места считаются у обоих**: жильё
   `ceil(площадь квартир / 22 100)`, нежилое `min(4, ceil(НП / 450))` — их у
   нас не было вовсе. Коэффициенты квартала: К1 0,75, К2 0,5 (вне ТТК),
   аренда 0,1184, район Нагатино-Садовники, зона 1.

9. **Три норматива приобъектной парковки в одном коде.** `MOSCOW_ATTACHED_PARKING_X2`
   (приложение 6 к 945-ПП: офис 63, ТЦ 54 м²/место, Nв = X / X2 × К1 × К2),
   `tep_derived_norms` и `vri_tep_quick` (90 м²/место × К1 × К2) и
   `project_preset.OFFICE_SQM_PER_SPACE` (100 м²/место, без коэффициентов). На
   92 845 м² ГНС офисного объекта они дают 1 247, 873 и 786 мест при К1=К2=1 —
   и 188 при К1=0,75, К2=0,2. Свести к одному, как свели прогноз РНВ: пока их
   три, отчёт и книга разойдутся на подземной ГНС, а это миллиарды
   себестоимости. 2118-ПП переписал приложение 5 (жильё), приложение 6 не
   трогал — переписывать его вместе нельзя.

10. **Непогашенный ПФ на экране не назван дефолтом.** Плашка про РВЭ на
   странице есть (`pfRveWarning`: «Эскроу не погашает ПФ полностью»), а долг на
   конец проекта стоит нейтральной плиткой «Остаток ПФ» среди ставок и лимитов.
   Текст «Долг ПФ не погашается к концу проекта — для банка это риск дефолта»
   живёт только в карточке бота и строкой в PDF. Прибыль при этом
   положительная и стоит выше — на экране это читается как хороший проект.
11. **ТЭП очередей: правка одного поля не двигает связанные.** Абсолютные
   значения и доли правятся, потолок исходного ТЭП держится
   (`phaseProductTepLimit` и предупреждение «Значение ограничено до …:
   доступный остаток исходного ТЭП»), — эта половина сделана. Не сделана
   вторая: `phaseProductDerived` считает каждое поле от своей доли независимо,
   поэтому правка ГНС очереди не пересчитывает продаваемую, как это устроено в
   одиночном ТЭП. Колонки «общая площадь» в таблице очередей нет вовсе — цепочка
   ГНС → общая → продаваемая там неполна по составу.
12. **Соцобъекты не видны в ТЭП, потому что у них нет ГНС.** Подтверждено на
    Румянцеве без скриншотов: после импорта пресета ДОУ несёт `total_area`
    4 764,9 м², СОШ — 9 265,1 м², а `gns` и `saleable` у обоих нули. Обе
    таблицы — вкладка «Результат» (`reportTep`) и ТЭП очередей (`phaseTepBody`) —
    печатают только ГНС · продаваемая · шт., поэтому строки выглядят пустыми,
    хотя объекты строятся. Рядом второе: у ДОУ `transfer` остался 3 000 м² из
    `TEP_DEFAULT` при площади 4 764,9 — импорт поле не перезаписал (то самое
    «поле, которого нет в карте записи, молча остаётся мусором из шаблона»).
13. **Поиск по КРТ с классом и ценой по округам.** Сейчас есть фильтр по
    округам, статусу и назначению и скоринг `krtFit` по ТЭП; класс и цена
    приходят только по одному выбранному проекту кнопкой «Получить маркетинг и
    прогнать модель». Массового отбора «наш класс и наша цена по округу» нет.
14. **Зона КРТ в градпроработке.** В `_NSPD_SCREEN_LAYERS` только слои НСПД;
    krt.mos.ru со скринингом участка не связан. Показывать, что участок входит
    в утверждённую зону КРТ или граничит с ней.
15. **Проверить, что изменения доехали в Excel.** В песочнице не проверяется:
    openpyxl формулы не считает, LibreOffice сломан. Нужен прогон
    `audit_plato_workbook` на книге, пересчитанной Excel и сохранённой. Отдельно
    сверить то, что появилось последним: ТЭП очередей и лестницу ставок.

## Вопросы

### 1. Ориентир максимальной цены покупки на аукционе КРТ (22.08.2026)

**Вопрос.** В блоке КРТ, наверное, стоит давать ориентир максимальной цены
покупки на аукционе — сколько можно поднять ставку, пока проект остаётся
проходным.

**Что уже есть.** `auction_search/krt_screening.py` считает `entry_capacity`
через `_goal_seek_entry_capacity`: подбор `purchase_price_mln` при целевом
LLCR 1,20x (`TARGET_LLCR`), на слабейшей очереди в фазированном режиме. То есть
число уже считается движком, а не рядом с ним. Наружу оно выходит как «Резерв
при LLCR 1,20x» в карточке лота (`auction_search/ui.py`) с подписью
«предельный резерв по переменной "цена входа"».

**Что неясно.**
- Резерв при LLCR 1,20x — это ещё не предельная ставка на торгах. Из него не
  вычтены обязательства КРТ, которых в каталоге нет: плата за ВРИ, переселение,
  соцобъекты, внеплощадочные сети (они перечислены в `exclusions` того же
  расчёта). Назвать резерв «максимальной ценой» — значит показать заведомо
  завышенный потолок под уверенным именем. Решить: показываем потолок только
  когда обязательства оцифрованы, или показываем всегда с явным
  «до обязательств».
- По какому критерию считать потолок. LLCR 1,20x — банковский порог
  проходимости; у покупателя на торгах критерием может быть маржа или прибыль.
  Возможно, нужны две линии: «выше этого банк не финансирует» и «выше этого
  проект не окупается».
- Один потолок или лестница. Аукцион идёт шагами, и полезнее видеть, что даёт
  каждый шаг: при такой ставке LLCR такой, при следующей — такой.

### 2. Стартовая цена аукциона из документов КРТ (22.08.2026)

**Вопрос.** Нужна цифра старта аукциона из документов КРТ.

**Что уже есть.** У лота ЭТП поле есть — `AuctionLot.start_price_rub`
(`auction_search/models.py`), оно доезжает до пресета и до карточки
(`preset_mapper.py`, `developaid_mapper.py`). А у проекта из официального
каталога krt.mos.ru его нет: `auction_search/krt.py` вынимает из документов
программу (`extract_krt_program`) и обязательства (`extract_krt_obligations`),
цену — нет. Поэтому скрининг КРТ и стартует с `purchase_price_mln: 0.0`.

**Что неясно.**
- Где эта цифра лежит в документах КРТ и одна ли она: начальная цена права,
  задаток, шаг аукциона — разные числа, и брать «первое похожее на деньги»
  нельзя. Нужна пара живых комплектов документов, чтобы увидеть формулировки,
  а не гадать по шаблону.
- Что делать, когда цифры в документах нет. По правилу «пустой результат
  проверки — не "чисто"»: отсутствие стартовой цены должно быть видно как
  «не нашли», а не как ноль, иначе оно неотличимо от бесплатного входа — ровно
  то, чем сейчас является `purchase_price_mln: 0.0`.

**Осторожно с именем.** В коде уже два разных «старта»:
`AuctionLot.start_price_rub` — начальная цена лота на торгах (₽ за лот), а
`start_price` в `krt_screening.py` — стартовая цена продажи квартир (₽/м²,
приходит из маркетинга), в ответе она лежит как `market.start_price_rub_sqm`.
Третье поле с корнем «start» без второй базы в имени читаться не будет —
правило про `*_per_gns_th` / `*_per_saleable_th` здесь то же самое.

### 3. Классы в DevelopAid — отложено 18.08.2026, «возможно, потом»

Сейчас `PROJECT_CLASS_PRESETS` знает три уровня (комфорт, бизнес, элитный), у
«Пульса» их пять, и премиума между бизнесом и элитой у нас нет. Пока это не
мешает: кнопка «Рекомендация DevelopAid» шлёт только адрес и класс не передаёт
вовсе — он голосуется по соседям, то есть приходит от «Пульса», как и решено
для конструктора. Цена отсрочки в другом: выбранный в карточке класс на
ориентир не влияет, и для участка среди бизнеса ориентир будет бизнес-ценой,
даже когда в проекте выставлен «Элитный». Вводить четвёртый уровень — вместе с
решением, кто главнее: метка «Пульса» по месту или класс, заявленный в проекте.

### 4. Правило ±1 на уровне бизнеса

Соседний класс принят 10.08.2026 на элитном конце. У бизнеса соседи — комфорт
и премиум, выборка Кутузов Сити выходит 268 800…1 254 077 ₽/м². Сейчас общая
медиана и медиана своего класса печатаются рядом; не решено, оставлять так или
сузить правило до своего класса везде ниже премиума.

### 5. ПИРы в очередях: где именно неверно (22.08.2026)

**Вопрос.** «Почему совершенно неверно считаются ПИРы в очередях». По коду и
прогону движка расхождения не видно — нужен ваш пример.

**Что проверено.** ИРД, стадии П и РД считаются как ГНС × ставка × индекс
инфляции очереди. Румянцево одним проектом: 325 005 м² ГНС → стадия П
1 950,0 млн ₽ (ровно 6 тыс ₽/м²). Он же двумя очередями: 178 762 + 146 244 м²
→ 1 072,5 и 984,9 млн ₽ при индексе второй очереди ×1,122 — сходится до
0,1 млн. На умолчаниях та же картина. Итог по очередям выше одиночного расчёта
на 5,5% — это индексация второй очереди, а не ошибка.

**Что неясно.** Что именно считается неверным: сумма, разнесение по очередям
или срок. Есть содержательная развилка, которую стоит решить отдельно: ИРД и
стадия П — во многом разовые проектные затраты, а движок делит их между
очередями пропорционально метрам и заново начисляет каждой. Если ошибка в
этом, лечится не арифметикой, а тем, что часть ПИР уходит в общие затраты
проекта (`phasing.shared`), а не в очередь.

### 6. Статистики посещений и анкет нет по сути (22.08.2026)

**Вопрос.** «Проверить работу /statistics», «статистика не работает вообще ни
по пользователям, ни по анкетам, их нет по сути». Проверено — и это не
поломка: их действительно почти нечем показать.

**Посещений не существует как события.** Ни один заход на сайт в журнал не
пишется: `index()` отдаёт `PAGE` и ничего не учитывает, middleware в приложении
нет вовсе. Журнал знает только действия — `land` (поиск участка), `calc`,
`pdf`, `question`, `survey`, `invite` и обращения в бот. На вопрос «сколько
человек зашло» ответить нечем по устройству, а не по сбою.

**У не вошедшего не остаётся ни следа, ни анкеты.** Расчёт за входом (решение
владельца 18.08.2026), поэтому `usage_track("calc")` для него не случится. И
анкету он не увидит: `feedbackMaybeAsk` показывает её только после хотя бы
одного расчёта и шестидесяти секунд чтения отчёта (`FEEDBACK_READ_SECONDS`).
Кто пришёл, посмотрел участок и ушёл, для статистики не существовал.

**Что при этом работает.** Журнал ядра выкатку переживает — том
`$ROOT/data:/app/data` в `deploy-developaid.sh`, — а журнал бота на Render нет,
это уже записано правилом. Анкета сайта ложится в тот же журнал
(`usage_track("survey", surface="site")`), свод собирает обе половины и
подписывает, чьих строк сколько.

**Первое, что проверить на стенде.** `DEVELOPAID_ADMIN_IDS`: без него `/stats`
и `/survey` отвечают «Статистика закрыта» — снаружи неотличимо от «не
работает». Второе — ответило ли ядро на `/internal/usage/summary`.

**Что решать.** Заводить ли событие визита — и если да, то как считать людей,
не строя своей аналитики и не записывая лишнего о тех, кто ничего не делал
(журнал хранит содержание обращений 180 дней, и политика это обещает).
И спрашивать ли анкету у не вошедшего: сейчас её видит только тот, кто дошёл
до отчёта, то есть самый лояльный, — а нужен как раз тот, кто бросил.

### 7. Класс проекта задаёт две ставки себестоимости, и одна из них живёт в двух местах (22.08.2026)

**Вопрос.** Настройки классов по себестоимости.

**Что есть.** `PROJECT_CLASS_PRESETS` — три уровня и пять чисел в каждом: цена
квартир, цена коммерции, цена машино-места и две ставки СМР
(`main_above_th_per_sqm`, `main_under_th_per_sqm`: 110 / 190 / 300). Всё
остальное строительство — ИРД, П, РД, подготовка, сети, благоустройство, сдача,
содержание площадки, вознаграждение ГП, технадзор, управление, резерв — общее
для всех классов из `DEFAULT_INPUTS`. То есть элитный проект отличается от
комфорта только основным строительством; проектирование у них по 6,0 и 8,5
тыс ₽/м² одинаково.

**Рядом вторая копия.** Бот берёт СМР класса не из пресета, а из своего
`_TELEGRAM_CLASS_SMR_PRESETS` (110 / 190 / 300). Сегодня числа совпадают, но
это два места на одну величину — та же болезнь, что была у `VERSION`: поправят
пресет, а бот останется на прежнем, и экспресс-расчёт разойдётся с сайтом
молча.

**Решение владельца 22.08.2026: у элиты отличается только благоустройство.**
Проектирование и наружные сети остаются общими для всех классов — разводить их
не надо. Значит работа такая: `landscaping_th_per_sqm` переезжает в
`PROJECT_CLASS_PRESETS` четвёртым числом класса, а `_TELEGRAM_CLASS_SMR_PRESETS`
исчезает — бот берёт ставки из пресета, как страница.

**Что осталось открытым.** Сама ставка благоустройства для элиты: сейчас у всех
11,5 тыс ₽/м² ГНС, и число для элитного уровня нужно от вас — придумывать его
нельзя. Заодно решить, различается ли благоустройство у бизнеса или ступеней
только две (комфорт и бизнес по 11,5, элита выше).

Рядом, но не то же самое: вопрос 3 — сколько уровней и кто главнее при
расхождении с «Пульсом»; здесь — что именно уровень задаёт внутри.
