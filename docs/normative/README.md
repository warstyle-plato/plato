# Нормативный пакет Москвы — ВРИ, соцкомпенсация, парковка

Свод прислан владельцем 20.08.2026 вместе с оригиналом 2118-ПП. Здесь лежит
сам файл (`DevelopAid_normative_pack_20260820.pdf`) и выжимка тех величин,
которые движок берёт числами. Числа продублированы в коде
(`main_legacy.py`, блок «нормативные справочники Москвы») версионируемыми
таблицами: у каждой есть дата действия и документ, и старое значение не
затирается новым — плата зависит от даты возникновения обязательства.

**Оговорка самого свода:** это не заверенная правовая подборка. При расхождении
с официальным текстом акта применяется акт в редакции на соответствующую дату.

## Что подтверждено оригиналом

**2118-ПП от 05.08.2026** — текст постановления у нас есть целиком. Приложение 5
к 945-ПП изложено в новой редакции:

- п. 1 (градостроительное проектирование): `Nп = S / (S₁ × 2,1) × D`,
  S — площадь квартир, S₁ = 33 м² на жителя, 2,1 — размер домовладения,
  D = 0,8 места на квартиру;
- п. 2 (со стадии АГР): `Nп = F₁×0,8 + F₂×1,2 + F₃×1,6` по составу квартир
  (до 70 м² / 70–100 / свыше 100 вместе с ИЖС и блокированными);
- К1 в постоянных местах жителей отсутствует;
- переходные положения (п. 2.1): разрешение на строительство, свидетельство
  АГР, одобренная 3D-модель на 05.08.2026, ввод до 01.01.2028 при
  положительном заключении экспертизы. Применение новой редакции для них —
  право, а не обязанность (п. 2.2), поэтому режим задаётся полем
  `parking_norm_regime`, а не выводится из даты.

Проверено на выгрузке калькулятора: 77 696 м² квартир → 897 мест, ровно как в
файле города.

## Что взято из свода числами

| Величина | Значение | Документ |
|---|---|---|
| Коэффициент изменения цен k1 | 1,9296 с 30.07.2026 | приказ ДГИ Москвы № 303 от 30.07.2026 |
| Функциональный коэффициент ВРИ | жильё 1,0 · временное проживание 1,0 · иное нежилое 0,001 | 593-ПП |
| УУПСС, выпуск 25 (уровень цен 01.01.2026) | ДОО 4 329,61 / 4 728,76 · СОШ 4 283,72 / 4 490,91 · поликлиника 7 707,29 / 7 924,58 тыс ₽ за единицу (без ТП / с ТП) | приказ Москомэкспертизы МКЭ-ОД/26-14 от 03.03.2026 |
| Приобъектная парковка, X2 | офис 63 м²/место · ТЦ 54 · магазин 63 · общепит 54 | 945-ПП, приложение 1 (реквизит исправлен 24.08.2026, см. `parking_sources_20260824.md`) |
| Квартал 77:01:0004023, Pi | жильё 287 560,46 · транспорт 89 143,74 · торговля 194 737,19 · временное проживание 206 274,93 · офис 187 578,99 · ИЖС 123 651,00 ₽/м² | 273-ПП, приложение 8, таблица 1 |
| Квартал 77:01:0004023, Kr | 0,1497 | 273-ПП, приложение 8, таблица 2 |

Контрольный квартал сходится с выгрузкой калькулятора по тому же участку — это
и есть проверка правильности импорта справочника.

## Что осталось нерешённым

- **593-ПП считает от кадастровой стоимости**: `П = (КС2 − КС1) × k1 × K`. КС1 и
  КС2 у нас нет ни из выгрузки, ни из анализа, поэтому этот путь пока не
  реализован. Действующий путь — пересчёт по параметрам исходного расчёта
  ГлавАПУ, где ставка снята с самой выгрузки.
- **Какая колонка УУПСС нужна** — с технологическим присоединением или без —
  свод не решает и прямо велит сверить по приказу № 141 и 3135-ПП. Пока не
  сверено, компенсация считается ставками исходного расчёта, а не УУПСС.
- **Аренда городской земли (273-ПП)**: п. 3.2.1 — повышенная составляющая
  первого года считается без площадей иного нежилого назначения. Режим аренды
  отдельно не реализован; при смене права пересчёт отказывается и просит
  свежую выгрузку.

## Что уточнено позже

24.08.2026 владелец сверил действующие редакции. Три вещи меняют то, что здесь
написано, и лежат отдельным файлом — `parking_sources_20260824.md`:

- **X2 живёт в приложении № 1**, а не в приложении № 6: у 945-ПП приложение № 6
  несёт формулу и К1, приложение № 3 — К2 по районам. В таблице выше реквизит
  поправлен.
- **База московского норматива названа сноской <2> приложения № 1: «нежилая
  наземная площадь».** Наша строка приобъектных мест делит её на 90, акт даёт
  63 — расхождение в 1,43 раза, и до примечаний приложения № 6 его не разрешить.
- **СП 42.13330.2016 не действует с 12.07.2026** — заменён СП 42.13330.2026.

Числа того файла в расчёт не заведены: он вторичный источник, а с нашей машины
правовые базы закрыты.

27.08.2026 владелец прислал постановление о правках 1557-ПП и 2152-ПП
(реквизиты в тексте не проставлены): нормативы озеленения, детских площадок и
плоскостного спорта разрешено заменять денежной компенсацией в бюджет через
инфраструктурный договор по решению ГЗК; в Садовом кольце малые участки
(<0,5 га) и реконструкция без нового строительства могут не предусматривать
озеленение вовсе. Порядок расчёта компенсации определит акт ДГП — его ещё нет,
поэтому в движок ничего не заведено. Файл и выжимка —
`greenery_sport_compensation_20260827.pdf` и `greenery_compensation_20260827.md`.

## Чего делать нельзя

- Путать K2 приобъектной парковки (деловая активность территории) с
  функциональным коэффициентом ВРИ 0,001 из 593-ПП: разные таблицы, разный
  смысл. В коде они называются `parking_business_activity_coeff` и
  `vri_function_coeff`.
- Масштабировать общий ВРИ по общей ГНС при смене функционального состава —
  считать по функциям.
- Зашивать k1 и УУПСС навсегда: обе величины версионируемые.

## Объекты образования — РНГП Москвы (прислано 02.09.2026)

`education_rngp_20260902.pdf` — выдержка из РНГП (приложение к 945-ПП) по
объектам образования, в действующих редакциях: зоны нормирования по п. 1.3.2 в
редакции 1762-ПП от 23.07.2025, нежилая наземная площадь — 2579-ПП от
28.10.2025, площадь земельного участка школы — 3144-ПП от 24.12.2024.

Числами в движок взято:

| Величина | Значение | Где в коде |
|---|---|---|
| Обеспеченность ДОО | 44 места/1000 (зона 1) · 63 (зона 2) | `MOSCOW_SOCIAL_PLACES_PER_1000` |
| Обеспеченность СОШ | 90 мест/1000 (зона 1) · 124 (зона 2) | там же |
| Нежилая наземная площадь ДОО | до 125 мест — 27 м²/место · 125–250 — 18 · свыше 250 — 16 | `MOSCOW_SOCIAL_AREA_PER_PLACE` |
| Нежилая наземная площадь СОШ | до 550 мест — 18 м²/место · 551–1000 — 15 · свыше 1001 — 13 | там же |
| Состав второй зоны | 28 районов, п. 1.3.2.2 | `MOSCOW_ZONE_TWO_DISTRICTS` |

**Площадь на место — ступень по ёмкости здания, а не одно число.** Маленький
садик стоит 27 м² на место, крупный 16: одно число на все ёмкости ошибается тем
сильнее, чем меньше объект. Поля вводных (`social_dou_norm_sqm` = 12,
`social_school_norm_sqm` = 13) несут одно число на любую ёмкость и **ниже
минимума города**: у ДОО минимум 16 при любой ёмкости, у школы 13 — только
свыше 1001 места. Там, где площадь считаем мы сами (раскладка объёмов площадки
КРТ), берётся норматив города по ёмкости и записывается в то же поле — иначе
страница показывала бы не то, чем посчитано. Сами умолчания полей не тронуты:
их правка меняет ТЭП всех сохранённых проектов и остаётся за владельцем.

Чего в этом документе нет: **поликлиники** — он про образование, и норматив
15 м² на посещение остаётся вводным полем, а не нормой города. Земельные
участки (35/32/20 м² у ДОО, 19/16/14 у школы) прочитаны, но в расчёт не
заведены: пятно застройки движок не считает.

Отдельно из этого же документа поправлено то, что нормативом не является:
список второй зоны хранился с «ё» («Бирюлёво Восточное»), а город и каталог
пишут «Бирюлево» — приведение `replace("ё", "ё")` не делало ничего, и район
второй зоны читался как первая, давая 44 места вместо 63.
