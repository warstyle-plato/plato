# Индекс исторических правил CLAUDE

Собирается из архива: `python3 scripts/claude_rule_index.py --write`.
Руками не править — правка потеряется при следующей сборке.
Полный снимок прежнего файла: `docs/CLAUDE_HISTORY_2026-09-20.md`.
Ищите здесь по имени файла, функции, маршруту, предмету или ключевому слову; затем открывайте только нужный фрагмент архива.

- L8 [setup] `main_legacy.py` — движок (~17 000 строк): расчёт, страница (`PAGE`), Telegram-бот,
- L10 [setup] `main.py` — обёртка: грузит движок как `developaid_core`, монкей-патчит поведение
- L12 [setup] Бот и движок — **один процесс**, не разные сервисы.
- L13 [setup] Разворачивается двумя воркерами (`--workers 2`). Память у них раздельная:
- L19 [rules] Токен Telegram-сессии не опознаёт проект.
- L21 [rules] Формулы шаблона ПЛАТО не перезаписываются
- L24 [rules] Поле, которого нет в карте записи, молча остаётся мусором из шаблона.
- L27 [rules] `JSON.stringify` превращает пропуск в массиве в `null`.
- L29 [rules] Отсутствующий ключ — не «снято».
- L34 [rules] Сохранённое состояние браузера накладывается на умолчания, не заменяет их.
- L36 [rules] Тяжёлое не считается внутри запроса, от которого зависит окно.
- L38 [rules] Ошибка, ушедшая только в лог, — это ошибка, которой нет.
- L40 [rules] Поверхности считают один раз, а не каждая своё.
- L47 [rules] Методику меняют в двух местах: в движке и в книге.
- L55 [rules] Формула книги живёт в горизонте движка и на его базах.
- L69 [rules] Импорт ГлавАПУ включает то, для чего принёс данные.
- L74 [rules] Рассрочка ВРИ начинается в дату обязательства, а не периодом позже.
- L80 [rules] Снятый признак банковского бюджета ВРИ — тоже потеря, а не решение.
- L86 [rules] Заданная плата за ВРИ платится, даже если признак снят.
- L91 [rules] График платежей ВРИ доезжает в книгу отдельно от суммы.
- L98 [rules] Версия объявляется один раз — `main_legacy.VERSION`.
- L115 [rules] Маршрут Платона решает `PLATO_AI_URL`, а не наличие ключа.
- L122 [rules] «Зона есть» — не ответ, ответ — сколько участка она съела.
- L132 [rules] Пустой результат проверки — не «чисто».
- L139 [rules] Счёт виден, вывод — за входом (решение владельца, 18.08.2026).
- L147 [rules] Личность подтверждает телеграм, анкета — нет.
- L154 [rules] Ожидание без признака работы читается как внезапность.
- L161 [rules] Эмблема одна на все поверхности, и она лежит в `PAGE`.
- L167 [rules] Документ ИП живёт не только на главной.
- L173 [rules] Зашитая цифра на экране неотличима от посчитанной.
- L183 [rules] Импорт участка обнуляет то, что участку принадлежит.
- L192 [rules] Зелёный набор тестов не значит, что ветка кода верна — возможно, до неё просто не доходят.
- L197 [rules] Капитализация процентов лимит ПФ не выбирает и не уменьшает
- L203 [rules] Методика финансирования сверена с НКЛ Сбера
- L216 [rules] В компенсацию за соцобъекты входит земля, а земля у каждого квартала своя.
- L224 [rules] Нормативный пакет лежит файлом, а числа из него — версионируемым рядом.
- L238 [rules] Места постоянного размещения — приложение 5 к 945-ПП в редакции 2118-ПП
- L251 [rules] Коэффициенты парковки берём у ГлавАПУ, своего справочника районов нет
- L261 [rules] Территорию город уже посчитал — пересчитывают количество, а не методику
- L278 [rules] Калькулятор считает по нормативному ТЭП, а строят по утверждённому.
- L294 [rules] Коэффициент выбирают по признаку, а не по удобству.
- L304 [rules] Формула платы за ВРИ сверена с исходником калькулятора ГлавАПУ
- L318 [rules] Методику города сверять по выгрузкам, а не подгонять по итогу.
- L333 [rules] Специальная ставка в договорах ступенчатая — сделано (20.08.2026).
- L347 [rules] Доля из документа и доля из методики — разные числа, и подписывать их одинаково нельзя.
- L359 [rules] Фактический ТЭП вводится руками, второй колонки рядом с нормативом нет
- L367 [rules] Переданные муниципалитету метры строятся, но не продаются.
- L380 [rules] Профиль расходов, объявленный дважды, расходится там, где считают ступень.
- L395 [rules] Гостевые машино-места строятся, но не продаются, и обе книги это забывали.
- L407 [rules] Кладовые прибавляют подземную площадь, а не вычитаются из гаража
- L465 [rules] Удельный показатель без второй базы читается как другой показатель.
- L471 [rules] Правило, применённое к боту, надо применить и к сайту.
- L477 [rules] Порядок разделов PDF задаётся при сборке, а не порядком вычислений.
- L481 [rules] Признак телеграма — параметры запуска, а не SDK.
- L487 [rules] Чужой DOM не трогать: React считает свои узлы своими.
- L493 [rules] Селектор на чужой странице живёт парой с диагностикой.
- L500 [rules] Запасной ответ не живёт в кэше наравне со штатным.
- L504 [rules] Длинный ответ забирается вторым запросом, а не одним соединением.
- L510 [rules] Работу не держат соединением — её принимают.
- L523 [rules] Список полей и умолчания страница берёт из движка, а не хранит свои.
- L533 [rules] Модуль не заводит своего пути туда, где у движка уже есть общий, — берёт крючком.
- L548 [rules] До Render с ядра не дойти — направление развёрнуто (06.08.2026).
- L558 [rules] Собственные средства до ПФ — бесплатные, это решение владельца (06.08.2026).
- L567 [rules] Пустая выгрузка — не «людей не было», а «журнал не пережил выкатку».
- L578 [rules] Документ обещает то, что делает код, — иначе он неверен, а не неточен.
- L590 [rules] Оценка без автора — не обратная связь, а цифра.
- L600 [rules] Одна оценка — не анкета, а две записи на неё — двойной счёт.
- L611 [rules] Учёт обращений стоит там, где они сходятся, а не по веткам разбора.
- L619 [rules] Слой перестройки `/ia` живёт на чужой разметке — значит, на диагностике.
- L627 [rules] Данные страницы объявлены `let`, и свойствами `window` они не становятся.
- L633 [rules] Страница пересчитывает модель сама, а шапка обещает обратное.
- L639 [rules] В книгу уходит посчитанная льгота, а не поле «льгота — сумма».
- L648 [rules] Присланный проект — снимок, а не общий доступ.
- L659 [rules] Личность пользователя одна — chat_id из бота, и сайт её не дублирует.
- L671 [rules] Поисковый сниппет содержит слова запроса — значит и адрес объекта оценки.
- L682 [rules] Точность геокодера — часть ответа, а не подробность.
- L688 [rules] Тип страницы решает, что с неё можно взять.
- L695 [rules] Терминаторы заголовка пишут ASCII-дефисом.
- L698 [rules] Ключ сущности считают от очищенного имени, а не от сырого заголовка.
- L706 [rules] Диапазон правдоподобия не заменяет разбор числа.
- L711 [rules] Глиф без буквы выбрасывать нельзя — его надо показать.
- L721 [rules] Район — жёсткая отсечка сопоставимости, решение владельца (11.08.2026).
- L728 [rules] Строку итога прогона читать до слияния, отдельной командой.
- L732 [rules] Проверка цепляется за контракт, а не за соседнюю строку.
- L746 [rules] Два прогона в один файл — ни одного прогона.
- L755 [rules] Метка перечисления и метка прозы — разные вещи, и тест на своей форме ошибку в чужой не ловит.
- L765 [rules] Класс в конструкторе отчётов ставит «Пульс», своего суждения нет
- L774 [rules] Занятый порт называет владельца, а не приглашает расчистить.
- L786 [rules] Класс считаем так, как считает источник — включая дробные метки
- L800 [rules] Соседний класс расширяет выборку тем сильнее, чем ниже уровень.
- L807 [rules] Журнал пишется там, где обслужен запрос, а читают его в другом месте.
- L815 [rules] Выкатка не уводит прод на выпуск назад молча.
- L837 [rules] Выкатка убирает за собой, иначе диск кончается молча.
- L859 [rules] Размещение соцобъектов объявляют объектами, а не метрами в фазе.
- L875 [rules] Очередь строит то, что написано рядом с её именем, — или имя врёт.
- L897 [rules] Чей это термин — часть самого термина
- L912 [rules] Подземная часть в ГНС не входит — закрыто 04.09.2026.
- L942 [rules] Что задал город и что придумали мы — разные разделы отчёта
- L953 [rules] В отчёте для инвестора нет истории наших правок.
- L965 [rules] Скрининг каталога ставит соцобъекты первым очередям — иначе балл снимает за нашу же раскладку.
- L983 [rules] Балл — из одного источника чисел, а не из той копии, что успела загрузиться.
- L996 [rules] Балл один, и расчёт его снижает, а не заменяет собой
- L1012 [rules] Балл у лота тем же правилом, что у КРТ.
- L1022 [rules] Платон стоит там, где смотрят, а не на соседней странице.
- L1031 [rules] Новое в каталоге называется вслух.
- L1040 [rules] Посчитанное не выбрасывают.
- L1055 [rules] Вводные едут вместе с результатом, иначе их соберут второй раз.
- L1064 [rules] Присланный проект накладывается на умолчания одним способом.
- L1072 [rules] Ограничение при вводе не спасает от правки задним числом.
- L1084 [rules] Проверка равенством целиком запрещает добавлять, а не утверждает.
- L1096 [rules] Сверка живёт рядом с базой, а не рядом с суммой.
- L1111 [rules] Что входит в МКД, объявлено один раз
- L1119 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1130 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1141 [rules] Рамка запроса шире участка — значит соседнее не вердикт.
- L1157 [rules] Скрининг спрашивал точку, а не участок — теперь контур.
- L1174 [rules] Слитый PR закончен: дописывать в его ветку нельзя.
- L1193 [rules] Состояние PR и его описание — разные утверждения, и расходятся они молча.
- L1201 [rules] Поверхность, добавленная позже, в проверку списком не попадает.
- L1215 [rules] Точка карты и точка отчёта — одна и та же точка, иначе смотрят на одну, а считают по другой.
- L1232 [rules] Скрининг КРТ читал доли ТЭП мимо разбора движка.
- L1243 [rules] Незакрытая кавычка в `PAGE` убивает продукт целиком и молча.
- L1261 [rules] Российский госсайт не проверить обычным хранилищем корней, и это не «сеть закрыта».
- L1303 [rules] Очереди — один налогоплательщик, и книга считает так же
- L1337 [rules] Умолчание, которое утверждает, — не умолчание.
- L1352 [rules] Умолчание, которое можно переписать, — уже не умолчание.
- L1383 [rules] Параметр, ухудшающий выдачу, — не безобидный.
- L1394 [rules] Отсутствие фильтра — тоже ответ, и его надо измерить.
- L1406 [rules] «Не проверили» и «не заполнено» — разные ответы, и на экране тоже.
- L1420 [rules] Карта, которую крутят, считает тот же меркатор, что карта, которую печатают.
- L1445 [rules] Заполненный контур перехватывает указатель на всей своей площади.
- L1454 [rules] `textContent` правит строку целиком и сносит вложенные узлы.
- L1462 [rules] Экранированное значение внутри `onclick` снова становится кодом.
- L1469 [rules] Оговорка живёт у числа, а не у события, из-за которого она нужна.
- L1495 [rules] Свободный номер выпуска сверяют с ветками, а не только с main.
- L1515 [rules] Ноль вместо числа и ноль как значение выглядят одинаково.
- L1523 [rules] Сумма под именем момента — не свод, а бред
- L1543 [rules] Кэш-свип: банк забирает долю поступлений очереди, передавшей долг
- L1571 [rules] Сохранённое значение сильнее базы, и правка базы под ним не видна.
- L1590 [rules] Выгрузка одна, и лишние сняты — решение владельца (30.08.2026).
- L1613 [rules] Выход, которого у этой конфигурации нет, предлагать нельзя.
- L1625 [rules] Проверок нет — спрашивают почему, а не ждут.
- L1636 [rules] Внутренний проект в своде статистики обезличен, и маскирует выдача, а не данные.
- L1658 [rules] Список отдаёт лоты, а смотрят извещения.
- L1679 [rules] Лот в списке обязан открываться — или сказать заранее, что не откроется.
- L1697 [rules] Плашка отвечает тому, кто открыл, — сообщение тому, кто не открывал.
- L1716 [rules] «Платон:» в подписи фильтра — обещание, которого фильтр не выполняет.
- L1725 [rules] Площадка в реализации инвестора уже нашла — войти в неё нельзя.
- L1739 [rules] Посчитанное за маршрутом — не показанное.
- L1757 [rules] Имена статей расходов страница держала своей копией — и статьи, заведённые позже, приезжали сырыми ключами.
- L1769 [rules] ГИС Торги — не тот рынок, и это надо было измерить до включения.
- L1805 [rules] Пропуск в ряду — не ноль, и на графике это видно.
- L1815 [rules] Продано столько-то — не показатель, пока рядом нет базы
- L1834 [rules] Вымывание отвечает на «почему не покупают» тем, что у нас есть.
- L1844 [rules] Цена от нуля не читается, но урезанная шкала обязана назваться.
- L1849 [rules] Источники одного проекта приходят порознь и живут на ядре.
- L1859 [rules] Управленцу нужна не ещё одна таблица, а вывод словами
- L1866 [rules] Причину отказа в CRM считать не по чему, и выдумывать её нельзя.
- L1881 [rules] Число берут целиком, а не с середины — во второй раз.
- L1890 [rules] Комментарий CRM — пересказ BitrixGPT, а не слова клиента
- L1895 [rules] Достаточность эскроу отвечает двумя числами, и путать их нельзя.
- L1909 [rules] Кто спрашивает из браузера — идёт через передачу работы опросу.
- L1918 [rules] Заявленное отсутствие проверяется так же, как заявленное наличие.
- L1936 [rules] Цена — всегда линия на своей шкале, а не вкладка со столбиками
- L1945 [rules] «Нет цены победителя» — не «не продалось».
- L1959 [rules] Охват торгов измеряется по реестру, а не обещается.
- L1969 [rules] Балл лота — соответствие девелоперскому профилю, и эталон измерен по сделкам
- L1989 [rules] Основная подборка — не склад неполных карточек
- L2005 [rules] Контракт SPA показывается без секрета сессии.
- L2013 [rules] Свод продаж начинался с конца — с подписанного договора.
- L2028 [rules] Раздел без вывода и раздел, которому вывод не из чего сложить, — разные вещи.
- L2035 [rules] Бюджет считают на всё сообщение, а не на его середину.
- L2049 [rules] Заголовок кодируют, чтобы раскодировать, а не чтобы он проехал.
- L2059 [rules] Потолок в страницах ограничивает объём, а не время.
- L2073 [rules] Ответ разбирают, зная, что он может быть не ответом.
- L2082 [rules] Склад кабинета общий, и это решение владельца
- L2089 [rules] Загрузка одна, потому что источник даты один.
- L2098 [rules] Проблемную очередь надо увидеть; решение за банк не выдумывают
- L2112 [rules] Дефолт называют, но модель на нём не обрывают
- L2137 [rules] Порядок строк таблицы — это рассказ, и неверный порядок читается как ошибка счёта.
- L2150 [rules] Свод по проекту не отвечает на вопрос «что куда ушло».
- L2165 [rules] Отдельно стоящий объект строится S-кривой, как жилой дом
- L2185 [rules] Одну статью нельзя делить по двум базам: движок по кассе, книга по ТЭП.
- L2210 [rules] Ряд в книгу заводят в пустую строку, а формула, читающая свой же итог, — круговая ссылка.
- L2228 [rules] Долг переезжает в момент раскрытия эскроу, и это следствие того, что НКЛ закрывается.
- L2251 [rules] Обнулённое обязательство исчезает бесследно, если не сказать, куда оно ушло.
- L2270 [rules] Документ собирают из той же вёрстки, а не по тем же данным.
- L2296 [rules] Вопрос задают о том, чего нельзя ни прочитать, ни посчитать.
- L2313 [rules] Единица — часть числа, и принятое без неё значение это другое число.
- L2325 [rules] Гарантийные удержания — скрытый резерв РСС, и лимит их не знает
- L2342 [rules] Дефицит бывает общий и структурный, и это разные числа.
- L2354 [rules] «Кто должен был работать» собирается из двух уже прочитанных источников.
- L2366 [rules] Умолчание ТЭП — это пример, а не пустой проект, и сброс его не восстанавливает.
- L2382 [rules] Отказ целиком там, где можно ответить наполовину, — это потеря, а не осторожность.
- L2396 [rules] Отказ считать по нормативу — не повод отказаться от пропорции.
- L2410 [rules] Заводской график — тоже сообщение, и оно про нас.
- L2419 [rules] Сокращение подрядчика — то же лицо.
- L2423 [rules] В реестрах РСС стоит генподрядчик, а на площадку выходят его субподрядчики.
- L2435 [rules] Имя подрядчика в строке сильнее заголовка над ней.
- L2447 [rules] Вход в контур открывается состоянием, а не инструментом.
- L2458 [rules] Число называют по его источнику, и границу источника называют тоже.
- L2488 [rules] Две суммы об одном остатке считают по одному контуру — или разница раскладывается, а не называется.
- L2507 [rules] Дефицит называется по вопросу, на который отвечает, а слово «структурный» на экране не живёт вовсе
- L2527 [rules] Строка таблицы складывается — или у колонок разные окна.
- L2535 [rules] Источник перераспределения — не всякий свободный лимит
- L2558 [rules] РСС — бюджет всей стройки, а не банковская доля
- L2575 [rules] Изоляция состояния покрывает всё дисковое, а не перечисленное по памяти.
- L2586 [rules] `CERTIFICATE_VERIFY_FAILED` — это вопрос «чего не хватает», а не диагноз.
- L2600 [rules] Проверка, объявленная дважды, работает один раз — и это молчание.
- L2609 [rules] Вывод, который печатается «по остаточному принципу», однажды скажет обратное посчитанному.
- L2622 [rules] Второй источник — это проверка первого, а не только вторая витрина.
- L2633 [rules] Готовая средняя источника — не выборка, и в полосу квартилей она не идёт.
- L2640 [rules] Пустое поле новой строки не встаёт на место прочитанного.
- L2656 [rules] Кто пишет файл целиком, тот теряет чужую запись.
- L2677 [rules] Мера сравнения — часть вопроса, и не та мера отвечает не на тот вопрос.
- L2694 [rules] Точка после сокращения фразу не кончает, а правило «в одном предложении» на этом ломается молча.
- L2712 [rules] Проверка, которая ищет строку в исходнике, не знает, разрешается ли имя.
- L2724 [rules] Балл, который не различает, — не балл; и «задача» обязана менять список.
- L2740 [rules] Три снижения за одно явление — это одно снижение, сложенное трижды; и шкала по процентилям каталога красит список в «Низкое» по построению.
- L2765 [rules] Ячейка вводной без формулы-читателя — не вводная; статья движка без строки в книге — потеря.
- L2784 [rules] Накопленный итог не складывают — складывают поток.
- L2800 [rules] Пустой слайд — это не «мало данных», а невыбранная вёрстка.
- L2818 [rules] Лечить пустоту потерей содержимого нельзя — лечит ширина.
- L2839 [rules] График экрана, у которого нет своей таблицы, в документ не попадает вовсе.
- L2854 [rules] Порядок таблицы и порядок оси — разные порядки.
- L2867 [rules] Молчит документ — это не «молчит источник».
- L2887 [rules] Проект решения — не решение, и метка называет документ, а не его отсутствие.
- L2901 [rules] Выбор, которого у источника нет, изображать нельзя.
- L2908 [rules] Пара осей без данных рисует пустое поле, а пустое поле читается как «про рынок сказать нечего».
- L2917 [rules] Одноимённая функция съедает предыдущую молча — и в коде тоже.
- L2924 [rules] Одна площадка в двух источниках портала записана по-разному, и точный ключ имени — не ключ.
- L2954 [rules] Реестр КРТ лежит файлом карты, а не за API.
- L2969 [rules] «Машине нечем» проверяют распознаванием, а не объявляют.
- L2981 [rules] `CERTIFICATE_VERIFY_FAILED` у mos.ru-хостов — это снова один лист.
- L2988 [rules] Проверки страницы гоняют собранную страницу, а не сырую константу.
- L2993 [rules] Статус источника отвечает на свой вопрос, а не на наш.
- L3023 [rules] Живой лот сильнее публикации: пока право на договор о КРТ на торгах, оператора у площадки нет.
- L3038 [rules] Передаваемая модель начинается с обнуления участка, а не с умолчаний.
- L3061 [rules] Территория проекта живёт не только во вводных — и подмена проекта обязана забыть её целиком.
- L3085 [rules] Фоновая работа приложения — не часть теста, и в прогоне её быть не должно.
- L3096 [rules] График — вводная, и читают его обе поверхности из одного места.
- L3113 [rules] Поле, которое молчит, чинят, а не сносят — оно и есть образец.
- L3135 [rules] Доля в юрлице — своя сделка, и мерить её метрами нельзя.
- L3165 [rules] Живая карта одна на сервис, и чужая страница берёт её, а не пишет свою.
- L3180 [rules] Норматив площади соцобъекта — ступень по ёмкости, и считается от числа мест
- L3194 [rules] График вводится ячейками, а не строкой через запятую
- L3207 [rules] Улица опознаёт квартал, а не площадку.
- L3227 [rules] Из чужого раздела читают сначала то, за чем пришли.
- L3235 [rules] Пятый элемент поля значит разное, и читать его надо по типу поля.
- L3247 [rules] Корзина находок, заведённая позже, в счётчик не попадает.
- L3264 [rules] Источники говорят языком продаж, а корзины были написаны языком канцелярии.
- L3298 [rules] Стенд страницы берёт блок по границам, а не перечисляет функции.
- L3306 [rules] Тест, читающий каталог из `.gitignore`, зелен только на своей машине.
- L3319 [rules] Правка, применённая наполовину, выглядит применённой.
- L3335 [rules] Незаписанный отказ нельзя посчитать, а непосчитанный отказ выглядит как отсутствие признака.
- L3351 [rules] Смена версии правила снимает признаки сразу у всех, и это надо сказать числом.
- L3361 [rules] У раздела не было обхода по страницам, и это оказалось настоящей причиной «одного КРТ».
- L3377 [rules] Оговорка «мы этого не читаем» устаревает молча.
- L3388 [rules] Одна жалоба — одна цепочка, и чинить надо то звено, где теряется.
- L3415 [rules] Машино-места считает постановление, и это одна формула на все поверхности
- L3442 [rules] Функция, читающая переменную страницы, обязана пережить её отсутствие.
- L3460 [rules] Необъявленная переменная страницы — не тихий undefined, а обрыв загрузки.
- L3480 [rules] У соцобъекта два числа: бюджет от МЕСТ, площадь для контроля объёма
- L3497 [rules] Средняя квартира — делитель с ОСНОВАНИЕМ, а не число
- L3514 [rules] Снижение балла меряют, а не назначают.
- L3525 [rules] Пол, написанный как `Math.max(points, N)`, не срабатывает там, где нужен.
- L3535 [rules] Подпись сравнения называет порог и своё число.
- L3545 [rules] Отказ подбора называет, чьё число не дотянуло, — иначе он спорит со сводом.
- L3569 [rules] Расшифровка пика БРИДЖа свода — по очередям с открытой линией, а не по всем.
- L3588 [rules] Линия, рассчитавшаяся в РВЭ, после него не кредитует — и это относится не только к переносу долга.
- L3607 [rules] Стартовая цена скрининга — та же, что рекомендует отчёт о рынке.
- L3620 [rules] Объект строится в своей очереди и не раньше её РнС — БРИДЖ стройку не финансирует
- L3634 [rules] БРИДЖ у очередей — свой у каждой, и блок рисуется по очередям.
- L3643 [rules] Проектирование, подготовка и сети идут по размеру очереди, ИРД — вперёд.
- L3651 [rules] Метры Программы реновации строятся, но не продаются — это цена входа
- L3677 [rules] Число на месте формулы шаблона рвёт цепочку молча, и снаружи оно неотличимо от посчитанного.
- L3695 [rules] Лимит очереди округляют по очереди, а не по сумме.
- L3703 [rules] Календарь объекта — это ячейки объекта, а не доля от общей даты.
- L3713 [rules] Ключ вводной живёт там же, где решается, куда писать значение.
- L3722 [rules] Счёт «чего в книге нет» считает то, о чём знает.
- L3730 [rules] Вводная, которую никто не читает, — не вводная, а обещание.
- L3744 [rules] Цвет — утверждение о ячейке, и врать он может так же, как подпись.
- L3757 [rules] ТЭП вбивается там, где его не найти, и это не придирка к вёрстке.
- L3772 [rules] Кнопка обновления обязана сходить в источник, а не перерисовать файл.
- L3791 [rules] Очередь платит общепроектные статьи по своему объёму, и цена метра очереди стоит рядом с вводной
- L3810 [rules] Число берут из оборота, который его называет, — а обрезка прячет непонятое.
- L3846 [rules] Отдельно стоящий объект меряется своей площадью, а наземный паркинг — местами
- L3877 [rules] В расчётный лимит БРИДЖа входит то, что платится ДО РнС
- L3893 [rules] Предел длины вопроса Платону — на сценарий, а не на весь маршрут.
- L3906 [rules] Обзорная карта рисовала только то, что город положил в свой файл.
- L3925 [rules] Сводная линия эскроу — сумма чужих счетов, и на вопрос про очередь она не отвечает.
- L3947 [rules] Один драйвер на два сценария — это второй сценарий, выданный за первый.
- L3973 [rules] Число квартир двигает машино-места — пунктом 2 по средней квартире.
- L3988 [rules] Цифры площадки лежат в документе, а не только в карточке.
- L4012 [rules] Вид величины объявляют там же, где её пишут.
- L4021 [rules] Пустой выбор фильтра неотличим от сломанного, пока рядом нет числа.
- L4028 [rules] Оси фильтра различают по вопросу, на который отвечают, а не по полю данных.
- L4048 [rules] Источник засчитывается в «спросили» только там, где он мог бы ответить.
- L4061 [rules] Спор двух источников решается только там, где обе стороны переживают перезагрузку.
- L4088 [rules] `computed_at` отвечает «когда», а не «чем».
- L4116 [rules] Порядок, изменившийся молча, читается как несработавшая сортировка.
- L4123 [rules] Съехавший разбор прячут целиком, а не только в ячейке.
- L4133 [rules] Прогон ходит по тому списку, который видит человек, а не по одной его половине
- L4166 [rules] Рассрочка ВРИ и резерв: методику снова меняли в одном месте из двух.
- L4189 [rules] Пятисотка отвечает текстом, а страница разбирает её как JSON.
- L4207 [rules] Отказ ресурса — не смерть страницы, а ловушка их не различала.
- L4223 [rules] Отдают метры и места, а не рубли
- L4244 [rules] Гостевые места приходят числом только из выгрузки, остальным их выводят обратным ходом.
- L4274 [rules] Витрина продаж называет застройщика СВОЕГО дома, и номер у дома и у владения один и тот же.
- L4307 [rules] Балл меряется тем объёмом, который у площадки ЕСТЬ, и мера называется.
- L4327 [rules] Объект уходит целиком одним путём, и признак у него один
- L4348 [rules] Проверка, не знающая о новой строке, кричит на верном расчёте.
- L4360 [rules] Имя функции у юрисдикций своё, и одно на обе таблицы найдёт не ту строку.
- L4371 [rules] «Не тронули» читается как «убрали».
- L4386 [rules] Надпись у поля — утверждение о ПОЛЕ, а не о методике.
- L4408 [rules] Подпись под графиком называет то, что на графике видно.
- L4426 [rules] Один рисовальщик на три контейнера — три копии на экране, и строковый тест их не видит.
- L4443 [rules] Лист, который собрали мы, обязан выглядеть книгой владельца.
- L4482 [rules] Правило списка применяют там, где список опознан.
- L4505 [rules] 404 от сервиса — «сервис не отдал», а не «объекта нет».
- L4536 [rules] Ось без единицы читается как другая шкала.
- L4544 [rules] Строка, вставленная между `if` и `else`, уводит себе чужой `else`.
- L4556 [rules] «Жив ли прогон» спрашивают у списка процессов, а не поиском по своей же строке.
- L4573 [rules] Точка входа модуля стоит в конце файла.
- L4579 [rules] Цены сделки в отчёте «Пульса» нет, и это измеряют, а не предполагают.
- L4598 [rules] Тип объекта решает, что попадает в цену и в комнатность.
- L4605 [rules] «Продано застройщиком» — не «продано».
- L4612 [rules] Имя поля бывает занято другим источником.
- L4618 [rules] Месяц выпуска в проверке числом не закрепляют.
- L4622 [rules] Прогон идёт по тому списку, который видит человек.
- L4632 [rules] У площадки-решения субъект есть — это её адрес, а обязательства лежат в её же PDF.
- L4656 [rules] Голая цифра адресом не является — а чужой контур подписан «официальный полигон».
- L4674 [rules] Число становится номером владения только в адресной позиции — и это же правило решает, откуда берётся балл.
- L4710 [rules] Колонка сортируется тем словом, которое в ней написано.
- L4721 [rules] Балл подписан тем, кто его считает, а не тем, чьё имя красивее.
- L4730 [rules] Одно и то же не говорят дважды подряд.
- L4743 [rules] Половина списка приезжает из другого источника, и карточка обязана называть свой.
- L4763 [rules] Догадку о причине проверяют на своём проекте, а не правилом.
- L4781 [rules] Раздел без картинки и без вывода — это две таблицы, а не раздел.
- L4791 [rules] Карточка была короткой, а колонка под ней — 118 500 пикселей.
- L4804 [rules] Порядок карточки — порядок решения, и у каждого шага есть имя на экране.
- L4815 [rules] «Проверка данных» говорит своё место сама.
- L4825 [rules] Длинный список сворачивается, короткий стоит открытым, и порог назван один раз.
- L4839 [rules] Неразобранная строка до модели не доходит, а «не из набора» сверяют без регистра.
- L4875 [rules] Модель считает и по объявленной цене торгов, а не только при нуле.
- L4895 [rules] Один документ в двух разделах портала — одна строка, и убранную называют числом.
- L4917 [rules] Строгое правило совпадения отказывало на ТОЖДЕСТВЕ.
- L4934 [rules] Вывод пишут словами вопроса, а не именами величин.
- L4954 [rules] Продаётся то, что можно поставить на кадастр
- L5001 [rules] Гараж объекта — величина проекта, и по очередям она делится или удваивается.
- L5027 [rules] Верхний край — это ответ, но ответ НАШ, и двигает он не только число.
- L5053 [rules] Своя правка, названная ошибкой источника, читается как находка в нём.
- L5072 [rules] Нормативное собрано вместе, кнопки после него, модель и Платон — в конце
- L5086 [rules] Пересчёт ТЭП по плотности книга умеет, и владеет им она — не трогать.
- L5107 [rules] Ревизия формул считает четыре болезни, а сторож был на одну.
- L5129 [rules] Легенда обещает ровно те полосы, которые нарисованы.
- L5145 [rules] Раздел, которого нет в общем выводе, отвечает только на свой вопрос.
- L5166 [rules] Доля за месяц и доля за год — разные величины, и окно называется у самой полосы.
- L5205 [rules] Ответ, за которым надо догадаться нажать, — ответ, которого нет.
- L5221 [rules] Кнопка стоит там, куда смотрят, а не там, где кончается блок.
- L5249 [rules] Двенадцать строк одним списком — это не таблица, а список; и одно имя на продукт бывает только тогда, когда оно объявлено один раз.
- L5279 [rules] Прежде чем рисовать величину, считают, есть ли в ней сигнал.
- L5297 [rules] Победитель, выбранный порядком множества, — два ответа об одном проекте.
- L5306 [rules] Итог книги считает её же формула, и текстовая ячейка в неё не входит.
- L5318 [rules] Выгрузку называют так, как отвечает источник, а не так, как её прислали.
- L5330 [rules] Землю под зданием спрашивают точкой, а не полем.
- L5341 [rules] Правообладатель земли и правообладатель строения — разные лица, и колонка у них разная.
- L5352 [rules] Слой, который крупнее, лежит ниже — иначе верхнего не существует.
- L5360 [rules] Служебная страница — та, на которую ниоткуда не ведёт ссылка, а числа за ней спрашивают владельца
- L5369 [rules] Кусок карты, вынутый из `PAGE`, тянет за собой не только карту.
- L5379 [rules] Фигура на карте опознаётся своим ключом, а не местом в списке.
- L5388 [rules] Площадку в реестре опознают геометрией, а не именем.
- L5398 [rules] Площадь участка и площадь, ВХОДЯЩАЯ в площадку, — разные величины, и справочная колонка обязана назвать свою.
- L5414 [rules] Номер, связывающий карту с таблицей, считает сервер.
- L5423 [rules] «Один экран» — это порядок и отбор, а не одно место.
- L5438 [rules] Стоимость земли и стоимость строений в одну колонку не складываются.
- L5453 [rules] Картинку без координат на карту не накладывают.
- L5467 [rules] Колонку книги читают по заголовку, а не по номеру — и проверки тоже.
- L5477 [rules] Подпись ставят в самое широкое место фигуры, а не в центр её рамки.
- L5491 [rules] `max-height` с `overflow:hidden` — это обрезка молча.
- L5499 [rules] Счётчик по одному источнику молчит о втором.
- L5507 [rules] Утверждение о втором источнике, поднятое к плиткам первого, читается как сомнение в первом.
- L5515 [rules] Итог по ЕГРН обязан стоять рядом с площадкой, которую он не равен.
- L5529 [rules] «Участка нет» и «участок есть, права не зарегистрированы» — разные состояния, и под похожими словами они сливаются.
- L5543 [rules] Бледнеть надо по величине, а не по признаку.
- L5552 [rules] Подпись вправе слегка выступать за мелкую фигуру.
- L5562 [rules] «Уверен» — плохой ответ там, где есть чем померить.
- L5571 [rules] Кадр строят по предмету, а не по всему нарисованному.
- L5580 [rules] Высоту кадру задаёт пропорция, а не картинка подложки.
- L5586 [rules] Ответ — числом в плитке, объяснение — один раз.
- L5597 [rules] Вырезая соседа, проверь, кого утащил.
- L5604 [rules] Ссылка на служебную страницу — тот же механизм, что «Поделиться» проектом
- L5617 [rules] Код доступа живёт своим файлом, а не в чужом.
- L5622 [rules] Ключей у сервиса два, и отказ обязан называть, какого не хватило.
- L5636 [rules] Пустой параметр в `bool` — это 422, а не «нет».
- L5649 [rules] Долю налога считают той мерой, из которой налог возник, — а мера эта год.
- L5681 [rules] Убыток переносится бессрочно, ограничение — в доле
- L5697 [rules] Состав пишет тот, кто видит его целиком, — иначе список сам себе делает новости.
- L5731 [rules] Величина, которую источник назвал ДРУГИМ полем, — это данные, а не их отсутствие.
- L5759 [rules] Кнопка, которая не может сработать, гаснет до нажатия, а отказ печатается там, где нажали.
- L5775 [rules] Красный main меряют полным прогоном по нему, а не тем файлом, который упал первым.
- L5797 [rules] Браузерные проверки на CI не идут вовсе, и «зелёный CI» о них не говорит ничего.
- L5820 [rules] Русскую дату разбирает сервер, браузер её читает не тем порядком.
- L5855 [rules] Лист трогают строкой, а не ячейкой, — и это правило уже было записано один раз.
- L5880 [rules] Сторож считает проходы, а подорожал сам проход.
- L5904 [rules] Ширина сетки объявлена один раз, а копии её живут внутри формул.
- L5922 [rules] ННП — площадь в габаритах ВНУТРЕННИХ стен, то есть общая, а не ГНС
- L5939 [rules] Сверка «ничего лишнего не изменилось» не отвечает на «всё ли нужное изменилось».
- L5951 [rules] Подсказка у поля и подпись под ним — одно место, а не два.
- L5967 [rules] Функцию, написанную объяснять молчание, надо ещё и позвать.
- L5990 [rules] Производную считают там, где её читают, — а хранят её в четырёх местах и по-разному.
- L6019 [rules] Денежная компенсация — не объект, и метров его в проекте нет
- L6029 [rules] Заглушка с придуманной сигнатурой проверяет не наш код, а себя.
- L6042 [rules] Гейт, которому передали не то, отвечает «нет» всем и всегда — и выглядит строгостью.
- L6059 [rules] У молчащего сторожа обязан быть счётчик молчания — второй раз, теперь у нормативов.
- L6076 [rules] Кнопка отвечает на тот вопрос, который в её подписи.
- L6087 [rules] Час без зоны — другой час, и в Москве это не видно.
- L6107 [rules] Расход признают в месяце, когда он возник, а не когда за него заплатили.
- L6134 [rules] Ширина сетки книги объявлялась трижды, и обрезка молчала.
- L6173 [rules] Экранирование чужого XML не раскодируют и не кодируют обратно.
- L6182 [rules] Ответ одного экрана собирают по одному чтению источника.
- L6225 [rules] Умолчание строки ТЭП — не литерал, а тот же счёт, что у всех.
- L6242 [rules] Делитель совпал с числителем случайно — и держался на чужом нуле.
- L6260 [rules] Строка, которую чинит только страница, чинится не везде.
- L6294 [rules] Площадь документа — наземная, и колонка у неё своя.
- L6314 [rules] Проверка, написанная под прежний ответ, закрепляет его.
- L6324 [rules] Стенд разрешает зависимости страницы сам, а не перечисляет их.
- L6363 [rules] Состояние стенда задают таким, каким его оставляет страница.
- L6377 [rules] Правило, закрытое на одной вкладке, соседнюю не защищает
- L6401 [rules] «Изменилось» без единого своего слова — это «скачали не тот документ».
- L6419 [rules] Маршрут, объявленный выше своей модели, читает тело как строку запроса.
- L6449 [rules] Отметка времени, напечатанная как есть, — не дата.
- L6486 [rules] Прогон делят, когда времени нет у отдельных тестов, — а делят по файлам и своими раннерами
- L6540 [rules] Деление прогона нашло тест, который зависел от соседа.
- L6567 [rules] Число без окна и без предмета сравнивают с чем попало.
- L6592 [rules] Модель не сходилась сама с собой, и нашла это приёмка чужой правки.
- L6614 [rules] Снимок «ни одно число не сдвинулось» слеп там, где числа нули.
- L6635 [rules] Книга пишет ТЭП, присланный страницей, а отчёт — тот, что движок посчитал.
- L6664 [rules] Пара «места и площадь гаража» читалась страницей и движком по-разному.
- L6687 [rules] Вынужденная копия живёт со сторожем, иначе отстаёт молча — и отстала.
- L6708 [rules] Проверка порядка скриптов мерила порядок объявлений — и держала сборку main красной двое суток.
- L6734 [rules] Ревизия чужой правки начинается с того, что мерят СВОЮ меру.
- L6744 [rules] Одно имя, два смысла — и обе поверхности выглядят верными.
- L6781 [rules] Приписка о переданном стоит у обеих мер, а не у той, до которой дошли.
- L6795 [rules] У максимума номеров бывает несколько держателей, и названный один прячет столкновение.
- L6806 [rules] Благоустраивают двор, а не дом — но два числа нормы с бюджетом не сошлись.
- L6853 [rules] Норматив — в профиль класса, и вместе с ним в таблицу приезжает единица.
- L6889 [rules] Доля стоит под тем числом, которое из неё получается, — и число сменило колонку.
- L6926 [rules] Облагается всё, кроме квартир, и список продуктов этого не решает.
- L6942 [rules] Производная величина не может быть точнее своих слагаемых.
- L6952 [rules] Строку таблицы книги читают по подписи, а не по номеру — и проверки тоже.
- L6958 [rules] Фикстура, подобранная под порог, устаревает вместе с методикой.
- L6965 [rules] Страницу проверяют по тому адресу, по которому её открывает человек, — а не файлом с диска.
- L6989 [rules] Буква у `-r` заменяет умолчание, а не добавляется к нему.
- L6998 [rules] Проценты по рассрочке ВРИ вычета НДС не дают, и строка паритета нашла это первым же прогоном.
- L7015 [rules] Путь к браузеру спрашивают у сервиса, а не помнят числом.
- L7023 [rules] Проверка «строка входит в вердикт» держала конец диапазона литералом.
- L7029 [rules] Диапазон, написанный краями, читается вычитанием — а имя периода нет.
- L7048 [rules] Хост, который не может сделать работу, не должен её начинать.
- L7069 [rules] Величина, за которой источник спрошен не о том, — не молчание источника.
- L7091 [rules] Апартаменты и квартиры лежат в одном классе, а покупатель у них разный.
- L7113 [rules] Витрину двигает не только прайс: у соседа есть рассрочка.
- L7169 [rules] Предложение по акции живёт на пересечении двух сводок, и до сих пор их было две разных.
- L7208 [rules] Этажа в источнике нет, а комнатность есть и не читалась.
- L7241 [rules] Штуки и метры складываются только внутри своего товара — рубли складываются всегда.
- L7274 [rules] «Формат не поддержан» бывает нашим пробелом, а не чужим форматом.
- L7289 [rules] DOCX — тоже зип, а подпись площадки врёт в обе стороны.
- L7298 [rules] У архива три списка, а не один: прочитанное, спутники и непрочитанное.
- L7308 [rules] Проверку на чужой флаг нельзя собрать чужим писателем.
- L7316 [rules] Число за всё время не отвечает на «как сейчас», а колонка без существительного — своя мера.
- L7337 [rules] Мера товара — часть товара, и нулевая площадь проданного лота это пропуск.
- L7361 [rules] Имя меры и единица при числе — разные формы.
- L7373 [rules] У величины есть мера — значит сравнение с ней стоит рядом, в обе стороны.
- L7397 [rules] Подпись у пары полей, стоящая ВЫШЕ ввода, приклеивается к соседней строке.
- L7410 [rules] Поток и остаток складываются по-разному, и очередь после своего горизонта не исчезает.
- L7431 [rules] Остановку прогона проверяют списком процессов, а не кодом возврата.
- L7440 [rules] Получателя переданных метров модель не знает — значит не называет.
- L7465 [rules] Инфляция объекта — от старта его очереди (решение владельца, 13.09.2026).
- L7482 [rules] Широкое содержимое держит СВОЁ окно прокрутки, а на бумаге его размыкают.
- L7508 [rules] Ссылку на источник проверяют содержимым, а не кодом ответа.
- L7521 [rules] Проект, ставший законом, — оговорка устаревает в опасную сторону.
- L7539 [rules] Отказ, записанный в общий список, до свода не доезжает — и свод отвечает «не спрашивали».
- L7552 [rules] Спутник ПРИ содержимом и спутник ВМЕСТО содержимого — разные ответы.
- L7563 [rules] Пустой ответ площадки и «документов нет» на экране одно и то же.
- L7570 [rules] Образец, выбрасывающий «служебное», выбросил и то, ради чего написан.
- L7590 [rules] Зарегистрированное право без имени — не отсутствие права.
- L7605 [rules] `async` стоит перед словом `function`, и стенд терял это слово.
- L7611 [rules] Один запрос на вложение выдаёт перебой площадки за отсутствие документа.
- L7635 [rules] Байты вложения разбирают несколько читателей — значит файл лежит, а не выбрасывается.
- L7667 [rules] У склада на диске есть строка в `.gitignore` — и спрашивают об этом git.
- L7678 [rules] Данные объявляют выше своих читателей, а не дозаписывают после.
- L7697 [rules] `const`, прочитанный выше своего объявления, роняет ВЕСЬ скрипт страницы — и молча.
- L7715 [rules] Стенд, несущий свою копию состава, — вторая жизнь у списка, объявленного один раз.
- L7732 [rules] Выбор значения, которое и так стоит умолчанием, ничего не различает.
- L7739 [rules] «Exit code 0» фоновой задачи — код последней команды цепочки, а не прогона.
- L7748 [rules] Проверка, не знающая о новой величине, кричит FAIL на верном расчёте — и сторож паритета этого не видит.
- L7767 [rules] Рукописная копия чужого листа отстаёт в тот же день, когда код учится новой формуле.
- L7777 [rules] Фикстура, у которой вводные и ТЭП говорят об объекте разное, — не проект.
- L7787 [rules] Клик по узлу, которым владеет другой слой, — гонка, и в одиночку её выигрывают.
- L7797 [rules] Читатель, режущий строку, делает сторожа слепым за срезом — и зрячим там, где ничего не сломано.
- L7814 [rules] У продукта, которого книга не знает, есть в ней ПУСТАЯ строка — и итог над ней его складывает.
- L7846 [rules] Проверка, зелёная по совпадению последнего разряда, не доказывает ничего.
- L7867 [rules] Фикстура, кладущая величину прямо в строку ТЭП, устаревает в тот день, когда строку начинают ВЫВОДИТЬ.
- L7883 [rules] «Падает и на main» — это замер, а не догадка, и мерить надо ВЕСЬ список.
- L7895 [rules] У записанного отказа спрашивают, не удался ли запасной путь.
- L7926 [rules] Свод территории собирает один сборщик, а площадки меняют ему источники.
- L7967 [rules] Свод из одного источника, поделённый на коэффициент, — не измерение того класса, на который его поделили.
- L7998 [rules] Проверка, стоящая на позиции, падает от дописанной рядом строки.
- L8017 [rules] Поле, сменившее место правки, объявлено там же, где было, — переезжает только отрисовка.
- L8042 [rules] У всего, что приходит без спроса, обязан быть выключатель — и он один на все поводы.
- L8069 [rules] Гейт стоит у той двери, в которую ходят, — а Telegram ходит в обёртку.
- L8102 [rules] Гейт читал не ту величину: карта отказывалась рисовать, имея 55 контуров.
- L8126 [rules] Счётчик считает то, что читают, — и это третья копия одной болезни.
- L8148 [rules] Работа, которую делает только рука, на экране выглядит ответом источника.
- L8182 [rules] Гейт по виду вложения, которого площадка не ставит, не срабатывает ни разу.
- L8200 [rules] Ячейки строки листа идут по возрастанию колонки — иначе Excel их теряет молча.
- L8230 [rules] Проверка, отобранная по имени строки, слепа ко всему остальному листу.
- L8250 [rules] Восстановить вводные из отчёта можно, но проверяется это прогоном, а не перепиской чисел.
- L8268 [rules] Перечисление объектов руками теряет объект, заведённый позже — второй раз за десять дней.
- L8295 [rules] Подземное в колонке «ГНС» — и три соседние потери в одном блоке.
- L8318 [rules] Самозакрывающаяся ячейка съедала следующую, и книга выглядела пустой.
- L8331 [rules] Детализация соцнагрузки в книге была числами, и это меряется, а не обсуждается.
- L8356 [rules] Цель паритета замерла на дате сборки, и правка вводной красит книгу зря.
- L8394 [rules] Форм у источника больше, чем читает разбор, — и молчание об этом наше.
- L8418 [rules] Печатная форма форме не равна.
- L8426 [rules] Повтор объявляют один раз, и площадка, отвечающая через раз, требует его у КАЖДОЙ двери.
- L8441 [rules] Ось фильтра называет вопрос, а не слово источника.
- L8461 [rules] Пустого раскрытия не бывает.
- L8474 [rules] Срок, взятый у шлюза, не годится сторожу.
- L8485 [rules] Разрешитель зависимостей стенда знает, чья это страница.
- L8496 [rules] Разобранное лежит на диске с версией правил, которыми прочитано, — иначе починка читателя не доезжает до прочитанного.
- L8527 [rules] Гейт стоит по действию документа, а не по его подписи.
- L8557 [rules] `a||b ? x : y` — это `(a||b) ? x : y`, и первая половина условия в ответ не попадает.
- L8571 [rules] Негодное тело запроса — это отказ с причиной, а не пятисотка.
- L8608 [rules] Правило, закрытое у одного поля, соседнее не защищает — теперь у номера распоряжения.
- L8627 [rules] Разрешитель стенда слеп к ошибке, которую чужой `try/catch` проглотил.
- L8655 [rules] Сторож запирал свой же гейт: обход просился строкой ВЫШЕ проверки полноты.
- L8680 [rules] Различающий ответ, до поверхности не доехавший, — это ответ, которого нет.
- L8707 [rules] Номер берут целиком, а границу ему задаёт форма, а не длина.
- L8739 [rules] Счётчик устаревшего разбора считал записи, а у склада без записей отставать нечему.
- L8757 [rules] Немой отказ чинить нечем: он обязан назвать, что видел.
- L8767 [rules] Показатель, который предлагают вписать, считается на базе того поля, куда его вписывают.
- L8782 [rules] «Имени нет» у node две формы, и стенд узнавал одну.
- L8791 [rules] Явный признак сильнее совпадения с числом.
- L8803 [rules] Свод рядом со ставкой обязан назвать СВОЮ базу, а подсказка — не ответ.
- L8818 [rules] Предохранитель, меряющий одну координату, падает на законной сетке.
- L8839 [rules] Допуск проверки не бывает уже ступеньки самой величины.
- L8858 [rules] Подпись, идущая за выбранным видом, отвечает не о том поле, под которым стоит — а если она ещё и пишет, то пишет чужое число.
- L8887 [rules] Стенд, гоняющий node сам, не знает о функции, заведённой вчера.
- L8897 [rules] Форма, которую забирают целиком, превращает показанное в введённое.
- L8925 [rules] Ожидание в браузерной проверке не смотрит на то, что проверяется.
- L8934 [rules] Обрезка по длине стоит между диагнозом и читателем.
- L8958 [rules] Счёт «чего в книге нет» мерит то, чем меришь, — и трижды соврал в разные стороны.
- L8986 [rules] Сколько у источника есть — спрашивают у источника, а недособранный обход дополняет, а не заменяет.
- L9024 [rules] Свой предел — тоже правило читателя, и меняя его, поднимают версию.
- L9043 [rules] Мера полноты сравнивает однородное, а молча выброшенное делает объявленное число недостижимым.
- L9077 [rules] Зависание, которого нет в песочнице, мерят там, где оно есть — а для этого молчание обязано заговорить.
- L9101 [rules] Раннер ждёт закрытия ТРУБЫ шага, а не завершения дерева процессов.
- L9124 [rules] Границу числу задаёт форма, а не длина клетки — и склеенное рядом не делает число непрочитанным.
- L9157 [rules] Проверка подстрокой не отличает утверждение от его отрицания.
- L9169 [rules] Правовую посылку проверяют источником, а не памятью.
- L9188 [rules] Режим — это вводные, а не вторая методика счёта.
- L9217 [rules] Две поверхности, ошибающиеся одинаково, паритетом не ловятся — и это был не недосмотр, а копия.
- L9244 [rules] Величина, живущая в `finance`, на поверхности не существует.
- L9263 [rules] Одобренный лимит был потолком в движке и справкой в книге.
- L9283 [rules] Поле, которое просят заполнить, обязано лежать на листе ввода, и решает это СТИЛЬ.
- L9295 [rules] Формула, идущая в `_v4_set_cells`, — сырая: экранирует она сама.
- L9303 [rules] `git checkout -- файл` откатывает файл ЦЕЛИКОМ, включая несохранённую работу.
- L9309 [rules] Общий лимит ПФ у очередей — ожидание, потолок очереди — только её НКЛ
- L9347 [rules] Поле, которое пишут рядом со строкой и не читает никто, — это молчание, а не запись.
- L9395 [rules] Показатель, который обещают вернуть, считается независимо от того, что стоит в поле.
- L9426 [rules] Статья, посчитанная нулём, исчезает из расходов целиком — и её отсутствие читается как ответ методики.
- L9451 [rules] Дешёвый гейт перед дорогой дверью — копия условия по построению, и она обязана быть вызовом.
- L9469 [rules] Пустая сторона пары с непустой не спорит — и потому признак читают отовсюду, а не оттуда, где его писали в первый раз.
- L9498 [rules] Дашборд книги и тизер — одна модель представления, и она не считает
- L9521 [rules] Образец владельца задаёт СОСТАВ, а ТЗ по нему — только рамку.
- L9549 [rules] У источника бывает две поверхности, и вторую надо СПРОСИТЬ, а не считать копией первой.
- L9580 [rules] Сторож с литералом падает на том, ради чего написан, и молчит о самой величине.
- L9600 [rules] Кнопка и подпись под ней отвечают на один вопрос — значит считает их одно правило.
- L9613 [rules] «Последний прогон на ветке» — не тот прогон, которого ждёшь.
- L9623 [rules] Поле, заведённое без подъёма версии правил, доезжает только до новых строк.
- L9643 [rules] Номер выпуска не опознаёт образ: сторож версии на слиянии не перечитывают.
- L9663 [rules] Замок, который отпускает `finally`, переживает смерть процесса — и полчаса выдаёт труп за живого.
- L9692 [rules] Вердикт проверки, прочитанный вскоре после её старта, — не вердикт, а её незаполненное состояние.
- L9700 [rules] «Доехал ли PR в main» сверяют по содержимому, а не по номеру в сообщении коммита.
- L9708 [rules] Диверсию снимают копией файла, а не `git checkout` — и я это проверил на себе.
- L9718 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9760 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9761 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9763 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9767 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9809 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9810 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9812 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9816 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9858 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9859 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9861 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9863 [checks] Две величины на одной шкале — это одна из них, сплющенная.
- L9875 [checks] Легенда — часть графика, и объявляется там же, где он один.
- L9884 [checks] Своё окно прокрутки помнит, докуда его дочитали.
- L9900 [checks] Шапка отвечает на тот же вопрос, что метки строки, — и тогда она обязана назвать их цену.
- L9914 [checks] Паспорт площадки — её собственные числа, и он опознаёт публикацию не хуже адреса
- L9935 [checks] Улица опознаёт квартал — и «Строгино 360» это подтвердило, а не опровергло.
- L9958 [checks] Гейт читает поле, а вторая сборка строки его не кладёт — и отказ винит источник.
- L9984 [checks] Связку считают по тому списку, который видит человек.
- L10004 [checks] У молчащего сторожа обязан быть счётчик молчания — и у сторожа новостей его не было.
- L10033 [checks] Итог прогона читают в ЕГО файле, а не в том, что подвернулось.
- L10048 [checks] «Работа принята» и «пустого ответа» с виду не отличить, и поверхность, которая ждать не умеет, показывает принятое как отказ.
- L10064 [checks] Одно имя — один автор: «Короткий вывод Платона» считал не Платон.
- L10077 [checks] Порядок карточки КРТ: сначала выводы, потом данные города, потом детали
- L10092 [checks] Два отрисовщика на одни данные — две разные карточки об одной площадке.
- L10100 [checks] `100vh` на телефоне — высота вместе с полосой браузера, а не то, что видно.
- L10121 [checks] Репозиторий публичный — решение владельца (07.09.2026), и потому чужое в него не кладут.
- L10136 [checks] Срок строительства при очередности задаёт очередь, и поле проекта обязано это сказать
- L10169 [checks] Погасшая кнопка без слов читается как несработавшее нажатие.
- L10191 [checks] Пример поломки, взятый у умолчания, умирает вместе с его починкой.
- L10198 [checks] Встречную правку одного места находят по одному и тому же падению.
- L10215 [checks] Правка строки заполняет свою строку, а доля целого связывает соседние.
- L10248 [checks] Число из документа и формула документа — разные вещи, и заморозка выхода ломает вход.
- L10272 [checks] Правило, закрытое у одного читателя, соседнего не защищает — и «живой лот» это доказал во второй раз.
- L10286 [checks] Маркер конфликта — рабочий код до тех пор, пока его никто не ищет.
- L10300 [checks] Первое вхождение строки — не то место, о котором думает проверка, и на этом лёг main.
- L10328 [checks] Свод превращает снятые доли в «failure», и прежний признак на нём не работает.
- L10343 [checks] Смягчение, которое даёт город, — справка у числа, а не множитель в расчёте
- L10363 [checks] Поле, заполненное нормой, обязано нести её пометку — иначе оно замирает и выглядит решением человека.
- L10398 [checks] Глобальное поле, сильное сильнее методики, умолчанием не ставят — его ставит РЕЖИМ.
- L10425 [checks] Колонка «на метр» была подписана базой, которой не делится, — и проверка её держала.
