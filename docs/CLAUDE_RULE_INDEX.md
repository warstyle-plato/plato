# Индекс исторических правил CLAUDE

Собирается из архива: `python3 scripts/claude_rule_index.py --write`.
Руками не править — правка потеряется при следующей сборке.
Полный снимок прежнего файла: `docs/CLAUDE_HISTORY_2026-09-20.md`.
Ищите здесь по имени файла, функции, маршруту, предмету или ключевому слову; затем открывайте только нужный фрагмент архива.

- L8 [setup] `main_legacy.py` — движок (~17 000 строк): расчёт, страница (`PAGE`), Telegram-бот,
- L10 [setup] `main.py` — обёртка: грузит движок как `developaid_core`, монкей-патчит поведение
- L12 [setup] Бот и движок — **один процесс**, не разные сервисы.
- L13 [setup] Разворачивается двумя воркерами (`--workers 2`). Память у них раздельная:
- L19 [rules] Токен Telegram-сессии не опознаёт проект.
- L21 [rules] Формулы шаблона ПЛАТО не перезаписываются
- L24 [rules] Поле, которого нет в карте записи, молча остаётся мусором из шаблона.
- L27 [rules] `JSON.stringify` превращает пропуск в массиве в `null`.
- L29 [rules] Отсутствующий ключ — не «снято».
- L34 [rules] Сохранённое состояние браузера накладывается на умолчания, не заменяет их.
- L36 [rules] Тяжёлое не считается внутри запроса, от которого зависит окно.
- L38 [rules] Ошибка, ушедшая только в лог, — это ошибка, которой нет.
- L40 [rules] Поверхности считают один раз, а не каждая своё.
- L47 [rules] Методику меняют в двух местах: в движке и в книге.
- L55 [rules] Формула книги живёт в горизонте движка и на его базах.
- L69 [rules] Импорт ГлавАПУ включает то, для чего принёс данные.
- L74 [rules] Рассрочка ВРИ начинается в дату обязательства, а не периодом позже.
- L80 [rules] Снятый признак банковского бюджета ВРИ — тоже потеря, а не решение.
- L86 [rules] Заданная плата за ВРИ платится, даже если признак снят.
- L91 [rules] График платежей ВРИ доезжает в книгу отдельно от суммы.
- L98 [rules] Версия объявляется один раз — `main_legacy.VERSION`.
- L115 [rules] Маршрут Платона решает `PLATO_AI_URL`, а не наличие ключа.
- L122 [rules] «Зона есть» — не ответ, ответ — сколько участка она съела.
- L132 [rules] Пустой результат проверки — не «чисто».
- L139 [rules] Счёт виден, вывод — за входом (решение владельца, 18.08.2026).
- L147 [rules] Личность подтверждает телеграм, анкета — нет.
- L154 [rules] Ожидание без признака работы читается как внезапность.
- L161 [rules] Эмблема одна на все поверхности, и она лежит в `PAGE`.
- L167 [rules] Документ ИП живёт не только на главной.
- L173 [rules] Зашитая цифра на экране неотличима от посчитанной.
- L183 [rules] Импорт участка обнуляет то, что участку принадлежит.
- L192 [rules] Зелёный набор тестов не значит, что ветка кода верна — возможно, до неё просто не доходят.
- L197 [rules] Капитализация процентов лимит ПФ не выбирает и не уменьшает
- L203 [rules] Методика финансирования сверена с НКЛ Сбера
- L215 [rules] В компенсацию за соцобъекты входит земля, а земля у каждого квартала своя.
- L223 [rules] Нормативный пакет лежит файлом, а числа из него — версионируемым рядом.
- L237 [rules] Места постоянного размещения — приложение 5 к 945-ПП в редакции 2118-ПП
- L250 [rules] Коэффициенты парковки берём у ГлавАПУ, своего справочника районов нет
- L260 [rules] Территорию город уже посчитал — пересчитывают количество, а не методику
- L277 [rules] Калькулятор считает по нормативному ТЭП, а строят по утверждённому.
- L293 [rules] Коэффициент выбирают по признаку, а не по удобству.
- L303 [rules] Формула платы за ВРИ сверена с исходником калькулятора ГлавАПУ
- L317 [rules] Методику города сверять по выгрузкам, а не подгонять по итогу.
- L332 [rules] Специальная ставка в договорах ступенчатая — сделано (20.08.2026).
- L346 [rules] Доля из документа и доля из методики — разные числа, и подписывать их одинаково нельзя.
- L358 [rules] Фактический ТЭП вводится руками, второй колонки рядом с нормативом нет
- L366 [rules] Переданные муниципалитету метры строятся, но не продаются.
- L379 [rules] Профиль расходов, объявленный дважды, расходится там, где считают ступень.
- L394 [rules] Гостевые машино-места строятся, но не продаются, и обе книги это забывали.
- L406 [rules] Кладовые прибавляют подземную площадь, а не вычитаются из гаража
- L464 [rules] Удельный показатель без второй базы читается как другой показатель.
- L470 [rules] Правило, применённое к боту, надо применить и к сайту.
- L476 [rules] Порядок разделов PDF задаётся при сборке, а не порядком вычислений.
- L480 [rules] Признак телеграма — параметры запуска, а не SDK.
- L486 [rules] Чужой DOM не трогать: React считает свои узлы своими.
- L492 [rules] Селектор на чужой странице живёт парой с диагностикой.
- L499 [rules] Запасной ответ не живёт в кэше наравне со штатным.
- L503 [rules] Длинный ответ забирается вторым запросом, а не одним соединением.
- L509 [rules] Работу не держат соединением — её принимают.
- L522 [rules] Список полей и умолчания страница берёт из движка, а не хранит свои.
- L532 [rules] Модуль не заводит своего пути туда, где у движка уже есть общий, — берёт крючком.
- L547 [rules] До Render с ядра не дойти — направление развёрнуто (06.08.2026).
- L557 [rules] Собственные средства до ПФ — бесплатные, это решение владельца (06.08.2026).
- L566 [rules] Пустая выгрузка — не «людей не было», а «журнал не пережил выкатку».
- L577 [rules] Документ обещает то, что делает код, — иначе он неверен, а не неточен.
- L589 [rules] Оценка без автора — не обратная связь, а цифра.
- L599 [rules] Одна оценка — не анкета, а две записи на неё — двойной счёт.
- L610 [rules] Учёт обращений стоит там, где они сходятся, а не по веткам разбора.
- L618 [rules] Слой перестройки `/ia` живёт на чужой разметке — значит, на диагностике.
- L626 [rules] Данные страницы объявлены `let`, и свойствами `window` они не становятся.
- L632 [rules] Страница пересчитывает модель сама, а шапка обещает обратное.
- L638 [rules] В книгу уходит посчитанная льгота, а не поле «льгота — сумма».
- L647 [rules] Присланный проект — снимок, а не общий доступ.
- L658 [rules] Личность пользователя одна — chat_id из бота, и сайт её не дублирует.
- L670 [rules] Поисковый сниппет содержит слова запроса — значит и адрес объекта оценки.
- L681 [rules] Точность геокодера — часть ответа, а не подробность.
- L687 [rules] Тип страницы решает, что с неё можно взять.
- L694 [rules] Терминаторы заголовка пишут ASCII-дефисом.
- L697 [rules] Ключ сущности считают от очищенного имени, а не от сырого заголовка.
- L705 [rules] Диапазон правдоподобия не заменяет разбор числа.
- L710 [rules] Глиф без буквы выбрасывать нельзя — его надо показать.
- L720 [rules] Район — жёсткая отсечка сопоставимости, решение владельца (11.08.2026).
- L727 [rules] Строку итога прогона читать до слияния, отдельной командой.
- L731 [rules] Проверка цепляется за контракт, а не за соседнюю строку.
- L745 [rules] Два прогона в один файл — ни одного прогона.
- L754 [rules] Метка перечисления и метка прозы — разные вещи, и тест на своей форме ошибку в чужой не ловит.
- L764 [rules] Класс в конструкторе отчётов ставит «Пульс», своего суждения нет
- L773 [rules] Занятый порт называет владельца, а не приглашает расчистить.
- L785 [rules] Класс считаем так, как считает источник — включая дробные метки
- L799 [rules] Соседний класс расширяет выборку тем сильнее, чем ниже уровень.
- L806 [rules] Журнал пишется там, где обслужен запрос, а читают его в другом месте.
- L814 [rules] Выкатка не уводит прод на выпуск назад молча.
- L836 [rules] Выкатка убирает за собой, иначе диск кончается молча.
- L858 [rules] Размещение соцобъектов объявляют объектами, а не метрами в фазе.
- L874 [rules] Очередь строит то, что написано рядом с её именем, — или имя врёт.
- L896 [rules] Чей это термин — часть самого термина
- L911 [rules] Подземная часть в ГНС не входит — закрыто 04.09.2026.
- L941 [rules] Что задал город и что придумали мы — разные разделы отчёта
- L952 [rules] В отчёте для инвестора нет истории наших правок.
- L964 [rules] Скрининг каталога ставит соцобъекты первым очередям — иначе балл снимает за нашу же раскладку.
- L982 [rules] Балл — из одного источника чисел, а не из той копии, что успела загрузиться.
- L995 [rules] Балл один, и расчёт его снижает, а не заменяет собой
- L1011 [rules] Балл у лота тем же правилом, что у КРТ.
- L1021 [rules] Платон стоит там, где смотрят, а не на соседней странице.
- L1030 [rules] Новое в каталоге называется вслух.
- L1039 [rules] Посчитанное не выбрасывают.
- L1054 [rules] Вводные едут вместе с результатом, иначе их соберут второй раз.
- L1063 [rules] Присланный проект накладывается на умолчания одним способом.
- L1071 [rules] Ограничение при вводе не спасает от правки задним числом.
- L1083 [rules] Проверка равенством целиком запрещает добавлять, а не утверждает.
- L1095 [rules] Сверка живёт рядом с базой, а не рядом с суммой.
- L1110 [rules] Что входит в МКД, объявлено один раз
- L1118 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1129 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1140 [rules] Рамка запроса шире участка — значит соседнее не вердикт.
- L1156 [rules] Скрининг спрашивал точку, а не участок — теперь контур.
- L1173 [rules] Слитый PR закончен: дописывать в его ветку нельзя.
- L1192 [rules] Поверхность, добавленная позже, в проверку списком не попадает.
- L1206 [rules] Точка карты и точка отчёта — одна и та же точка, иначе смотрят на одну, а считают по другой.
- L1223 [rules] Скрининг КРТ читал доли ТЭП мимо разбора движка.
- L1234 [rules] Незакрытая кавычка в `PAGE` убивает продукт целиком и молча.
- L1252 [rules] Российский госсайт не проверить обычным хранилищем корней, и это не «сеть закрыта».
- L1294 [rules] Очереди — один налогоплательщик, и книга считает так же
- L1328 [rules] Умолчание, которое утверждает, — не умолчание.
- L1343 [rules] Умолчание, которое можно переписать, — уже не умолчание.
- L1374 [rules] Параметр, ухудшающий выдачу, — не безобидный.
- L1385 [rules] Отсутствие фильтра — тоже ответ, и его надо измерить.
- L1397 [rules] «Не проверили» и «не заполнено» — разные ответы, и на экране тоже.
- L1411 [rules] Карта, которую крутят, считает тот же меркатор, что карта, которую печатают.
- L1436 [rules] Заполненный контур перехватывает указатель на всей своей площади.
- L1445 [rules] `textContent` правит строку целиком и сносит вложенные узлы.
- L1453 [rules] Экранированное значение внутри `onclick` снова становится кодом.
- L1460 [rules] Оговорка живёт у числа, а не у события, из-за которого она нужна.
- L1486 [rules] Свободный номер выпуска сверяют с ветками, а не только с main.
- L1506 [rules] Ноль вместо числа и ноль как значение выглядят одинаково.
- L1514 [rules] Сумма под именем момента — не свод, а бред
- L1534 [rules] Кэш-свип: банк забирает долю поступлений очереди, передавшей долг
- L1562 [rules] Сохранённое значение сильнее базы, и правка базы под ним не видна.
- L1581 [rules] Выгрузка одна, и лишние сняты — решение владельца (30.08.2026).
- L1604 [rules] Выход, которого у этой конфигурации нет, предлагать нельзя.
- L1616 [rules] Проверок нет — спрашивают почему, а не ждут.
- L1627 [rules] Внутренний проект в своде статистики обезличен, и маскирует выдача, а не данные.
- L1649 [rules] Список отдаёт лоты, а смотрят извещения.
- L1670 [rules] Лот в списке обязан открываться — или сказать заранее, что не откроется.
- L1688 [rules] Плашка отвечает тому, кто открыл, — сообщение тому, кто не открывал.
- L1707 [rules] «Платон:» в подписи фильтра — обещание, которого фильтр не выполняет.
- L1716 [rules] Площадка в реализации инвестора уже нашла — войти в неё нельзя.
- L1730 [rules] Посчитанное за маршрутом — не показанное.
- L1748 [rules] Имена статей расходов страница держала своей копией — и статьи, заведённые позже, приезжали сырыми ключами.
- L1760 [rules] ГИС Торги — не тот рынок, и это надо было измерить до включения.
- L1796 [rules] Пропуск в ряду — не ноль, и на графике это видно.
- L1806 [rules] Продано столько-то — не показатель, пока рядом нет базы
- L1825 [rules] Вымывание отвечает на «почему не покупают» тем, что у нас есть.
- L1835 [rules] Цена от нуля не читается, но урезанная шкала обязана назваться.
- L1840 [rules] Источники одного проекта приходят порознь и живут на ядре.
- L1850 [rules] Управленцу нужна не ещё одна таблица, а вывод словами
- L1857 [rules] Причину отказа в CRM считать не по чему, и выдумывать её нельзя.
- L1872 [rules] Число берут целиком, а не с середины — во второй раз.
- L1881 [rules] Комментарий CRM — пересказ BitrixGPT, а не слова клиента
- L1886 [rules] Достаточность эскроу отвечает двумя числами, и путать их нельзя.
- L1900 [rules] Кто спрашивает из браузера — идёт через передачу работы опросу.
- L1909 [rules] Заявленное отсутствие проверяется так же, как заявленное наличие.
- L1927 [rules] Цена — всегда линия на своей шкале, а не вкладка со столбиками
- L1936 [rules] «Нет цены победителя» — не «не продалось».
- L1950 [rules] Охват торгов измеряется по реестру, а не обещается.
- L1960 [rules] Балл лота — соответствие девелоперскому профилю, и эталон измерен по сделкам
- L1980 [rules] Основная подборка — не склад неполных карточек
- L1996 [rules] Контракт SPA показывается без секрета сессии.
- L2004 [rules] Свод продаж начинался с конца — с подписанного договора.
- L2019 [rules] Раздел без вывода и раздел, которому вывод не из чего сложить, — разные вещи.
- L2026 [rules] Бюджет считают на всё сообщение, а не на его середину.
- L2040 [rules] Заголовок кодируют, чтобы раскодировать, а не чтобы он проехал.
- L2050 [rules] Потолок в страницах ограничивает объём, а не время.
- L2064 [rules] Ответ разбирают, зная, что он может быть не ответом.
- L2073 [rules] Склад кабинета общий, и это решение владельца
- L2080 [rules] Загрузка одна, потому что источник даты один.
- L2089 [rules] Проблемную очередь надо увидеть; решение за банк не выдумывают
- L2103 [rules] Дефолт называют, но модель на нём не обрывают
- L2128 [rules] Порядок строк таблицы — это рассказ, и неверный порядок читается как ошибка счёта.
- L2141 [rules] Свод по проекту не отвечает на вопрос «что куда ушло».
- L2156 [rules] Отдельно стоящий объект строится S-кривой, как жилой дом
- L2176 [rules] Одну статью нельзя делить по двум базам: движок по кассе, книга по ТЭП.
- L2201 [rules] Ряд в книгу заводят в пустую строку, а формула, читающая свой же итог, — круговая ссылка.
- L2219 [rules] Долг переезжает в момент раскрытия эскроу, и это следствие того, что НКЛ закрывается.
- L2242 [rules] Обнулённое обязательство исчезает бесследно, если не сказать, куда оно ушло.
- L2261 [rules] Документ собирают из той же вёрстки, а не по тем же данным.
- L2287 [rules] Вопрос задают о том, чего нельзя ни прочитать, ни посчитать.
- L2304 [rules] Единица — часть числа, и принятое без неё значение это другое число.
- L2316 [rules] Гарантийные удержания — скрытый резерв РСС, и лимит их не знает
- L2333 [rules] Дефицит бывает общий и структурный, и это разные числа.
- L2345 [rules] «Кто должен был работать» собирается из двух уже прочитанных источников.
- L2357 [rules] Умолчание ТЭП — это пример, а не пустой проект, и сброс его не восстанавливает.
- L2373 [rules] Отказ целиком там, где можно ответить наполовину, — это потеря, а не осторожность.
- L2387 [rules] Отказ считать по нормативу — не повод отказаться от пропорции.
- L2401 [rules] Заводской график — тоже сообщение, и оно про нас.
- L2410 [rules] Сокращение подрядчика — то же лицо.
- L2414 [rules] В реестрах РСС стоит генподрядчик, а на площадку выходят его субподрядчики.
- L2426 [rules] Имя подрядчика в строке сильнее заголовка над ней.
- L2438 [rules] Вход в контур открывается состоянием, а не инструментом.
- L2449 [rules] Число называют по его источнику, и границу источника называют тоже.
- L2479 [rules] Две суммы об одном остатке считают по одному контуру — или разница раскладывается, а не называется.
- L2498 [rules] Дефицит называется по вопросу, на который отвечает, а слово «структурный» на экране не живёт вовсе
- L2518 [rules] Строка таблицы складывается — или у колонок разные окна.
- L2526 [rules] Источник перераспределения — не всякий свободный лимит
- L2549 [rules] РСС — бюджет всей стройки, а не банковская доля
- L2566 [rules] Изоляция состояния покрывает всё дисковое, а не перечисленное по памяти.
- L2577 [rules] `CERTIFICATE_VERIFY_FAILED` — это вопрос «чего не хватает», а не диагноз.
- L2591 [rules] Проверка, объявленная дважды, работает один раз — и это молчание.
- L2600 [rules] Вывод, который печатается «по остаточному принципу», однажды скажет обратное посчитанному.
- L2613 [rules] Второй источник — это проверка первого, а не только вторая витрина.
- L2624 [rules] Готовая средняя источника — не выборка, и в полосу квартилей она не идёт.
- L2631 [rules] Пустое поле новой строки не встаёт на место прочитанного.
- L2647 [rules] Кто пишет файл целиком, тот теряет чужую запись.
- L2668 [rules] Мера сравнения — часть вопроса, и не та мера отвечает не на тот вопрос.
- L2685 [rules] Точка после сокращения фразу не кончает, а правило «в одном предложении» на этом ломается молча.
- L2703 [rules] Проверка, которая ищет строку в исходнике, не знает, разрешается ли имя.
- L2715 [rules] Балл, который не различает, — не балл; и «задача» обязана менять список.
- L2731 [rules] Три снижения за одно явление — это одно снижение, сложенное трижды; и шкала по процентилям каталога красит список в «Низкое» по построению.
- L2756 [rules] Ячейка вводной без формулы-читателя — не вводная; статья движка без строки в книге — потеря.
- L2775 [rules] Накопленный итог не складывают — складывают поток.
- L2791 [rules] Пустой слайд — это не «мало данных», а невыбранная вёрстка.
- L2809 [rules] Лечить пустоту потерей содержимого нельзя — лечит ширина.
- L2830 [rules] График экрана, у которого нет своей таблицы, в документ не попадает вовсе.
- L2845 [rules] Порядок таблицы и порядок оси — разные порядки.
- L2858 [rules] Молчит документ — это не «молчит источник».
- L2878 [rules] Проект решения — не решение, и метка называет документ, а не его отсутствие.
- L2892 [rules] Выбор, которого у источника нет, изображать нельзя.
- L2899 [rules] Пара осей без данных рисует пустое поле, а пустое поле читается как «про рынок сказать нечего».
- L2908 [rules] Одноимённая функция съедает предыдущую молча — и в коде тоже.
- L2915 [rules] Одна площадка в двух источниках портала записана по-разному, и точный ключ имени — не ключ.
- L2945 [rules] Реестр КРТ лежит файлом карты, а не за API.
- L2960 [rules] «Машине нечем» проверяют распознаванием, а не объявляют.
- L2972 [rules] `CERTIFICATE_VERIFY_FAILED` у mos.ru-хостов — это снова один лист.
- L2979 [rules] Проверки страницы гоняют собранную страницу, а не сырую константу.
- L2984 [rules] Статус источника отвечает на свой вопрос, а не на наш.
- L3014 [rules] Живой лот сильнее публикации: пока право на договор о КРТ на торгах, оператора у площадки нет.
- L3029 [rules] Передаваемая модель начинается с обнуления участка, а не с умолчаний.
- L3052 [rules] Территория проекта живёт не только во вводных — и подмена проекта обязана забыть её целиком.
- L3076 [rules] Фоновая работа приложения — не часть теста, и в прогоне её быть не должно.
- L3087 [rules] График — вводная, и читают его обе поверхности из одного места.
- L3104 [rules] Поле, которое молчит, чинят, а не сносят — оно и есть образец.
- L3126 [rules] Доля в юрлице — своя сделка, и мерить её метрами нельзя.
- L3156 [rules] Живая карта одна на сервис, и чужая страница берёт её, а не пишет свою.
- L3171 [rules] Норматив площади соцобъекта — ступень по ёмкости, и считается от числа мест
- L3185 [rules] График вводится ячейками, а не строкой через запятую
- L3198 [rules] Улица опознаёт квартал, а не площадку.
- L3218 [rules] Из чужого раздела читают сначала то, за чем пришли.
- L3226 [rules] Пятый элемент поля значит разное, и читать его надо по типу поля.
- L3238 [rules] Корзина находок, заведённая позже, в счётчик не попадает.
- L3255 [rules] Источники говорят языком продаж, а корзины были написаны языком канцелярии.
- L3289 [rules] Стенд страницы берёт блок по границам, а не перечисляет функции.
- L3297 [rules] Тест, читающий каталог из `.gitignore`, зелен только на своей машине.
- L3310 [rules] Правка, применённая наполовину, выглядит применённой.
- L3326 [rules] Незаписанный отказ нельзя посчитать, а непосчитанный отказ выглядит как отсутствие признака.
- L3342 [rules] Смена версии правила снимает признаки сразу у всех, и это надо сказать числом.
- L3352 [rules] У раздела не было обхода по страницам, и это оказалось настоящей причиной «одного КРТ».
- L3368 [rules] Оговорка «мы этого не читаем» устаревает молча.
- L3379 [rules] Одна жалоба — одна цепочка, и чинить надо то звено, где теряется.
- L3406 [rules] Машино-места считает постановление, и это одна формула на все поверхности
- L3433 [rules] Функция, читающая переменную страницы, обязана пережить её отсутствие.
- L3451 [rules] Необъявленная переменная страницы — не тихий undefined, а обрыв загрузки.
- L3471 [rules] У соцобъекта два числа: бюджет от МЕСТ, площадь для контроля объёма
- L3488 [rules] Средняя квартира — делитель с ОСНОВАНИЕМ, а не число
- L3505 [rules] Снижение балла меряют, а не назначают.
- L3516 [rules] Пол, написанный как `Math.max(points, N)`, не срабатывает там, где нужен.
- L3526 [rules] Подпись сравнения называет порог и своё число.
- L3536 [rules] Отказ подбора называет, чьё число не дотянуло, — иначе он спорит со сводом.
- L3560 [rules] Расшифровка пика БРИДЖа свода — по очередям с открытой линией, а не по всем.
- L3579 [rules] Линия, рассчитавшаяся в РВЭ, после него не кредитует — и это относится не только к переносу долга.
- L3598 [rules] Стартовая цена скрининга — та же, что рекомендует отчёт о рынке.
- L3611 [rules] Объект строится в своей очереди и не раньше её РнС — БРИДЖ стройку не финансирует
- L3625 [rules] БРИДЖ у очередей — свой у каждой, и блок рисуется по очередям.
- L3634 [rules] Проектирование, подготовка и сети идут по размеру очереди, ИРД — вперёд.
- L3642 [rules] Метры Программы реновации строятся, но не продаются — это цена входа
- L3668 [rules] Число на месте формулы шаблона рвёт цепочку молча, и снаружи оно неотличимо от посчитанного.
- L3686 [rules] Лимит очереди округляют по очереди, а не по сумме.
- L3694 [rules] Календарь объекта — это ячейки объекта, а не доля от общей даты.
- L3704 [rules] Ключ вводной живёт там же, где решается, куда писать значение.
- L3713 [rules] Счёт «чего в книге нет» считает то, о чём знает.
- L3721 [rules] Вводная, которую никто не читает, — не вводная, а обещание.
- L3735 [rules] Цвет — утверждение о ячейке, и врать он может так же, как подпись.
- L3748 [rules] ТЭП вбивается там, где его не найти, и это не придирка к вёрстке.
- L3763 [rules] Кнопка обновления обязана сходить в источник, а не перерисовать файл.
- L3782 [rules] Очередь платит общепроектные статьи по своему объёму, и цена метра очереди стоит рядом с вводной
- L3801 [rules] Число берут из оборота, который его называет, — а обрезка прячет непонятое.
- L3837 [rules] Отдельно стоящий объект меряется своей площадью, а наземный паркинг — местами
- L3868 [rules] В расчётный лимит БРИДЖа входит то, что платится ДО РнС
- L3884 [rules] Предел длины вопроса Платону — на сценарий, а не на весь маршрут.
- L3897 [rules] Обзорная карта рисовала только то, что город положил в свой файл.
- L3916 [rules] Сводная линия эскроу — сумма чужих счетов, и на вопрос про очередь она не отвечает.
- L3938 [rules] Один драйвер на два сценария — это второй сценарий, выданный за первый.
- L3964 [rules] Число квартир двигает машино-места — пунктом 2 по средней квартире.
- L3979 [rules] Цифры площадки лежат в документе, а не только в карточке.
- L4003 [rules] Вид величины объявляют там же, где её пишут.
- L4012 [rules] Пустой выбор фильтра неотличим от сломанного, пока рядом нет числа.
- L4019 [rules] Оси фильтра различают по вопросу, на который отвечают, а не по полю данных.
- L4039 [rules] Источник засчитывается в «спросили» только там, где он мог бы ответить.
- L4052 [rules] Спор двух источников решается только там, где обе стороны переживают перезагрузку.
- L4079 [rules] `computed_at` отвечает «когда», а не «чем».
- L4107 [rules] Порядок, изменившийся молча, читается как несработавшая сортировка.
- L4114 [rules] Съехавший разбор прячут целиком, а не только в ячейке.
- L4124 [rules] Прогон ходит по тому списку, который видит человек, а не по одной его половине
- L4157 [rules] Рассрочка ВРИ и резерв: методику снова меняли в одном месте из двух.
- L4180 [rules] Пятисотка отвечает текстом, а страница разбирает её как JSON.
- L4198 [rules] Отказ ресурса — не смерть страницы, а ловушка их не различала.
- L4214 [rules] Отдают метры и места, а не рубли
- L4235 [rules] Гостевые места приходят числом только из выгрузки, остальным их выводят обратным ходом.
- L4265 [rules] Витрина продаж называет застройщика СВОЕГО дома, и номер у дома и у владения один и тот же.
- L4298 [rules] Балл меряется тем объёмом, который у площадки ЕСТЬ, и мера называется.
- L4318 [rules] Объект уходит целиком одним путём, и признак у него один
- L4339 [rules] Проверка, не знающая о новой строке, кричит на верном расчёте.
- L4351 [rules] Имя функции у юрисдикций своё, и одно на обе таблицы найдёт не ту строку.
- L4362 [rules] «Не тронули» читается как «убрали».
- L4377 [rules] Надпись у поля — утверждение о ПОЛЕ, а не о методике.
- L4399 [rules] Подпись под графиком называет то, что на графике видно.
- L4417 [rules] Один рисовальщик на три контейнера — три копии на экране, и строковый тест их не видит.
- L4434 [rules] Лист, который собрали мы, обязан выглядеть книгой владельца.
- L4473 [rules] Правило списка применяют там, где список опознан.
- L4496 [rules] 404 от сервиса — «сервис не отдал», а не «объекта нет».
- L4527 [rules] Ось без единицы читается как другая шкала.
- L4535 [rules] Строка, вставленная между `if` и `else`, уводит себе чужой `else`.
- L4547 [rules] «Жив ли прогон» спрашивают у списка процессов, а не поиском по своей же строке.
- L4564 [rules] Точка входа модуля стоит в конце файла.
- L4570 [rules] Цены сделки в отчёте «Пульса» нет, и это измеряют, а не предполагают.
- L4589 [rules] Тип объекта решает, что попадает в цену и в комнатность.
- L4596 [rules] «Продано застройщиком» — не «продано».
- L4603 [rules] Имя поля бывает занято другим источником.
- L4609 [rules] Месяц выпуска в проверке числом не закрепляют.
- L4613 [rules] Прогон идёт по тому списку, который видит человек.
- L4623 [rules] У площадки-решения субъект есть — это её адрес, а обязательства лежат в её же PDF.
- L4647 [rules] Голая цифра адресом не является — а чужой контур подписан «официальный полигон».
- L4665 [rules] Число становится номером владения только в адресной позиции — и это же правило решает, откуда берётся балл.
- L4701 [rules] Колонка сортируется тем словом, которое в ней написано.
- L4712 [rules] Балл подписан тем, кто его считает, а не тем, чьё имя красивее.
- L4721 [rules] Одно и то же не говорят дважды подряд.
- L4734 [rules] Половина списка приезжает из другого источника, и карточка обязана называть свой.
- L4754 [rules] Догадку о причине проверяют на своём проекте, а не правилом.
- L4772 [rules] Раздел без картинки и без вывода — это две таблицы, а не раздел.
- L4782 [rules] Карточка была короткой, а колонка под ней — 118 500 пикселей.
- L4795 [rules] Порядок карточки — порядок решения, и у каждого шага есть имя на экране.
- L4806 [rules] «Проверка данных» говорит своё место сама.
- L4816 [rules] Длинный список сворачивается, короткий стоит открытым, и порог назван один раз.
- L4830 [rules] Неразобранная строка до модели не доходит, а «не из набора» сверяют без регистра.
- L4866 [rules] Модель считает и по объявленной цене торгов, а не только при нуле.
- L4886 [rules] Один документ в двух разделах портала — одна строка, и убранную называют числом.
- L4908 [rules] Строгое правило совпадения отказывало на ТОЖДЕСТВЕ.
- L4925 [rules] Вывод пишут словами вопроса, а не именами величин.
- L4945 [rules] Продаётся то, что можно поставить на кадастр
- L4992 [rules] Гараж объекта — величина проекта, и по очередям она делится или удваивается.
- L5018 [rules] Верхний край — это ответ, но ответ НАШ, и двигает он не только число.
- L5044 [rules] Своя правка, названная ошибкой источника, читается как находка в нём.
- L5063 [rules] Нормативное собрано вместе, кнопки после него, модель и Платон — в конце
- L5077 [rules] Пересчёт ТЭП по плотности книга умеет, и владеет им она — не трогать.
- L5098 [rules] Ревизия формул считает четыре болезни, а сторож был на одну.
- L5120 [rules] Легенда обещает ровно те полосы, которые нарисованы.
- L5136 [rules] Раздел, которого нет в общем выводе, отвечает только на свой вопрос.
- L5157 [rules] Доля за месяц и доля за год — разные величины, и окно называется у самой полосы.
- L5196 [rules] Ответ, за которым надо догадаться нажать, — ответ, которого нет.
- L5212 [rules] Кнопка стоит там, куда смотрят, а не там, где кончается блок.
- L5240 [rules] Двенадцать строк одним списком — это не таблица, а список; и одно имя на продукт бывает только тогда, когда оно объявлено один раз.
- L5270 [rules] Прежде чем рисовать величину, считают, есть ли в ней сигнал.
- L5288 [rules] Победитель, выбранный порядком множества, — два ответа об одном проекте.
- L5297 [rules] Итог книги считает её же формула, и текстовая ячейка в неё не входит.
- L5309 [rules] Выгрузку называют так, как отвечает источник, а не так, как её прислали.
- L5321 [rules] Землю под зданием спрашивают точкой, а не полем.
- L5332 [rules] Правообладатель земли и правообладатель строения — разные лица, и колонка у них разная.
- L5343 [rules] Слой, который крупнее, лежит ниже — иначе верхнего не существует.
- L5351 [rules] Служебная страница — та, на которую ниоткуда не ведёт ссылка, а числа за ней спрашивают владельца
- L5360 [rules] Кусок карты, вынутый из `PAGE`, тянет за собой не только карту.
- L5370 [rules] Фигура на карте опознаётся своим ключом, а не местом в списке.
- L5379 [rules] Площадку в реестре опознают геометрией, а не именем.
- L5389 [rules] Площадь участка и площадь, ВХОДЯЩАЯ в площадку, — разные величины, и справочная колонка обязана назвать свою.
- L5405 [rules] Номер, связывающий карту с таблицей, считает сервер.
- L5414 [rules] «Один экран» — это порядок и отбор, а не одно место.
- L5429 [rules] Стоимость земли и стоимость строений в одну колонку не складываются.
- L5444 [rules] Картинку без координат на карту не накладывают.
- L5458 [rules] Колонку книги читают по заголовку, а не по номеру — и проверки тоже.
- L5468 [rules] Подпись ставят в самое широкое место фигуры, а не в центр её рамки.
- L5482 [rules] `max-height` с `overflow:hidden` — это обрезка молча.
- L5490 [rules] Счётчик по одному источнику молчит о втором.
- L5498 [rules] Утверждение о втором источнике, поднятое к плиткам первого, читается как сомнение в первом.
- L5506 [rules] Итог по ЕГРН обязан стоять рядом с площадкой, которую он не равен.
- L5520 [rules] «Участка нет» и «участок есть, права не зарегистрированы» — разные состояния, и под похожими словами они сливаются.
- L5534 [rules] Бледнеть надо по величине, а не по признаку.
- L5543 [rules] Подпись вправе слегка выступать за мелкую фигуру.
- L5553 [rules] «Уверен» — плохой ответ там, где есть чем померить.
- L5562 [rules] Кадр строят по предмету, а не по всему нарисованному.
- L5571 [rules] Высоту кадру задаёт пропорция, а не картинка подложки.
- L5577 [rules] Ответ — числом в плитке, объяснение — один раз.
- L5588 [rules] Вырезая соседа, проверь, кого утащил.
- L5595 [rules] Ссылка на служебную страницу — тот же механизм, что «Поделиться» проектом
- L5608 [rules] Код доступа живёт своим файлом, а не в чужом.
- L5613 [rules] Ключей у сервиса два, и отказ обязан называть, какого не хватило.
- L5627 [rules] Пустой параметр в `bool` — это 422, а не «нет».
- L5640 [rules] Долю налога считают той мерой, из которой налог возник, — а мера эта год.
- L5672 [rules] Убыток переносится бессрочно, ограничение — в доле
- L5688 [rules] Состав пишет тот, кто видит его целиком, — иначе список сам себе делает новости.
- L5722 [rules] Величина, которую источник назвал ДРУГИМ полем, — это данные, а не их отсутствие.
- L5750 [rules] Кнопка, которая не может сработать, гаснет до нажатия, а отказ печатается там, где нажали.
- L5766 [rules] Красный main меряют полным прогоном по нему, а не тем файлом, который упал первым.
- L5788 [rules] Браузерные проверки на CI не идут вовсе, и «зелёный CI» о них не говорит ничего.
- L5811 [rules] Русскую дату разбирает сервер, браузер её читает не тем порядком.
- L5846 [rules] Лист трогают строкой, а не ячейкой, — и это правило уже было записано один раз.
- L5871 [rules] Сторож считает проходы, а подорожал сам проход.
- L5895 [rules] Ширина сетки объявлена один раз, а копии её живут внутри формул.
- L5913 [rules] ННП — площадь в габаритах ВНУТРЕННИХ стен, то есть общая, а не ГНС
- L5930 [rules] Сверка «ничего лишнего не изменилось» не отвечает на «всё ли нужное изменилось».
- L5942 [rules] Подсказка у поля и подпись под ним — одно место, а не два.
- L5958 [rules] Функцию, написанную объяснять молчание, надо ещё и позвать.
- L5981 [rules] Производную считают там, где её читают, — а хранят её в четырёх местах и по-разному.
- L6010 [rules] Денежная компенсация — не объект, и метров его в проекте нет
- L6020 [rules] Заглушка с придуманной сигнатурой проверяет не наш код, а себя.
- L6033 [rules] Гейт, которому передали не то, отвечает «нет» всем и всегда — и выглядит строгостью.
- L6050 [rules] У молчащего сторожа обязан быть счётчик молчания — второй раз, теперь у нормативов.
- L6067 [rules] Кнопка отвечает на тот вопрос, который в её подписи.
- L6078 [rules] Час без зоны — другой час, и в Москве это не видно.
- L6098 [rules] Расход признают в месяце, когда он возник, а не когда за него заплатили.
- L6125 [rules] Ширина сетки книги объявлялась трижды, и обрезка молчала.
- L6164 [rules] Экранирование чужого XML не раскодируют и не кодируют обратно.
- L6173 [rules] Ответ одного экрана собирают по одному чтению источника.
- L6216 [rules] Умолчание строки ТЭП — не литерал, а тот же счёт, что у всех.
- L6233 [rules] Делитель совпал с числителем случайно — и держался на чужом нуле.
- L6251 [rules] Строка, которую чинит только страница, чинится не везде.
- L6285 [rules] Площадь документа — наземная, и колонка у неё своя.
- L6305 [rules] Проверка, написанная под прежний ответ, закрепляет его.
- L6315 [rules] Стенд разрешает зависимости страницы сам, а не перечисляет их.
- L6354 [rules] Состояние стенда задают таким, каким его оставляет страница.
- L6368 [rules] Правило, закрытое на одной вкладке, соседнюю не защищает
- L6392 [rules] «Изменилось» без единого своего слова — это «скачали не тот документ».
- L6410 [rules] Маршрут, объявленный выше своей модели, читает тело как строку запроса.
- L6440 [rules] Отметка времени, напечатанная как есть, — не дата.
- L6477 [rules] Прогон делят, когда времени нет у отдельных тестов, — а делят по файлам и своими раннерами
- L6531 [rules] Деление прогона нашло тест, который зависел от соседа.
- L6558 [rules] Число без окна и без предмета сравнивают с чем попало.
- L6583 [rules] Модель не сходилась сама с собой, и нашла это приёмка чужой правки.
- L6605 [rules] Снимок «ни одно число не сдвинулось» слеп там, где числа нули.
- L6626 [rules] Книга пишет ТЭП, присланный страницей, а отчёт — тот, что движок посчитал.
- L6655 [rules] Пара «места и площадь гаража» читалась страницей и движком по-разному.
- L6678 [rules] Вынужденная копия живёт со сторожем, иначе отстаёт молча — и отстала.
- L6699 [rules] Проверка порядка скриптов мерила порядок объявлений — и держала сборку main красной двое суток.
- L6725 [rules] Ревизия чужой правки начинается с того, что мерят СВОЮ меру.
- L6735 [rules] Одно имя, два смысла — и обе поверхности выглядят верными.
- L6772 [rules] Приписка о переданном стоит у обеих мер, а не у той, до которой дошли.
- L6786 [rules] У максимума номеров бывает несколько держателей, и названный один прячет столкновение.
- L6797 [rules] Благоустраивают двор, а не дом — но два числа нормы с бюджетом не сошлись.
- L6844 [rules] Норматив — в профиль класса, и вместе с ним в таблицу приезжает единица.
- L6880 [rules] Доля стоит под тем числом, которое из неё получается, — и число сменило колонку.
- L6917 [rules] Облагается всё, кроме квартир, и список продуктов этого не решает.
- L6933 [rules] Производная величина не может быть точнее своих слагаемых.
- L6943 [rules] Строку таблицы книги читают по подписи, а не по номеру — и проверки тоже.
- L6949 [rules] Фикстура, подобранная под порог, устаревает вместе с методикой.
- L6956 [rules] Страницу проверяют по тому адресу, по которому её открывает человек, — а не файлом с диска.
- L6980 [rules] Буква у `-r` заменяет умолчание, а не добавляется к нему.
- L6989 [rules] Проценты по рассрочке ВРИ вычета НДС не дают, и строка паритета нашла это первым же прогоном.
- L7006 [rules] Путь к браузеру спрашивают у сервиса, а не помнят числом.
- L7014 [rules] Проверка «строка входит в вердикт» держала конец диапазона литералом.
- L7020 [rules] Диапазон, написанный краями, читается вычитанием — а имя периода нет.
- L7039 [rules] Хост, который не может сделать работу, не должен её начинать.
- L7060 [rules] Величина, за которой источник спрошен не о том, — не молчание источника.
- L7082 [rules] Апартаменты и квартиры лежат в одном классе, а покупатель у них разный.
- L7104 [rules] Витрину двигает не только прайс: у соседа есть рассрочка.
- L7160 [rules] Предложение по акции живёт на пересечении двух сводок, и до сих пор их было две разных.
- L7199 [rules] Этажа в источнике нет, а комнатность есть и не читалась.
- L7232 [rules] Штуки и метры складываются только внутри своего товара — рубли складываются всегда.
- L7265 [rules] «Формат не поддержан» бывает нашим пробелом, а не чужим форматом.
- L7280 [rules] DOCX — тоже зип, а подпись площадки врёт в обе стороны.
- L7289 [rules] У архива три списка, а не один: прочитанное, спутники и непрочитанное.
- L7299 [rules] Проверку на чужой флаг нельзя собрать чужим писателем.
- L7307 [rules] Число за всё время не отвечает на «как сейчас», а колонка без существительного — своя мера.
- L7328 [rules] Мера товара — часть товара, и нулевая площадь проданного лота это пропуск.
- L7352 [rules] Имя меры и единица при числе — разные формы.
- L7364 [rules] У величины есть мера — значит сравнение с ней стоит рядом, в обе стороны.
- L7388 [rules] Подпись у пары полей, стоящая ВЫШЕ ввода, приклеивается к соседней строке.
- L7401 [rules] Поток и остаток складываются по-разному, и очередь после своего горизонта не исчезает.
- L7422 [rules] Остановку прогона проверяют списком процессов, а не кодом возврата.
- L7431 [rules] Получателя переданных метров модель не знает — значит не называет.
- L7456 [rules] Инфляция объекта — от старта его очереди (решение владельца, 13.09.2026).
- L7473 [rules] Широкое содержимое держит СВОЁ окно прокрутки, а на бумаге его размыкают.
- L7499 [rules] Ссылку на источник проверяют содержимым, а не кодом ответа.
- L7512 [rules] Проект, ставший законом, — оговорка устаревает в опасную сторону.
- L7530 [rules] Отказ, записанный в общий список, до свода не доезжает — и свод отвечает «не спрашивали».
- L7543 [rules] Спутник ПРИ содержимом и спутник ВМЕСТО содержимого — разные ответы.
- L7554 [rules] Пустой ответ площадки и «документов нет» на экране одно и то же.
- L7561 [rules] Образец, выбрасывающий «служебное», выбросил и то, ради чего написан.
- L7581 [rules] Зарегистрированное право без имени — не отсутствие права.
- L7596 [rules] `async` стоит перед словом `function`, и стенд терял это слово.
- L7602 [rules] Один запрос на вложение выдаёт перебой площадки за отсутствие документа.
- L7626 [rules] Байты вложения разбирают несколько читателей — значит файл лежит, а не выбрасывается.
- L7658 [rules] У склада на диске есть строка в `.gitignore` — и спрашивают об этом git.
- L7669 [rules] Данные объявляют выше своих читателей, а не дозаписывают после.
- L7688 [rules] `const`, прочитанный выше своего объявления, роняет ВЕСЬ скрипт страницы — и молча.
- L7706 [rules] Стенд, несущий свою копию состава, — вторая жизнь у списка, объявленного один раз.
- L7723 [rules] Выбор значения, которое и так стоит умолчанием, ничего не различает.
- L7730 [rules] «Exit code 0» фоновой задачи — код последней команды цепочки, а не прогона.
- L7739 [rules] Проверка, не знающая о новой величине, кричит FAIL на верном расчёте — и сторож паритета этого не видит.
- L7758 [rules] Рукописная копия чужого листа отстаёт в тот же день, когда код учится новой формуле.
- L7768 [rules] Фикстура, у которой вводные и ТЭП говорят об объекте разное, — не проект.
- L7778 [rules] Клик по узлу, которым владеет другой слой, — гонка, и в одиночку её выигрывают.
- L7788 [rules] Читатель, режущий строку, делает сторожа слепым за срезом — и зрячим там, где ничего не сломано.
- L7805 [rules] У продукта, которого книга не знает, есть в ней ПУСТАЯ строка — и итог над ней его складывает.
- L7837 [rules] Проверка, зелёная по совпадению последнего разряда, не доказывает ничего.
- L7858 [rules] Фикстура, кладущая величину прямо в строку ТЭП, устаревает в тот день, когда строку начинают ВЫВОДИТЬ.
- L7874 [rules] «Падает и на main» — это замер, а не догадка, и мерить надо ВЕСЬ список.
- L7886 [rules] У записанного отказа спрашивают, не удался ли запасной путь.
- L7917 [rules] Свод территории собирает один сборщик, а площадки меняют ему источники.
- L7958 [rules] Свод из одного источника, поделённый на коэффициент, — не измерение того класса, на который его поделили.
- L7989 [rules] Проверка, стоящая на позиции, падает от дописанной рядом строки.
- L8008 [rules] Поле, сменившее место правки, объявлено там же, где было, — переезжает только отрисовка.
- L8033 [rules] У всего, что приходит без спроса, обязан быть выключатель — и он один на все поводы.
- L8060 [rules] Гейт стоит у той двери, в которую ходят, — а Telegram ходит в обёртку.
- L8093 [rules] Гейт читал не ту величину: карта отказывалась рисовать, имея 55 контуров.
- L8117 [rules] Счётчик считает то, что читают, — и это третья копия одной болезни.
- L8139 [rules] Работа, которую делает только рука, на экране выглядит ответом источника.
- L8173 [rules] Гейт по виду вложения, которого площадка не ставит, не срабатывает ни разу.
- L8191 [rules] Ячейки строки листа идут по возрастанию колонки — иначе Excel их теряет молча.
- L8221 [rules] Проверка, отобранная по имени строки, слепа ко всему остальному листу.
- L8241 [rules] Восстановить вводные из отчёта можно, но проверяется это прогоном, а не перепиской чисел.
- L8259 [rules] Перечисление объектов руками теряет объект, заведённый позже — второй раз за десять дней.
- L8286 [rules] Подземное в колонке «ГНС» — и три соседние потери в одном блоке.
- L8309 [rules] Самозакрывающаяся ячейка съедала следующую, и книга выглядела пустой.
- L8322 [rules] Детализация соцнагрузки в книге была числами, и это меряется, а не обсуждается.
- L8347 [rules] Цель паритета замерла на дате сборки, и правка вводной красит книгу зря.
- L8385 [rules] Форм у источника больше, чем читает разбор, — и молчание об этом наше.
- L8409 [rules] Печатная форма форме не равна.
- L8417 [rules] Повтор объявляют один раз, и площадка, отвечающая через раз, требует его у КАЖДОЙ двери.
- L8432 [rules] Ось фильтра называет вопрос, а не слово источника.
- L8452 [rules] Пустого раскрытия не бывает.
- L8465 [rules] Срок, взятый у шлюза, не годится сторожу.
- L8476 [rules] Разрешитель зависимостей стенда знает, чья это страница.
- L8487 [rules] Разобранное лежит на диске с версией правил, которыми прочитано, — иначе починка читателя не доезжает до прочитанного.
- L8518 [rules] Гейт стоит по действию документа, а не по его подписи.
- L8548 [rules] `a||b ? x : y` — это `(a||b) ? x : y`, и первая половина условия в ответ не попадает.
- L8562 [rules] Негодное тело запроса — это отказ с причиной, а не пятисотка.
- L8599 [rules] Правило, закрытое у одного поля, соседнее не защищает — теперь у номера распоряжения.
- L8618 [rules] Разрешитель стенда слеп к ошибке, которую чужой `try/catch` проглотил.
- L8646 [rules] Сторож запирал свой же гейт: обход просился строкой ВЫШЕ проверки полноты.
- L8671 [rules] Различающий ответ, до поверхности не доехавший, — это ответ, которого нет.
- L8698 [rules] Номер берут целиком, а границу ему задаёт форма, а не длина.
- L8730 [rules] Счётчик устаревшего разбора считал записи, а у склада без записей отставать нечему.
- L8748 [rules] Немой отказ чинить нечем: он обязан назвать, что видел.
- L8758 [rules] Показатель, который предлагают вписать, считается на базе того поля, куда его вписывают.
- L8773 [rules] «Имени нет» у node две формы, и стенд узнавал одну.
- L8782 [rules] Явный признак сильнее совпадения с числом.
- L8794 [rules] Свод рядом со ставкой обязан назвать СВОЮ базу, а подсказка — не ответ.
- L8809 [rules] Предохранитель, меряющий одну координату, падает на законной сетке.
- L8830 [rules] Допуск проверки не бывает уже ступеньки самой величины.
- L8849 [rules] Подпись, идущая за выбранным видом, отвечает не о том поле, под которым стоит — а если она ещё и пишет, то пишет чужое число.
- L8878 [rules] Стенд, гоняющий node сам, не знает о функции, заведённой вчера.
- L8888 [rules] Форма, которую забирают целиком, превращает показанное в введённое.
- L8916 [rules] Ожидание в браузерной проверке не смотрит на то, что проверяется.
- L8925 [rules] Обрезка по длине стоит между диагнозом и читателем.
- L8949 [rules] Счёт «чего в книге нет» мерит то, чем меришь, — и трижды соврал в разные стороны.
- L8977 [rules] Сколько у источника есть — спрашивают у источника, а недособранный обход дополняет, а не заменяет.
- L9015 [rules] Свой предел — тоже правило читателя, и меняя его, поднимают версию.
- L9034 [rules] Мера полноты сравнивает однородное, а молча выброшенное делает объявленное число недостижимым.
- L9068 [rules] Зависание, которого нет в песочнице, мерят там, где оно есть — а для этого молчание обязано заговорить.
- L9092 [rules] Раннер ждёт закрытия ТРУБЫ шага, а не завершения дерева процессов.
- L9115 [rules] Границу числу задаёт форма, а не длина клетки — и склеенное рядом не делает число непрочитанным.
- L9148 [rules] Две поверхности, ошибающиеся одинаково, паритетом не ловятся — и это был не недосмотр, а копия.
- L9175 [rules] Величина, живущая в `finance`, на поверхности не существует.
- L9194 [rules] Одобренный лимит был потолком в движке и справкой в книге.
- L9214 [rules] Поле, которое просят заполнить, обязано лежать на листе ввода, и решает это СТИЛЬ.
- L9226 [rules] Формула, идущая в `_v4_set_cells`, — сырая: экранирует она сама.
- L9234 [rules] `git checkout -- файл` откатывает файл ЦЕЛИКОМ, включая несохранённую работу.
- L9240 [rules] Общий лимит ПФ у очередей — ожидание, потолок очереди — только её НКЛ
- L9278 [rules] Поле, которое пишут рядом со строкой и не читает никто, — это молчание, а не запись.
- L9326 [rules] Дешёвый гейт перед дорогой дверью — копия условия по построению, и она обязана быть вызовом.
- L9344 [rules] Пустая сторона пары с непустой не спорит — и потому признак читают отовсюду, а не оттуда, где его писали в первый раз.
- L9373 [rules] Дашборд книги и тизер — одна модель представления, и она не считает
- L9396 [rules] У источника бывает две поверхности, и вторую надо СПРОСИТЬ, а не считать копией первой.
- L9427 [rules] Сторож с литералом падает на том, ради чего написан, и молчит о самой величине.
- L9447 [rules] Кнопка и подпись под ней отвечают на один вопрос — значит считает их одно правило.
- L9460 [rules] «Последний прогон на ветке» — не тот прогон, которого ждёшь.
- L9470 [rules] Поле, заведённое без подъёма версии правил, доезжает только до новых строк.
- L9490 [rules] Номер выпуска не опознаёт образ: сторож версии на слиянии не перечитывают.
- L9510 [rules] Замок, который отпускает `finally`, переживает смерть процесса — и полчаса выдаёт труп за живого.
- L9539 [rules] Вердикт проверки, прочитанный вскоре после её старта, — не вердикт, а её незаполненное состояние.
- L9547 [rules] «Доехал ли PR в main» сверяют по содержимому, а не по номеру в сообщении коммита.
- L9555 [rules] Диверсию снимают копией файла, а не `git checkout` — и я это проверил на себе.
- L9565 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9607 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9608 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9610 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9614 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9656 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9657 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9659 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9661 [checks] Две величины на одной шкале — это одна из них, сплющенная.
- L9673 [checks] Легенда — часть графика, и объявляется там же, где он один.
- L9682 [checks] Своё окно прокрутки помнит, докуда его дочитали.
- L9698 [checks] Шапка отвечает на тот же вопрос, что метки строки, — и тогда она обязана назвать их цену.
- L9712 [checks] Паспорт площадки — её собственные числа, и он опознаёт публикацию не хуже адреса
- L9733 [checks] Улица опознаёт квартал — и «Строгино 360» это подтвердило, а не опровергло.
- L9756 [checks] Гейт читает поле, а вторая сборка строки его не кладёт — и отказ винит источник.
- L9782 [checks] Связку считают по тому списку, который видит человек.
- L9802 [checks] У молчащего сторожа обязан быть счётчик молчания — и у сторожа новостей его не было.
- L9831 [checks] Итог прогона читают в ЕГО файле, а не в том, что подвернулось.
- L9846 [checks] «Работа принята» и «пустого ответа» с виду не отличить, и поверхность, которая ждать не умеет, показывает принятое как отказ.
- L9862 [checks] Одно имя — один автор: «Короткий вывод Платона» считал не Платон.
- L9875 [checks] Порядок карточки КРТ: сначала выводы, потом данные города, потом детали
- L9890 [checks] Два отрисовщика на одни данные — две разные карточки об одной площадке.
- L9898 [checks] `100vh` на телефоне — высота вместе с полосой браузера, а не то, что видно.
- L9919 [checks] Репозиторий публичный — решение владельца (07.09.2026), и потому чужое в него не кладут.
- L9934 [checks] Срок строительства при очередности задаёт очередь, и поле проекта обязано это сказать
- L9967 [checks] Погасшая кнопка без слов читается как несработавшее нажатие.
- L9989 [checks] Пример поломки, взятый у умолчания, умирает вместе с его починкой.
- L9996 [checks] Встречную правку одного места находят по одному и тому же падению.
- L10013 [checks] Правка строки заполняет свою строку, а доля целого связывает соседние.
- L10046 [checks] Число из документа и формула документа — разные вещи, и заморозка выхода ломает вход.
- L10070 [checks] Правило, закрытое у одного читателя, соседнего не защищает — и «живой лот» это доказал во второй раз.
- L10084 [checks] Маркер конфликта — рабочий код до тех пор, пока его никто не ищет.
- L10098 [checks] Первое вхождение строки — не то место, о котором думает проверка, и на этом лёг main.
- L10126 [checks] Свод превращает снятые доли в «failure», и прежний признак на нём не работает.
- L10141 [checks] Смягчение, которое даёт город, — справка у числа, а не множитель в расчёте
- L10161 [checks] Поле, заполненное нормой, обязано нести её пометку — иначе оно замирает и выглядит решением человека.
