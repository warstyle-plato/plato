# Индекс исторических правил CLAUDE

Собирается из архива: `python3 scripts/claude_rule_index.py --write`.
Руками не править — правка потеряется при следующей сборке.
Полный снимок прежнего файла: `docs/CLAUDE_HISTORY_2026-09-20.md`.
Ищите здесь по имени файла, функции, маршруту, предмету или ключевому слову; затем открывайте только нужный фрагмент архива.

- L8 [setup] `main_legacy.py` — движок (~17 000 строк): расчёт, страница (`PAGE`), Telegram-бот,
- L10 [setup] `main.py` — обёртка: грузит движок как `developaid_core`, монкей-патчит поведение
- L12 [setup] Бот и движок — **один процесс**, не разные сервисы.
- L13 [setup] Разворачивается двумя воркерами (`--workers 2`). Память у них раздельная:
- L19 [rules] Токен Telegram-сессии не опознаёт проект.
- L21 [rules] Формулы шаблона ПЛАТО не перезаписываются
- L24 [rules] Поле, которого нет в карте записи, молча остаётся мусором из шаблона.
- L27 [rules] `JSON.stringify` превращает пропуск в массиве в `null`.
- L29 [rules] Отсутствующий ключ — не «снято».
- L34 [rules] Сохранённое состояние браузера накладывается на умолчания, не заменяет их.
- L36 [rules] Тяжёлое не считается внутри запроса, от которого зависит окно.
- L38 [rules] Ошибка, ушедшая только в лог, — это ошибка, которой нет.
- L40 [rules] Поверхности считают один раз, а не каждая своё.
- L47 [rules] Методику меняют в двух местах: в движке и в книге.
- L55 [rules] Формула книги живёт в горизонте движка и на его базах.
- L69 [rules] Импорт ГлавАПУ включает то, для чего принёс данные.
- L74 [rules] Рассрочка ВРИ начинается в дату обязательства, а не периодом позже.
- L80 [rules] Снятый признак банковского бюджета ВРИ — тоже потеря, а не решение.
- L86 [rules] Заданная плата за ВРИ платится, даже если признак снят.
- L91 [rules] График платежей ВРИ доезжает в книгу отдельно от суммы.
- L98 [rules] Версия объявляется один раз — `main_legacy.VERSION`.
- L115 [rules] Маршрут Платона решает `PLATO_AI_URL`, а не наличие ключа.
- L122 [rules] «Зона есть» — не ответ, ответ — сколько участка она съела.
- L132 [rules] Пустой результат проверки — не «чисто».
- L139 [rules] Счёт виден, вывод — за входом (решение владельца, 18.08.2026).
- L147 [rules] Личность подтверждает телеграм, анкета — нет.
- L154 [rules] Ожидание без признака работы читается как внезапность.
- L161 [rules] Эмблема одна на все поверхности, и она лежит в `PAGE`.
- L167 [rules] Документ ИП живёт не только на главной.
- L173 [rules] Зашитая цифра на экране неотличима от посчитанной.
- L183 [rules] Импорт участка обнуляет то, что участку принадлежит.
- L192 [rules] Зелёный набор тестов не значит, что ветка кода верна — возможно, до неё просто не доходят.
- L197 [rules] Капитализация процентов лимит ПФ не выбирает и не уменьшает
- L203 [rules] Методика финансирования сверена с НКЛ Сбера
- L215 [rules] В компенсацию за соцобъекты входит земля, а земля у каждого квартала своя.
- L223 [rules] Нормативный пакет лежит файлом, а числа из него — версионируемым рядом.
- L237 [rules] Места постоянного размещения — приложение 5 к 945-ПП в редакции 2118-ПП
- L250 [rules] Коэффициенты парковки берём у ГлавАПУ, своего справочника районов нет
- L260 [rules] Территорию город уже посчитал — пересчитывают количество, а не методику
- L277 [rules] Калькулятор считает по нормативному ТЭП, а строят по утверждённому.
- L293 [rules] Коэффициент выбирают по признаку, а не по удобству.
- L303 [rules] Формула платы за ВРИ сверена с исходником калькулятора ГлавАПУ
- L317 [rules] Методику города сверять по выгрузкам, а не подгонять по итогу.
- L332 [rules] Специальная ставка в договорах ступенчатая — сделано (20.08.2026).
- L346 [rules] Доля из документа и доля из методики — разные числа, и подписывать их одинаково нельзя.
- L358 [rules] Фактический ТЭП вводится руками, второй колонки рядом с нормативом нет
- L366 [rules] Переданные муниципалитету метры строятся, но не продаются.
- L379 [rules] Профиль расходов, объявленный дважды, расходится там, где считают ступень.
- L394 [rules] Гостевые машино-места строятся, но не продаются, и обе книги это забывали.
- L406 [rules] Кладовые прибавляют подземную площадь, а не вычитаются из гаража
- L464 [rules] Удельный показатель без второй базы читается как другой показатель.
- L470 [rules] Правило, применённое к боту, надо применить и к сайту.
- L476 [rules] Порядок разделов PDF задаётся при сборке, а не порядком вычислений.
- L480 [rules] Признак телеграма — параметры запуска, а не SDK.
- L486 [rules] Чужой DOM не трогать: React считает свои узлы своими.
- L492 [rules] Селектор на чужой странице живёт парой с диагностикой.
- L499 [rules] Запасной ответ не живёт в кэше наравне со штатным.
- L503 [rules] Длинный ответ забирается вторым запросом, а не одним соединением.
- L509 [rules] Работу не держат соединением — её принимают.
- L522 [rules] Список полей и умолчания страница берёт из движка, а не хранит свои.
- L532 [rules] Модуль не заводит своего пути туда, где у движка уже есть общий, — берёт крючком.
- L547 [rules] До Render с ядра не дойти — направление развёрнуто (06.08.2026).
- L557 [rules] Собственные средства до ПФ — бесплатные, это решение владельца (06.08.2026).
- L566 [rules] Пустая выгрузка — не «людей не было», а «журнал не пережил выкатку».
- L577 [rules] Документ обещает то, что делает код, — иначе он неверен, а не неточен.
- L589 [rules] Оценка без автора — не обратная связь, а цифра.
- L599 [rules] Одна оценка — не анкета, а две записи на неё — двойной счёт.
- L610 [rules] Учёт обращений стоит там, где они сходятся, а не по веткам разбора.
- L618 [rules] Слой перестройки `/ia` живёт на чужой разметке — значит, на диагностике.
- L626 [rules] Данные страницы объявлены `let`, и свойствами `window` они не становятся.
- L632 [rules] Страница пересчитывает модель сама, а шапка обещает обратное.
- L638 [rules] В книгу уходит посчитанная льгота, а не поле «льгота — сумма».
- L647 [rules] Присланный проект — снимок, а не общий доступ.
- L658 [rules] Личность пользователя одна — chat_id из бота, и сайт её не дублирует.
- L670 [rules] Поисковый сниппет содержит слова запроса — значит и адрес объекта оценки.
- L681 [rules] Точность геокодера — часть ответа, а не подробность.
- L687 [rules] Тип страницы решает, что с неё можно взять.
- L694 [rules] Терминаторы заголовка пишут ASCII-дефисом.
- L697 [rules] Ключ сущности считают от очищенного имени, а не от сырого заголовка.
- L705 [rules] Диапазон правдоподобия не заменяет разбор числа.
- L710 [rules] Глиф без буквы выбрасывать нельзя — его надо показать.
- L720 [rules] Район — жёсткая отсечка сопоставимости, решение владельца (11.08.2026).
- L727 [rules] Строку итога прогона читать до слияния, отдельной командой.
- L731 [rules] Проверка цепляется за контракт, а не за соседнюю строку.
- L745 [rules] Два прогона в один файл — ни одного прогона.
- L754 [rules] Метка перечисления и метка прозы — разные вещи, и тест на своей форме ошибку в чужой не ловит.
- L764 [rules] Класс в конструкторе отчётов ставит «Пульс», своего суждения нет
- L773 [rules] Занятый порт называет владельца, а не приглашает расчистить.
- L785 [rules] Класс считаем так, как считает источник — включая дробные метки
- L799 [rules] Соседний класс расширяет выборку тем сильнее, чем ниже уровень.
- L806 [rules] Журнал пишется там, где обслужен запрос, а читают его в другом месте.
- L814 [rules] Выкатка не уводит прод на выпуск назад молча.
- L836 [rules] Выкатка убирает за собой, иначе диск кончается молча.
- L858 [rules] Размещение соцобъектов объявляют объектами, а не метрами в фазе.
- L874 [rules] Очередь строит то, что написано рядом с её именем, — или имя врёт.
- L896 [rules] Чей это термин — часть самого термина
- L911 [rules] Подземная часть в ГНС не входит — закрыто 04.09.2026.
- L941 [rules] Что задал город и что придумали мы — разные разделы отчёта
- L952 [rules] В отчёте для инвестора нет истории наших правок.
- L964 [rules] Скрининг каталога ставит соцобъекты первым очередям — иначе балл снимает за нашу же раскладку.
- L982 [rules] Балл — из одного источника чисел, а не из той копии, что успела загрузиться.
- L995 [rules] Балл один, и расчёт его снижает, а не заменяет собой
- L1011 [rules] Балл у лота тем же правилом, что у КРТ.
- L1021 [rules] Платон стоит там, где смотрят, а не на соседней странице.
- L1030 [rules] Новое в каталоге называется вслух.
- L1039 [rules] Посчитанное не выбрасывают.
- L1054 [rules] Вводные едут вместе с результатом, иначе их соберут второй раз.
- L1063 [rules] Присланный проект накладывается на умолчания одним способом.
- L1071 [rules] Ограничение при вводе не спасает от правки задним числом.
- L1083 [rules] Проверка равенством целиком запрещает добавлять, а не утверждает.
- L1095 [rules] Сверка живёт рядом с базой, а не рядом с суммой.
- L1110 [rules] Что входит в МКД, объявлено один раз
- L1118 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1129 [rules] Выручка одной строкой не говорит, чем очередь живёт.
- L1140 [rules] Рамка запроса шире участка — значит соседнее не вердикт.
- L1156 [rules] Скрининг спрашивал точку, а не участок — теперь контур.
- L1173 [rules] Слитый PR закончен: дописывать в его ветку нельзя.
- L1192 [rules] Состояние PR и его описание — разные утверждения, и расходятся они молча.
- L1200 [rules] Поверхность, добавленная позже, в проверку списком не попадает.
- L1214 [rules] Точка карты и точка отчёта — одна и та же точка, иначе смотрят на одну, а считают по другой.
- L1231 [rules] Скрининг КРТ читал доли ТЭП мимо разбора движка.
- L1242 [rules] Незакрытая кавычка в `PAGE` убивает продукт целиком и молча.
- L1260 [rules] Российский госсайт не проверить обычным хранилищем корней, и это не «сеть закрыта».
- L1302 [rules] Очереди — один налогоплательщик, и книга считает так же
- L1336 [rules] Умолчание, которое утверждает, — не умолчание.
- L1351 [rules] Умолчание, которое можно переписать, — уже не умолчание.
- L1382 [rules] Параметр, ухудшающий выдачу, — не безобидный.
- L1393 [rules] Отсутствие фильтра — тоже ответ, и его надо измерить.
- L1405 [rules] «Не проверили» и «не заполнено» — разные ответы, и на экране тоже.
- L1419 [rules] Карта, которую крутят, считает тот же меркатор, что карта, которую печатают.
- L1444 [rules] Заполненный контур перехватывает указатель на всей своей площади.
- L1453 [rules] `textContent` правит строку целиком и сносит вложенные узлы.
- L1461 [rules] Экранированное значение внутри `onclick` снова становится кодом.
- L1468 [rules] Оговорка живёт у числа, а не у события, из-за которого она нужна.
- L1494 [rules] Свободный номер выпуска сверяют с ветками, а не только с main.
- L1514 [rules] Ноль вместо числа и ноль как значение выглядят одинаково.
- L1522 [rules] Сумма под именем момента — не свод, а бред
- L1542 [rules] Кэш-свип: банк забирает долю поступлений очереди, передавшей долг
- L1570 [rules] Сохранённое значение сильнее базы, и правка базы под ним не видна.
- L1589 [rules] Выгрузка одна, и лишние сняты — решение владельца (30.08.2026).
- L1612 [rules] Выход, которого у этой конфигурации нет, предлагать нельзя.
- L1624 [rules] Проверок нет — спрашивают почему, а не ждут.
- L1635 [rules] Внутренний проект в своде статистики обезличен, и маскирует выдача, а не данные.
- L1657 [rules] Список отдаёт лоты, а смотрят извещения.
- L1678 [rules] Лот в списке обязан открываться — или сказать заранее, что не откроется.
- L1696 [rules] Плашка отвечает тому, кто открыл, — сообщение тому, кто не открывал.
- L1715 [rules] «Платон:» в подписи фильтра — обещание, которого фильтр не выполняет.
- L1724 [rules] Площадка в реализации инвестора уже нашла — войти в неё нельзя.
- L1738 [rules] Посчитанное за маршрутом — не показанное.
- L1756 [rules] Имена статей расходов страница держала своей копией — и статьи, заведённые позже, приезжали сырыми ключами.
- L1768 [rules] ГИС Торги — не тот рынок, и это надо было измерить до включения.
- L1804 [rules] Пропуск в ряду — не ноль, и на графике это видно.
- L1814 [rules] Продано столько-то — не показатель, пока рядом нет базы
- L1833 [rules] Вымывание отвечает на «почему не покупают» тем, что у нас есть.
- L1843 [rules] Цена от нуля не читается, но урезанная шкала обязана назваться.
- L1848 [rules] Источники одного проекта приходят порознь и живут на ядре.
- L1858 [rules] Управленцу нужна не ещё одна таблица, а вывод словами
- L1865 [rules] Причину отказа в CRM считать не по чему, и выдумывать её нельзя.
- L1880 [rules] Число берут целиком, а не с середины — во второй раз.
- L1889 [rules] Комментарий CRM — пересказ BitrixGPT, а не слова клиента
- L1894 [rules] Достаточность эскроу отвечает двумя числами, и путать их нельзя.
- L1908 [rules] Кто спрашивает из браузера — идёт через передачу работы опросу.
- L1917 [rules] Заявленное отсутствие проверяется так же, как заявленное наличие.
- L1935 [rules] Цена — всегда линия на своей шкале, а не вкладка со столбиками
- L1944 [rules] «Нет цены победителя» — не «не продалось».
- L1958 [rules] Охват торгов измеряется по реестру, а не обещается.
- L1968 [rules] Балл лота — соответствие девелоперскому профилю, и эталон измерен по сделкам
- L1988 [rules] Основная подборка — не склад неполных карточек
- L2004 [rules] Контракт SPA показывается без секрета сессии.
- L2012 [rules] Свод продаж начинался с конца — с подписанного договора.
- L2027 [rules] Раздел без вывода и раздел, которому вывод не из чего сложить, — разные вещи.
- L2034 [rules] Бюджет считают на всё сообщение, а не на его середину.
- L2048 [rules] Заголовок кодируют, чтобы раскодировать, а не чтобы он проехал.
- L2058 [rules] Потолок в страницах ограничивает объём, а не время.
- L2072 [rules] Ответ разбирают, зная, что он может быть не ответом.
- L2081 [rules] Склад кабинета общий, и это решение владельца
- L2088 [rules] Загрузка одна, потому что источник даты один.
- L2097 [rules] Проблемную очередь надо увидеть; решение за банк не выдумывают
- L2111 [rules] Дефолт называют, но модель на нём не обрывают
- L2136 [rules] Порядок строк таблицы — это рассказ, и неверный порядок читается как ошибка счёта.
- L2149 [rules] Свод по проекту не отвечает на вопрос «что куда ушло».
- L2164 [rules] Отдельно стоящий объект строится S-кривой, как жилой дом
- L2184 [rules] Одну статью нельзя делить по двум базам: движок по кассе, книга по ТЭП.
- L2209 [rules] Ряд в книгу заводят в пустую строку, а формула, читающая свой же итог, — круговая ссылка.
- L2227 [rules] Долг переезжает в момент раскрытия эскроу, и это следствие того, что НКЛ закрывается.
- L2250 [rules] Обнулённое обязательство исчезает бесследно, если не сказать, куда оно ушло.
- L2269 [rules] Документ собирают из той же вёрстки, а не по тем же данным.
- L2295 [rules] Вопрос задают о том, чего нельзя ни прочитать, ни посчитать.
- L2312 [rules] Единица — часть числа, и принятое без неё значение это другое число.
- L2324 [rules] Гарантийные удержания — скрытый резерв РСС, и лимит их не знает
- L2341 [rules] Дефицит бывает общий и структурный, и это разные числа.
- L2353 [rules] «Кто должен был работать» собирается из двух уже прочитанных источников.
- L2365 [rules] Умолчание ТЭП — это пример, а не пустой проект, и сброс его не восстанавливает.
- L2381 [rules] Отказ целиком там, где можно ответить наполовину, — это потеря, а не осторожность.
- L2395 [rules] Отказ считать по нормативу — не повод отказаться от пропорции.
- L2409 [rules] Заводской график — тоже сообщение, и оно про нас.
- L2418 [rules] Сокращение подрядчика — то же лицо.
- L2422 [rules] В реестрах РСС стоит генподрядчик, а на площадку выходят его субподрядчики.
- L2434 [rules] Имя подрядчика в строке сильнее заголовка над ней.
- L2446 [rules] Вход в контур открывается состоянием, а не инструментом.
- L2457 [rules] Число называют по его источнику, и границу источника называют тоже.
- L2487 [rules] Две суммы об одном остатке считают по одному контуру — или разница раскладывается, а не называется.
- L2506 [rules] Дефицит называется по вопросу, на который отвечает, а слово «структурный» на экране не живёт вовсе
- L2526 [rules] Строка таблицы складывается — или у колонок разные окна.
- L2534 [rules] Источник перераспределения — не всякий свободный лимит
- L2557 [rules] РСС — бюджет всей стройки, а не банковская доля
- L2574 [rules] Изоляция состояния покрывает всё дисковое, а не перечисленное по памяти.
- L2585 [rules] `CERTIFICATE_VERIFY_FAILED` — это вопрос «чего не хватает», а не диагноз.
- L2599 [rules] Проверка, объявленная дважды, работает один раз — и это молчание.
- L2608 [rules] Вывод, который печатается «по остаточному принципу», однажды скажет обратное посчитанному.
- L2621 [rules] Второй источник — это проверка первого, а не только вторая витрина.
- L2632 [rules] Готовая средняя источника — не выборка, и в полосу квартилей она не идёт.
- L2639 [rules] Пустое поле новой строки не встаёт на место прочитанного.
- L2655 [rules] Кто пишет файл целиком, тот теряет чужую запись.
- L2676 [rules] Мера сравнения — часть вопроса, и не та мера отвечает не на тот вопрос.
- L2693 [rules] Точка после сокращения фразу не кончает, а правило «в одном предложении» на этом ломается молча.
- L2711 [rules] Проверка, которая ищет строку в исходнике, не знает, разрешается ли имя.
- L2723 [rules] Балл, который не различает, — не балл; и «задача» обязана менять список.
- L2739 [rules] Три снижения за одно явление — это одно снижение, сложенное трижды; и шкала по процентилям каталога красит список в «Низкое» по построению.
- L2764 [rules] Ячейка вводной без формулы-читателя — не вводная; статья движка без строки в книге — потеря.
- L2783 [rules] Накопленный итог не складывают — складывают поток.
- L2799 [rules] Пустой слайд — это не «мало данных», а невыбранная вёрстка.
- L2817 [rules] Лечить пустоту потерей содержимого нельзя — лечит ширина.
- L2838 [rules] График экрана, у которого нет своей таблицы, в документ не попадает вовсе.
- L2853 [rules] Порядок таблицы и порядок оси — разные порядки.
- L2866 [rules] Молчит документ — это не «молчит источник».
- L2886 [rules] Проект решения — не решение, и метка называет документ, а не его отсутствие.
- L2900 [rules] Выбор, которого у источника нет, изображать нельзя.
- L2907 [rules] Пара осей без данных рисует пустое поле, а пустое поле читается как «про рынок сказать нечего».
- L2916 [rules] Одноимённая функция съедает предыдущую молча — и в коде тоже.
- L2923 [rules] Одна площадка в двух источниках портала записана по-разному, и точный ключ имени — не ключ.
- L2953 [rules] Реестр КРТ лежит файлом карты, а не за API.
- L2968 [rules] «Машине нечем» проверяют распознаванием, а не объявляют.
- L2980 [rules] `CERTIFICATE_VERIFY_FAILED` у mos.ru-хостов — это снова один лист.
- L2987 [rules] Проверки страницы гоняют собранную страницу, а не сырую константу.
- L2992 [rules] Статус источника отвечает на свой вопрос, а не на наш.
- L3022 [rules] Живой лот сильнее публикации: пока право на договор о КРТ на торгах, оператора у площадки нет.
- L3037 [rules] Передаваемая модель начинается с обнуления участка, а не с умолчаний.
- L3060 [rules] Территория проекта живёт не только во вводных — и подмена проекта обязана забыть её целиком.
- L3084 [rules] Фоновая работа приложения — не часть теста, и в прогоне её быть не должно.
- L3095 [rules] График — вводная, и читают его обе поверхности из одного места.
- L3112 [rules] Поле, которое молчит, чинят, а не сносят — оно и есть образец.
- L3134 [rules] Доля в юрлице — своя сделка, и мерить её метрами нельзя.
- L3164 [rules] Живая карта одна на сервис, и чужая страница берёт её, а не пишет свою.
- L3179 [rules] Норматив площади соцобъекта — ступень по ёмкости, и считается от числа мест
- L3193 [rules] График вводится ячейками, а не строкой через запятую
- L3206 [rules] Улица опознаёт квартал, а не площадку.
- L3226 [rules] Из чужого раздела читают сначала то, за чем пришли.
- L3234 [rules] Пятый элемент поля значит разное, и читать его надо по типу поля.
- L3246 [rules] Корзина находок, заведённая позже, в счётчик не попадает.
- L3263 [rules] Источники говорят языком продаж, а корзины были написаны языком канцелярии.
- L3297 [rules] Стенд страницы берёт блок по границам, а не перечисляет функции.
- L3305 [rules] Тест, читающий каталог из `.gitignore`, зелен только на своей машине.
- L3318 [rules] Правка, применённая наполовину, выглядит применённой.
- L3334 [rules] Незаписанный отказ нельзя посчитать, а непосчитанный отказ выглядит как отсутствие признака.
- L3350 [rules] Смена версии правила снимает признаки сразу у всех, и это надо сказать числом.
- L3360 [rules] У раздела не было обхода по страницам, и это оказалось настоящей причиной «одного КРТ».
- L3376 [rules] Оговорка «мы этого не читаем» устаревает молча.
- L3387 [rules] Одна жалоба — одна цепочка, и чинить надо то звено, где теряется.
- L3414 [rules] Машино-места считает постановление, и это одна формула на все поверхности
- L3441 [rules] Функция, читающая переменную страницы, обязана пережить её отсутствие.
- L3459 [rules] Необъявленная переменная страницы — не тихий undefined, а обрыв загрузки.
- L3479 [rules] У соцобъекта два числа: бюджет от МЕСТ, площадь для контроля объёма
- L3496 [rules] Средняя квартира — делитель с ОСНОВАНИЕМ, а не число
- L3513 [rules] Снижение балла меряют, а не назначают.
- L3524 [rules] Пол, написанный как `Math.max(points, N)`, не срабатывает там, где нужен.
- L3534 [rules] Подпись сравнения называет порог и своё число.
- L3544 [rules] Отказ подбора называет, чьё число не дотянуло, — иначе он спорит со сводом.
- L3568 [rules] Расшифровка пика БРИДЖа свода — по очередям с открытой линией, а не по всем.
- L3587 [rules] Линия, рассчитавшаяся в РВЭ, после него не кредитует — и это относится не только к переносу долга.
- L3606 [rules] Стартовая цена скрининга — та же, что рекомендует отчёт о рынке.
- L3619 [rules] Объект строится в своей очереди и не раньше её РнС — БРИДЖ стройку не финансирует
- L3633 [rules] БРИДЖ у очередей — свой у каждой, и блок рисуется по очередям.
- L3642 [rules] Проектирование, подготовка и сети идут по размеру очереди, ИРД — вперёд.
- L3650 [rules] Метры Программы реновации строятся, но не продаются — это цена входа
- L3676 [rules] Число на месте формулы шаблона рвёт цепочку молча, и снаружи оно неотличимо от посчитанного.
- L3694 [rules] Лимит очереди округляют по очереди, а не по сумме.
- L3702 [rules] Календарь объекта — это ячейки объекта, а не доля от общей даты.
- L3712 [rules] Ключ вводной живёт там же, где решается, куда писать значение.
- L3721 [rules] Счёт «чего в книге нет» считает то, о чём знает.
- L3729 [rules] Вводная, которую никто не читает, — не вводная, а обещание.
- L3743 [rules] Цвет — утверждение о ячейке, и врать он может так же, как подпись.
- L3756 [rules] ТЭП вбивается там, где его не найти, и это не придирка к вёрстке.
- L3771 [rules] Кнопка обновления обязана сходить в источник, а не перерисовать файл.
- L3790 [rules] Очередь платит общепроектные статьи по своему объёму, и цена метра очереди стоит рядом с вводной
- L3809 [rules] Число берут из оборота, который его называет, — а обрезка прячет непонятое.
- L3845 [rules] Отдельно стоящий объект меряется своей площадью, а наземный паркинг — местами
- L3876 [rules] В расчётный лимит БРИДЖа входит то, что платится ДО РнС
- L3892 [rules] Предел длины вопроса Платону — на сценарий, а не на весь маршрут.
- L3905 [rules] Обзорная карта рисовала только то, что город положил в свой файл.
- L3924 [rules] Сводная линия эскроу — сумма чужих счетов, и на вопрос про очередь она не отвечает.
- L3946 [rules] Один драйвер на два сценария — это второй сценарий, выданный за первый.
- L3972 [rules] Число квартир двигает машино-места — пунктом 2 по средней квартире.
- L3987 [rules] Цифры площадки лежат в документе, а не только в карточке.
- L4011 [rules] Вид величины объявляют там же, где её пишут.
- L4020 [rules] Пустой выбор фильтра неотличим от сломанного, пока рядом нет числа.
- L4027 [rules] Оси фильтра различают по вопросу, на который отвечают, а не по полю данных.
- L4047 [rules] Источник засчитывается в «спросили» только там, где он мог бы ответить.
- L4060 [rules] Спор двух источников решается только там, где обе стороны переживают перезагрузку.
- L4087 [rules] `computed_at` отвечает «когда», а не «чем».
- L4115 [rules] Порядок, изменившийся молча, читается как несработавшая сортировка.
- L4122 [rules] Съехавший разбор прячут целиком, а не только в ячейке.
- L4132 [rules] Прогон ходит по тому списку, который видит человек, а не по одной его половине
- L4165 [rules] Рассрочка ВРИ и резерв: методику снова меняли в одном месте из двух.
- L4188 [rules] Пятисотка отвечает текстом, а страница разбирает её как JSON.
- L4206 [rules] Отказ ресурса — не смерть страницы, а ловушка их не различала.
- L4222 [rules] Отдают метры и места, а не рубли
- L4243 [rules] Гостевые места приходят числом только из выгрузки, остальным их выводят обратным ходом.
- L4273 [rules] Витрина продаж называет застройщика СВОЕГО дома, и номер у дома и у владения один и тот же.
- L4306 [rules] Балл меряется тем объёмом, который у площадки ЕСТЬ, и мера называется.
- L4326 [rules] Объект уходит целиком одним путём, и признак у него один
- L4347 [rules] Проверка, не знающая о новой строке, кричит на верном расчёте.
- L4359 [rules] Имя функции у юрисдикций своё, и одно на обе таблицы найдёт не ту строку.
- L4370 [rules] «Не тронули» читается как «убрали».
- L4385 [rules] Надпись у поля — утверждение о ПОЛЕ, а не о методике.
- L4407 [rules] Подпись под графиком называет то, что на графике видно.
- L4425 [rules] Один рисовальщик на три контейнера — три копии на экране, и строковый тест их не видит.
- L4442 [rules] Лист, который собрали мы, обязан выглядеть книгой владельца.
- L4481 [rules] Правило списка применяют там, где список опознан.
- L4504 [rules] 404 от сервиса — «сервис не отдал», а не «объекта нет».
- L4535 [rules] Ось без единицы читается как другая шкала.
- L4543 [rules] Строка, вставленная между `if` и `else`, уводит себе чужой `else`.
- L4555 [rules] «Жив ли прогон» спрашивают у списка процессов, а не поиском по своей же строке.
- L4572 [rules] Точка входа модуля стоит в конце файла.
- L4578 [rules] Цены сделки в отчёте «Пульса» нет, и это измеряют, а не предполагают.
- L4597 [rules] Тип объекта решает, что попадает в цену и в комнатность.
- L4604 [rules] «Продано застройщиком» — не «продано».
- L4611 [rules] Имя поля бывает занято другим источником.
- L4617 [rules] Месяц выпуска в проверке числом не закрепляют.
- L4621 [rules] Прогон идёт по тому списку, который видит человек.
- L4631 [rules] У площадки-решения субъект есть — это её адрес, а обязательства лежат в её же PDF.
- L4655 [rules] Голая цифра адресом не является — а чужой контур подписан «официальный полигон».
- L4673 [rules] Число становится номером владения только в адресной позиции — и это же правило решает, откуда берётся балл.
- L4709 [rules] Колонка сортируется тем словом, которое в ней написано.
- L4720 [rules] Балл подписан тем, кто его считает, а не тем, чьё имя красивее.
- L4729 [rules] Одно и то же не говорят дважды подряд.
- L4742 [rules] Половина списка приезжает из другого источника, и карточка обязана называть свой.
- L4762 [rules] Догадку о причине проверяют на своём проекте, а не правилом.
- L4780 [rules] Раздел без картинки и без вывода — это две таблицы, а не раздел.
- L4790 [rules] Карточка была короткой, а колонка под ней — 118 500 пикселей.
- L4803 [rules] Порядок карточки — порядок решения, и у каждого шага есть имя на экране.
- L4814 [rules] «Проверка данных» говорит своё место сама.
- L4824 [rules] Длинный список сворачивается, короткий стоит открытым, и порог назван один раз.
- L4838 [rules] Неразобранная строка до модели не доходит, а «не из набора» сверяют без регистра.
- L4874 [rules] Модель считает и по объявленной цене торгов, а не только при нуле.
- L4894 [rules] Один документ в двух разделах портала — одна строка, и убранную называют числом.
- L4916 [rules] Строгое правило совпадения отказывало на ТОЖДЕСТВЕ.
- L4933 [rules] Вывод пишут словами вопроса, а не именами величин.
- L4953 [rules] Продаётся то, что можно поставить на кадастр
- L5000 [rules] Гараж объекта — величина проекта, и по очередям она делится или удваивается.
- L5026 [rules] Верхний край — это ответ, но ответ НАШ, и двигает он не только число.
- L5052 [rules] Своя правка, названная ошибкой источника, читается как находка в нём.
- L5071 [rules] Нормативное собрано вместе, кнопки после него, модель и Платон — в конце
- L5085 [rules] Пересчёт ТЭП по плотности книга умеет, и владеет им она — не трогать.
- L5106 [rules] Ревизия формул считает четыре болезни, а сторож был на одну.
- L5128 [rules] Легенда обещает ровно те полосы, которые нарисованы.
- L5144 [rules] Раздел, которого нет в общем выводе, отвечает только на свой вопрос.
- L5165 [rules] Доля за месяц и доля за год — разные величины, и окно называется у самой полосы.
- L5204 [rules] Ответ, за которым надо догадаться нажать, — ответ, которого нет.
- L5220 [rules] Кнопка стоит там, куда смотрят, а не там, где кончается блок.
- L5248 [rules] Двенадцать строк одним списком — это не таблица, а список; и одно имя на продукт бывает только тогда, когда оно объявлено один раз.
- L5278 [rules] Прежде чем рисовать величину, считают, есть ли в ней сигнал.
- L5296 [rules] Победитель, выбранный порядком множества, — два ответа об одном проекте.
- L5305 [rules] Итог книги считает её же формула, и текстовая ячейка в неё не входит.
- L5317 [rules] Выгрузку называют так, как отвечает источник, а не так, как её прислали.
- L5329 [rules] Землю под зданием спрашивают точкой, а не полем.
- L5340 [rules] Правообладатель земли и правообладатель строения — разные лица, и колонка у них разная.
- L5351 [rules] Слой, который крупнее, лежит ниже — иначе верхнего не существует.
- L5359 [rules] Служебная страница — та, на которую ниоткуда не ведёт ссылка, а числа за ней спрашивают владельца
- L5368 [rules] Кусок карты, вынутый из `PAGE`, тянет за собой не только карту.
- L5378 [rules] Фигура на карте опознаётся своим ключом, а не местом в списке.
- L5387 [rules] Площадку в реестре опознают геометрией, а не именем.
- L5397 [rules] Площадь участка и площадь, ВХОДЯЩАЯ в площадку, — разные величины, и справочная колонка обязана назвать свою.
- L5413 [rules] Номер, связывающий карту с таблицей, считает сервер.
- L5422 [rules] «Один экран» — это порядок и отбор, а не одно место.
- L5437 [rules] Стоимость земли и стоимость строений в одну колонку не складываются.
- L5452 [rules] Картинку без координат на карту не накладывают.
- L5466 [rules] Колонку книги читают по заголовку, а не по номеру — и проверки тоже.
- L5476 [rules] Подпись ставят в самое широкое место фигуры, а не в центр её рамки.
- L5490 [rules] `max-height` с `overflow:hidden` — это обрезка молча.
- L5498 [rules] Счётчик по одному источнику молчит о втором.
- L5506 [rules] Утверждение о втором источнике, поднятое к плиткам первого, читается как сомнение в первом.
- L5514 [rules] Итог по ЕГРН обязан стоять рядом с площадкой, которую он не равен.
- L5528 [rules] «Участка нет» и «участок есть, права не зарегистрированы» — разные состояния, и под похожими словами они сливаются.
- L5542 [rules] Бледнеть надо по величине, а не по признаку.
- L5551 [rules] Подпись вправе слегка выступать за мелкую фигуру.
- L5561 [rules] «Уверен» — плохой ответ там, где есть чем померить.
- L5570 [rules] Кадр строят по предмету, а не по всему нарисованному.
- L5579 [rules] Высоту кадру задаёт пропорция, а не картинка подложки.
- L5585 [rules] Ответ — числом в плитке, объяснение — один раз.
- L5596 [rules] Вырезая соседа, проверь, кого утащил.
- L5603 [rules] Ссылка на служебную страницу — тот же механизм, что «Поделиться» проектом
- L5616 [rules] Код доступа живёт своим файлом, а не в чужом.
- L5621 [rules] Ключей у сервиса два, и отказ обязан называть, какого не хватило.
- L5635 [rules] Пустой параметр в `bool` — это 422, а не «нет».
- L5648 [rules] Долю налога считают той мерой, из которой налог возник, — а мера эта год.
- L5680 [rules] Убыток переносится бессрочно, ограничение — в доле
- L5696 [rules] Состав пишет тот, кто видит его целиком, — иначе список сам себе делает новости.
- L5730 [rules] Величина, которую источник назвал ДРУГИМ полем, — это данные, а не их отсутствие.
- L5758 [rules] Кнопка, которая не может сработать, гаснет до нажатия, а отказ печатается там, где нажали.
- L5774 [rules] Красный main меряют полным прогоном по нему, а не тем файлом, который упал первым.
- L5796 [rules] Браузерные проверки на CI не идут вовсе, и «зелёный CI» о них не говорит ничего.
- L5819 [rules] Русскую дату разбирает сервер, браузер её читает не тем порядком.
- L5854 [rules] Лист трогают строкой, а не ячейкой, — и это правило уже было записано один раз.
- L5879 [rules] Сторож считает проходы, а подорожал сам проход.
- L5903 [rules] Ширина сетки объявлена один раз, а копии её живут внутри формул.
- L5921 [rules] ННП — площадь в габаритах ВНУТРЕННИХ стен, то есть общая, а не ГНС
- L5938 [rules] Сверка «ничего лишнего не изменилось» не отвечает на «всё ли нужное изменилось».
- L5950 [rules] Подсказка у поля и подпись под ним — одно место, а не два.
- L5966 [rules] Функцию, написанную объяснять молчание, надо ещё и позвать.
- L5989 [rules] Производную считают там, где её читают, — а хранят её в четырёх местах и по-разному.
- L6018 [rules] Денежная компенсация — не объект, и метров его в проекте нет
- L6028 [rules] Заглушка с придуманной сигнатурой проверяет не наш код, а себя.
- L6041 [rules] Гейт, которому передали не то, отвечает «нет» всем и всегда — и выглядит строгостью.
- L6058 [rules] У молчащего сторожа обязан быть счётчик молчания — второй раз, теперь у нормативов.
- L6075 [rules] Кнопка отвечает на тот вопрос, который в её подписи.
- L6086 [rules] Час без зоны — другой час, и в Москве это не видно.
- L6106 [rules] Расход признают в месяце, когда он возник, а не когда за него заплатили.
- L6133 [rules] Ширина сетки книги объявлялась трижды, и обрезка молчала.
- L6172 [rules] Экранирование чужого XML не раскодируют и не кодируют обратно.
- L6181 [rules] Ответ одного экрана собирают по одному чтению источника.
- L6224 [rules] Умолчание строки ТЭП — не литерал, а тот же счёт, что у всех.
- L6241 [rules] Делитель совпал с числителем случайно — и держался на чужом нуле.
- L6259 [rules] Строка, которую чинит только страница, чинится не везде.
- L6293 [rules] Площадь документа — наземная, и колонка у неё своя.
- L6313 [rules] Проверка, написанная под прежний ответ, закрепляет его.
- L6323 [rules] Стенд разрешает зависимости страницы сам, а не перечисляет их.
- L6362 [rules] Состояние стенда задают таким, каким его оставляет страница.
- L6376 [rules] Правило, закрытое на одной вкладке, соседнюю не защищает
- L6400 [rules] «Изменилось» без единого своего слова — это «скачали не тот документ».
- L6418 [rules] Маршрут, объявленный выше своей модели, читает тело как строку запроса.
- L6448 [rules] Отметка времени, напечатанная как есть, — не дата.
- L6485 [rules] Прогон делят, когда времени нет у отдельных тестов, — а делят по файлам и своими раннерами
- L6539 [rules] Деление прогона нашло тест, который зависел от соседа.
- L6566 [rules] Число без окна и без предмета сравнивают с чем попало.
- L6591 [rules] Модель не сходилась сама с собой, и нашла это приёмка чужой правки.
- L6613 [rules] Снимок «ни одно число не сдвинулось» слеп там, где числа нули.
- L6634 [rules] Книга пишет ТЭП, присланный страницей, а отчёт — тот, что движок посчитал.
- L6663 [rules] Пара «места и площадь гаража» читалась страницей и движком по-разному.
- L6686 [rules] Вынужденная копия живёт со сторожем, иначе отстаёт молча — и отстала.
- L6707 [rules] Проверка порядка скриптов мерила порядок объявлений — и держала сборку main красной двое суток.
- L6733 [rules] Ревизия чужой правки начинается с того, что мерят СВОЮ меру.
- L6743 [rules] Одно имя, два смысла — и обе поверхности выглядят верными.
- L6780 [rules] Приписка о переданном стоит у обеих мер, а не у той, до которой дошли.
- L6794 [rules] У максимума номеров бывает несколько держателей, и названный один прячет столкновение.
- L6805 [rules] Благоустраивают двор, а не дом — но два числа нормы с бюджетом не сошлись.
- L6852 [rules] Норматив — в профиль класса, и вместе с ним в таблицу приезжает единица.
- L6888 [rules] Доля стоит под тем числом, которое из неё получается, — и число сменило колонку.
- L6925 [rules] Облагается всё, кроме квартир, и список продуктов этого не решает.
- L6941 [rules] Производная величина не может быть точнее своих слагаемых.
- L6951 [rules] Строку таблицы книги читают по подписи, а не по номеру — и проверки тоже.
- L6957 [rules] Фикстура, подобранная под порог, устаревает вместе с методикой.
- L6964 [rules] Страницу проверяют по тому адресу, по которому её открывает человек, — а не файлом с диска.
- L6988 [rules] Буква у `-r` заменяет умолчание, а не добавляется к нему.
- L6997 [rules] Проценты по рассрочке ВРИ вычета НДС не дают, и строка паритета нашла это первым же прогоном.
- L7014 [rules] Путь к браузеру спрашивают у сервиса, а не помнят числом.
- L7022 [rules] Проверка «строка входит в вердикт» держала конец диапазона литералом.
- L7028 [rules] Диапазон, написанный краями, читается вычитанием — а имя периода нет.
- L7047 [rules] Хост, который не может сделать работу, не должен её начинать.
- L7068 [rules] Величина, за которой источник спрошен не о том, — не молчание источника.
- L7090 [rules] Апартаменты и квартиры лежат в одном классе, а покупатель у них разный.
- L7112 [rules] Витрину двигает не только прайс: у соседа есть рассрочка.
- L7168 [rules] Предложение по акции живёт на пересечении двух сводок, и до сих пор их было две разных.
- L7207 [rules] Этажа в источнике нет, а комнатность есть и не читалась.
- L7240 [rules] Штуки и метры складываются только внутри своего товара — рубли складываются всегда.
- L7273 [rules] «Формат не поддержан» бывает нашим пробелом, а не чужим форматом.
- L7288 [rules] DOCX — тоже зип, а подпись площадки врёт в обе стороны.
- L7297 [rules] У архива три списка, а не один: прочитанное, спутники и непрочитанное.
- L7307 [rules] Проверку на чужой флаг нельзя собрать чужим писателем.
- L7315 [rules] Число за всё время не отвечает на «как сейчас», а колонка без существительного — своя мера.
- L7336 [rules] Мера товара — часть товара, и нулевая площадь проданного лота это пропуск.
- L7360 [rules] Имя меры и единица при числе — разные формы.
- L7372 [rules] У величины есть мера — значит сравнение с ней стоит рядом, в обе стороны.
- L7396 [rules] Подпись у пары полей, стоящая ВЫШЕ ввода, приклеивается к соседней строке.
- L7409 [rules] Поток и остаток складываются по-разному, и очередь после своего горизонта не исчезает.
- L7430 [rules] Остановку прогона проверяют списком процессов, а не кодом возврата.
- L7439 [rules] Получателя переданных метров модель не знает — значит не называет.
- L7464 [rules] Инфляция объекта — от старта его очереди (решение владельца, 13.09.2026).
- L7481 [rules] Широкое содержимое держит СВОЁ окно прокрутки, а на бумаге его размыкают.
- L7507 [rules] Ссылку на источник проверяют содержимым, а не кодом ответа.
- L7520 [rules] Проект, ставший законом, — оговорка устаревает в опасную сторону.
- L7538 [rules] Отказ, записанный в общий список, до свода не доезжает — и свод отвечает «не спрашивали».
- L7551 [rules] Спутник ПРИ содержимом и спутник ВМЕСТО содержимого — разные ответы.
- L7562 [rules] Пустой ответ площадки и «документов нет» на экране одно и то же.
- L7569 [rules] Образец, выбрасывающий «служебное», выбросил и то, ради чего написан.
- L7589 [rules] Зарегистрированное право без имени — не отсутствие права.
- L7604 [rules] `async` стоит перед словом `function`, и стенд терял это слово.
- L7610 [rules] Один запрос на вложение выдаёт перебой площадки за отсутствие документа.
- L7634 [rules] Байты вложения разбирают несколько читателей — значит файл лежит, а не выбрасывается.
- L7666 [rules] У склада на диске есть строка в `.gitignore` — и спрашивают об этом git.
- L7677 [rules] Данные объявляют выше своих читателей, а не дозаписывают после.
- L7696 [rules] `const`, прочитанный выше своего объявления, роняет ВЕСЬ скрипт страницы — и молча.
- L7714 [rules] Стенд, несущий свою копию состава, — вторая жизнь у списка, объявленного один раз.
- L7731 [rules] Выбор значения, которое и так стоит умолчанием, ничего не различает.
- L7738 [rules] «Exit code 0» фоновой задачи — код последней команды цепочки, а не прогона.
- L7747 [rules] Проверка, не знающая о новой величине, кричит FAIL на верном расчёте — и сторож паритета этого не видит.
- L7766 [rules] Рукописная копия чужого листа отстаёт в тот же день, когда код учится новой формуле.
- L7776 [rules] Фикстура, у которой вводные и ТЭП говорят об объекте разное, — не проект.
- L7786 [rules] Клик по узлу, которым владеет другой слой, — гонка, и в одиночку её выигрывают.
- L7796 [rules] Читатель, режущий строку, делает сторожа слепым за срезом — и зрячим там, где ничего не сломано.
- L7813 [rules] У продукта, которого книга не знает, есть в ней ПУСТАЯ строка — и итог над ней его складывает.
- L7845 [rules] Проверка, зелёная по совпадению последнего разряда, не доказывает ничего.
- L7866 [rules] Фикстура, кладущая величину прямо в строку ТЭП, устаревает в тот день, когда строку начинают ВЫВОДИТЬ.
- L7882 [rules] «Падает и на main» — это замер, а не догадка, и мерить надо ВЕСЬ список.
- L7894 [rules] У записанного отказа спрашивают, не удался ли запасной путь.
- L7925 [rules] Свод территории собирает один сборщик, а площадки меняют ему источники.
- L7966 [rules] Свод из одного источника, поделённый на коэффициент, — не измерение того класса, на который его поделили.
- L7997 [rules] Проверка, стоящая на позиции, падает от дописанной рядом строки.
- L8016 [rules] Поле, сменившее место правки, объявлено там же, где было, — переезжает только отрисовка.
- L8041 [rules] У всего, что приходит без спроса, обязан быть выключатель — и он один на все поводы.
- L8068 [rules] Гейт стоит у той двери, в которую ходят, — а Telegram ходит в обёртку.
- L8101 [rules] Гейт читал не ту величину: карта отказывалась рисовать, имея 55 контуров.
- L8125 [rules] Счётчик считает то, что читают, — и это третья копия одной болезни.
- L8147 [rules] Работа, которую делает только рука, на экране выглядит ответом источника.
- L8181 [rules] Гейт по виду вложения, которого площадка не ставит, не срабатывает ни разу.
- L8199 [rules] Ячейки строки листа идут по возрастанию колонки — иначе Excel их теряет молча.
- L8229 [rules] Проверка, отобранная по имени строки, слепа ко всему остальному листу.
- L8249 [rules] Восстановить вводные из отчёта можно, но проверяется это прогоном, а не перепиской чисел.
- L8267 [rules] Перечисление объектов руками теряет объект, заведённый позже — второй раз за десять дней.
- L8294 [rules] Подземное в колонке «ГНС» — и три соседние потери в одном блоке.
- L8317 [rules] Самозакрывающаяся ячейка съедала следующую, и книга выглядела пустой.
- L8330 [rules] Детализация соцнагрузки в книге была числами, и это меряется, а не обсуждается.
- L8355 [rules] Цель паритета замерла на дате сборки, и правка вводной красит книгу зря.
- L8393 [rules] Форм у источника больше, чем читает разбор, — и молчание об этом наше.
- L8417 [rules] Печатная форма форме не равна.
- L8425 [rules] Повтор объявляют один раз, и площадка, отвечающая через раз, требует его у КАЖДОЙ двери.
- L8440 [rules] Ось фильтра называет вопрос, а не слово источника.
- L8460 [rules] Пустого раскрытия не бывает.
- L8473 [rules] Срок, взятый у шлюза, не годится сторожу.
- L8484 [rules] Разрешитель зависимостей стенда знает, чья это страница.
- L8495 [rules] Разобранное лежит на диске с версией правил, которыми прочитано, — иначе починка читателя не доезжает до прочитанного.
- L8526 [rules] Гейт стоит по действию документа, а не по его подписи.
- L8556 [rules] `a||b ? x : y` — это `(a||b) ? x : y`, и первая половина условия в ответ не попадает.
- L8570 [rules] Негодное тело запроса — это отказ с причиной, а не пятисотка.
- L8607 [rules] Правило, закрытое у одного поля, соседнее не защищает — теперь у номера распоряжения.
- L8626 [rules] Разрешитель стенда слеп к ошибке, которую чужой `try/catch` проглотил.
- L8654 [rules] Сторож запирал свой же гейт: обход просился строкой ВЫШЕ проверки полноты.
- L8679 [rules] Различающий ответ, до поверхности не доехавший, — это ответ, которого нет.
- L8706 [rules] Номер берут целиком, а границу ему задаёт форма, а не длина.
- L8738 [rules] Счётчик устаревшего разбора считал записи, а у склада без записей отставать нечему.
- L8756 [rules] Немой отказ чинить нечем: он обязан назвать, что видел.
- L8766 [rules] Показатель, который предлагают вписать, считается на базе того поля, куда его вписывают.
- L8781 [rules] «Имени нет» у node две формы, и стенд узнавал одну.
- L8790 [rules] Явный признак сильнее совпадения с числом.
- L8802 [rules] Свод рядом со ставкой обязан назвать СВОЮ базу, а подсказка — не ответ.
- L8817 [rules] Предохранитель, меряющий одну координату, падает на законной сетке.
- L8838 [rules] Допуск проверки не бывает уже ступеньки самой величины.
- L8857 [rules] Подпись, идущая за выбранным видом, отвечает не о том поле, под которым стоит — а если она ещё и пишет, то пишет чужое число.
- L8886 [rules] Стенд, гоняющий node сам, не знает о функции, заведённой вчера.
- L8896 [rules] Форма, которую забирают целиком, превращает показанное в введённое.
- L8924 [rules] Ожидание в браузерной проверке не смотрит на то, что проверяется.
- L8933 [rules] Обрезка по длине стоит между диагнозом и читателем.
- L8957 [rules] Счёт «чего в книге нет» мерит то, чем меришь, — и трижды соврал в разные стороны.
- L8985 [rules] Сколько у источника есть — спрашивают у источника, а недособранный обход дополняет, а не заменяет.
- L9023 [rules] Свой предел — тоже правило читателя, и меняя его, поднимают версию.
- L9042 [rules] Мера полноты сравнивает однородное, а молча выброшенное делает объявленное число недостижимым.
- L9076 [rules] Зависание, которого нет в песочнице, мерят там, где оно есть — а для этого молчание обязано заговорить.
- L9100 [rules] Раннер ждёт закрытия ТРУБЫ шага, а не завершения дерева процессов.
- L9123 [rules] Границу числу задаёт форма, а не длина клетки — и склеенное рядом не делает число непрочитанным.
- L9156 [rules] Проверка подстрокой не отличает утверждение от его отрицания.
- L9168 [rules] Правовую посылку проверяют источником, а не памятью.
- L9187 [rules] Режим — это вводные, а не вторая методика счёта.
- L9216 [rules] Две поверхности, ошибающиеся одинаково, паритетом не ловятся — и это был не недосмотр, а копия.
- L9243 [rules] Величина, живущая в `finance`, на поверхности не существует.
- L9262 [rules] Одобренный лимит был потолком в движке и справкой в книге.
- L9282 [rules] Поле, которое просят заполнить, обязано лежать на листе ввода, и решает это СТИЛЬ.
- L9294 [rules] Формула, идущая в `_v4_set_cells`, — сырая: экранирует она сама.
- L9302 [rules] `git checkout -- файл` откатывает файл ЦЕЛИКОМ, включая несохранённую работу.
- L9308 [rules] Общий лимит ПФ у очередей — ожидание, потолок очереди — только её НКЛ
- L9346 [rules] Поле, которое пишут рядом со строкой и не читает никто, — это молчание, а не запись.
- L9394 [rules] Показатель, который обещают вернуть, считается независимо от того, что стоит в поле.
- L9425 [rules] Статья, посчитанная нулём, исчезает из расходов целиком — и её отсутствие читается как ответ методики.
- L9450 [rules] Дешёвый гейт перед дорогой дверью — копия условия по построению, и она обязана быть вызовом.
- L9468 [rules] Пустая сторона пары с непустой не спорит — и потому признак читают отовсюду, а не оттуда, где его писали в первый раз.
- L9497 [rules] Дашборд книги и тизер — одна модель представления, и она не считает
- L9520 [rules] У источника бывает две поверхности, и вторую надо СПРОСИТЬ, а не считать копией первой.
- L9551 [rules] Сторож с литералом падает на том, ради чего написан, и молчит о самой величине.
- L9571 [rules] Кнопка и подпись под ней отвечают на один вопрос — значит считает их одно правило.
- L9584 [rules] «Последний прогон на ветке» — не тот прогон, которого ждёшь.
- L9594 [rules] Поле, заведённое без подъёма версии правил, доезжает только до новых строк.
- L9614 [rules] Номер выпуска не опознаёт образ: сторож версии на слиянии не перечитывают.
- L9634 [rules] Замок, который отпускает `finally`, переживает смерть процесса — и полчаса выдаёт труп за живого.
- L9663 [rules] Вердикт проверки, прочитанный вскоре после её старта, — не вердикт, а её незаполненное состояние.
- L9671 [rules] «Доехал ли PR в main» сверяют по содержимому, а не по номеру в сообщении коммита.
- L9679 [rules] Диверсию снимают копией файла, а не `git checkout` — и я это проверил на себе.
- L9689 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9731 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9732 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9734 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9738 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9780 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9781 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9783 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9787 [checks] `bash scripts/run_tests.sh` — весь набор одной командой, **6231 тест**
- L9829 [checks] Тесты страницы гоняют настоящий код из `PAGE` через `node`, а не его пересказ.
- L9830 [checks] `audit_plato_workbook` сверяет посчитанную книгу ПЛАТО с движком. Книга должна
- L9832 [checks] LibreOffice в песочнице сломан — пересчитать шаблон самостоятельно нельзя.
- L9834 [checks] Две величины на одной шкале — это одна из них, сплющенная.
- L9846 [checks] Легенда — часть графика, и объявляется там же, где он один.
- L9855 [checks] Своё окно прокрутки помнит, докуда его дочитали.
- L9871 [checks] Шапка отвечает на тот же вопрос, что метки строки, — и тогда она обязана назвать их цену.
- L9885 [checks] Паспорт площадки — её собственные числа, и он опознаёт публикацию не хуже адреса
- L9906 [checks] Улица опознаёт квартал — и «Строгино 360» это подтвердило, а не опровергло.
- L9929 [checks] Гейт читает поле, а вторая сборка строки его не кладёт — и отказ винит источник.
- L9955 [checks] Связку считают по тому списку, который видит человек.
- L9975 [checks] У молчащего сторожа обязан быть счётчик молчания — и у сторожа новостей его не было.
- L10004 [checks] Итог прогона читают в ЕГО файле, а не в том, что подвернулось.
- L10019 [checks] «Работа принята» и «пустого ответа» с виду не отличить, и поверхность, которая ждать не умеет, показывает принятое как отказ.
- L10035 [checks] Одно имя — один автор: «Короткий вывод Платона» считал не Платон.
- L10048 [checks] Порядок карточки КРТ: сначала выводы, потом данные города, потом детали
- L10063 [checks] Два отрисовщика на одни данные — две разные карточки об одной площадке.
- L10071 [checks] `100vh` на телефоне — высота вместе с полосой браузера, а не то, что видно.
- L10092 [checks] Репозиторий публичный — решение владельца (07.09.2026), и потому чужое в него не кладут.
- L10107 [checks] Срок строительства при очередности задаёт очередь, и поле проекта обязано это сказать
- L10140 [checks] Погасшая кнопка без слов читается как несработавшее нажатие.
- L10162 [checks] Пример поломки, взятый у умолчания, умирает вместе с его починкой.
- L10169 [checks] Встречную правку одного места находят по одному и тому же падению.
- L10186 [checks] Правка строки заполняет свою строку, а доля целого связывает соседние.
- L10219 [checks] Число из документа и формула документа — разные вещи, и заморозка выхода ломает вход.
- L10243 [checks] Правило, закрытое у одного читателя, соседнего не защищает — и «живой лот» это доказал во второй раз.
- L10257 [checks] Маркер конфликта — рабочий код до тех пор, пока его никто не ищет.
- L10271 [checks] Первое вхождение строки — не то место, о котором думает проверка, и на этом лёг main.
- L10299 [checks] Свод превращает снятые доли в «failure», и прежний признак на нём не работает.
- L10314 [checks] Смягчение, которое даёт город, — справка у числа, а не множитель в расчёте
- L10334 [checks] Поле, заполненное нормой, обязано нести её пометку — иначе оно замирает и выглядит решением человека.
- L10369 [checks] Глобальное поле, сильное сильнее методики, умолчанием не ставят — его ставит РЕЖИМ.
