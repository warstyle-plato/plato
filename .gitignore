__pycache__/
*.pyc
.pytest_cache/
.env
.env.local
run.err
data/platon_state/
# Реестр Telegram-пользователей — рантайм-состояние: заводится при первом
# запуске и живёт в томе data/, как и состояние Платона выше.
data/telegram_users.sqlite3
data/usage/
# Анкеты и реестр пользователей живут в томе data/, как проекты и знакомства:
# журнал подметается и умирает вместе с контейнером, а они должны пережить всё.
data/surveys/
data/users/
# Счётчики ГлавАПУ: состояние процесса, а не код.
data/glavapu-health.json
# Коды входа через бота: одноразовые, живут в томе data/, как и проекты.
data/web_login/
# Рейтинг КРТ, снимок каталога и отчёты площадок: всё это пишет сам прогон и
# живёт в томе data/, как проекты и состояние Платона выше. Три файла с
# тестовым слагом уехали в репозиторий по недосмотру (83c4139) и с тех пор
# грязнили дерево на каждом запуске тестов: набор пишет в НАСТОЯЩИЙ каталог
# данных. Пока там один тестовый слаг — безобидно, но однажды прогон затрёт
# живое.
data/market/krt/
