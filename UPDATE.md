# DevelopAid v0.12.47 — плата за изменение ВРИ стала графиком, а не разовым расходом

## Что изменено в v0.12.47

Плата за смену ВРИ раньше падала одной суммой на дату РнС. Это удобная, но неверная заглушка: обязательство возникает по соглашению, может гаситься рассрочкой с процентами на остаток и делиться между БРИДЖем, ПФ и собственным капиталом. От этого зависят размер бриджа, собственный капитал до ПФ, даты возникновения расходов, стоимость рассрочки, проценты, потребность в лимите ПФ и, следом, NPV, IRR и LLCR.

Теперь ВРИ — отдельный блок модели с собственным графиком.

### Вводные

Появилась группа «Смена ВРИ и земельные права»:

| Параметр | Значение |
| --- | --- |
| `vri_required` | требуется ли изменение ВРИ |
| `vri_region` | Москва / Московская область |
| `land_right` | собственность / аренда |
| `vri_obligation_date` | дата возникновения обязательства (пусто — РнС) |
| `vri_payment_mode` | единовременно / рассрочка |
| `vri_installment_years`, `vri_periodicity_months` | срок и периодичность |
| `vri_schedule_mode` | автоматический / ручной график |
| `vri_interest_enabled`, `vri_interest_spread_pp` | проценты на остаток и спред |
| `vri_early_repay_after_pf` | досрочное погашение остатка после открытия ПФ |
| `vri_pf_open_date` | дата открытия ПФ (пусто — РнС) |
| `vri_in_bank_budget` | включена ли ВРИ в банковский бюджет |
| `vri_financing_mode`, `vri_share_bridge_pct`, `vri_share_pf_pct`, `vri_share_equity_pct` | источники оплаты |
| `vri_security_cost_mln` | расходы на обеспечение обязательства |

Ручной график задаётся списком `vri_manual_schedule` — датами и суммами основного долга; расхождение с суммой обязательства попадает в предупреждения, а не втихую меняет расчёт.

### Москва

Рассрочка на 1, 3 или 6 лет, квартальные платежи, проценты на остаток по ключевой ставке плюс 3 п.п. Введённый срок вне трёх допустимых приводится к ближайшему. Остаток можно погасить досрочно после открытия ПФ.

### Московская область

Рассрочка зависит от суммы обязательства, периодичность своя, проценты автоматически не начисляются — только если это прямо предусмотрено соглашением; при включённых процентах модель об этом предупреждает. Официальные диапазоны сумм в код не зашиты: их задаёт `vri_mo_ranges` списком `{"limit_mln": …, "years": …, "periodicity_months": …}`, последняя строка без `limit_mln` работает как «свыше». Пока список пуст, применяется введённый срок и в отчёт уходит предупреждение — выдумывать чужой норматив модель не будет.

### Логика финансирования

- платежи до открытия ПФ — БРИДЖ или собственный капитал;
- платежи после открытия ПФ — ПФ, но только если ВРИ включена в банковский бюджет;
- если банк ВРИ не финансирует, платёж остаётся на капитале и **исключается из базы долгового финансирования**: он не создаёт ни выборки БРИДЖа, ни выборки ПФ;
- при заданных долях доля ПФ до его открытия принимается БРИДЖем, а без банковского бюджета — капиталом;
- проценты по рассрочке считаются отдельной статьёй `vri_interest` и не смешиваются с процентами по кредитам.

### Очерёдность

ВРИ — обязательство всего проекта, а не отдельной очереди. Даты платежей у всех очередей одни и те же, различается только доля: шестилетняя московская рассрочка идёт по общему календарю и не растягивается на срок проекта вслед за стартом каждой очереди. Ранние очереди платят из ПФ, поздние ту же дату несут БРИДЖем, потому что их ПФ ещё не открыт. Платёж, приходящийся на период до старта очереди, относится на её первый месяц — с явным предупреждением.

Сводный блок собирается в `vri` бандла очерёдности: одна сумма обязательства, общий график и разбивка каждого платежа по очередям.

### Что видно в отчётах

Карточка «Плата за изменение ВРИ» в отчёте: сумма обязательства, основной долг, проценты по рассрочке, расходы на обеспечение, выплаты до и после открытия ПФ, суммы в БРИДЖе, ПФ и капитале, график с остатком на каждую дату и источником платежа.

В выгрузке модели появился лист **«ВРИ»** — итоги, условия и график с формулами; он есть и в файле каждой очереди, и в книге-консолидаторе. В структуру расходов добавлена строка «Проценты по рассрочке ВРИ».

### Шаблон ПЛАТО не трогаем

Шаблон принимает плату за ВРИ одной суммой на листе «Расчет ВРИ (ТЭП)» и рассрочку выразить не может. Менять сам шаблон не стали: в архив выгрузки добавлена отдельная книга **«ВРИ_график_….xlsx»** с итогами, условиями и графиком платежей. Шаблон остаётся ровно тем файлом, что и был, а детализация едет рядом. Если изменения ВРИ не требуется, книга в архив не кладётся.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.47`.

# DevelopAid v0.12.46 — социальная нагрузка раскладывается по очередям сама

## Что изменено в v0.12.46

Раньше при включённой очерёдности социальные объекты расставлялись заглушкой: ДОУ — в первую очередь, школа и поликлиника — во вторую, независимо от размера очередей. Первая очередь и так несёт сети, подготовительный период и самый длинный срок, поэтому заглушка систематически перегружала самое тяжёлое место проекта.

Теперь работает **поздняя раскладка**: объекты уезжают вправо настолько, насколько позволяют ограничения.

| Очередей | ДОУ | Поликлиника | СОШ |
| --- | --- | --- | --- |
| 2 | 2 | 2 | 2 |
| 3 | 2 | 2 | 3 |
| 4 | 2 | 3 | 4 |
| 5 | 2 | 4 | 5 |

- **первая очередь по умолчанию без социальных объектов**;
- школа всегда в последней очереди — она самая дорогая и самая долгая;
- ДОУ во второй: дошкольники появляются с первым заселением;
- поликлиника ближе к концу, но именно ей первая очередь разрешена, если туда двигает обязательство.

### Обязательства сильнее автоматики

Потребность может существовать и без проекта — из-за дефицита мест в округе. Это внешний факт, из ТЭП он не выводится, поэтому у каждого объекта появился параметр «требуется не позже очереди N»: `kindergarten_not_later_than`, `school_not_later_than`, `clinic_not_later_than`. Если он задан и жёстче автоматики, объект переезжает раньше, а в раскладке видно почему.

Каждый объект отдаёт `reason` — «поздняя раскладка», «обязательство: не позже очереди N» или «вручную», — а также `auto_phase` (куда поставила бы автоматика) и признак `moved_earlier`. Видно и цену вопроса: перенос объекта на очередь вперёд сразу отражается в пике ПФ.

Ручная раскладка из реестра очерёдности по-прежнему главнее: если очередь у объекта проставлена руками, пересчёт её не перетирает.

### Что видно в отчётах

В сравнении очередей появились столбцы **«Социальная нагрузка, млн ₽»** и **«Социальные объекты»** — и в интерфейсе, и в листе «Сравнение очередей» книги-консолидатора. Сумма по столбцу считается формулой.

Режим денежной компенсации не затронут: там распределять нечего, платёж дробится по датам как раньше.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.46`.

# DevelopAid v0.12.45 — поквартальная сводка в выгрузке модели

## Что добавлено в v0.12.45

К помесячной детализации добавилась поквартальная — теми же цифрами, свёрнутыми по кварталам.

- **«Финансирование поквартально»** — продажи, расходы, БРИДЖ, ПФ, эскроу, ставки, проценты, комиссии и налог по кварталам;
- **«Расходы поквартально»** — статьи CAPEX × кварталы;
- **«Продажи поквартально»** — выручка и реализованные объёмы по продуктам × кварталы;
- при очерёдности поквартальные листы попадают в файл каждой очереди.

Свёртка сделана по смыслу показателя, а не механически:

| Тип показателя | Как сворачивается |
| --- | --- |
| Потоки (продажи, расходы, выборки, погашения, проценты, комиссии, налог) | сумма за квартал |
| Остатки (БРИДЖ, ПФ, эскроу, накопленная база налога) | значение на конец квартала |
| Ставки и покрытие эскроу | среднее за квартал |

В строке «Итого» по-прежнему складываются только потоки: остатки и ставки там намеренно пустые. Помесячные листы остались на месте — квартальные идут рядом, а не вместо.

## Уточнения по шаблону ПЛАТО из v0.12.44

Подтверждено решение: формулы шаблона остаются живыми — заполняются только листы-вводные, всё остальное считает сам шаблон. Очереди выгружаются отдельными файлами, каждый из них полноценная книга ПЛАТО. Сетка периодов внутри шаблона остаётся месячной: она зашита в его формулы, поквартальная сводка живёт в выгрузке модели, где раскладку определяем мы.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.45`.

# DevelopAid v0.12.44 — выгрузка в шаблон ПЛАТО

## Что добавлено в v0.12.44

Рядом с «Скачать модель (ZIP)» появилась кнопка **«Выгрузить в шаблон ПЛАТО»**: расчёт ложится в ваш рабочий файл `PLATO_template`, а не в самодельную книгу.

### Как это устроено

Шаблон — живая модель: 27 листов и **113 708 формул**. Трогать их нельзя, поэтому заполняются только два листа-вводных, а всё остальное — Дашборд, ОТЧЕТ, ТЭП, СРОКИ, Цены, Ставки, CF, `cf_0`…`cf_2`, КРЕДИТЫ, гр.эскроу, ЗУ, LLCR — шаблон считает сам при открытии:

- **«Вводные»** — 87 ячеек: сделка, сроки, цены и темпы продаж, себестоимость по статьям, коммерческие расходы, налоги, спреды и комиссии финансирования, ставка дисконтирования, прогноз ключевой ставки, социальная нагрузка (ДОУ, СОШ, поликлиника) и три отдельно стоящих объекта — МФОЦ, ТЦ, наземный паркинг;
- **«Расчет ВРИ (ТЭП)»** — площади и объёмы модели: количество квартир, СПП жилая и нежилая, наземная площадь, площадь квартир, ННП, постоянные парковки, стоимость смены ВРИ.

Привязка идёт по подписи параметра, а не по адресу ячейки: вставленная в шаблон строка не ломает выгрузку. Проценты пишутся долями, даты — датами, флаги объектов — «Да/Нет», как ожидает шаблон.

Значения модели записываются во все три сценария шаблона, поэтому переключатель «консервативный / базовый / оптимистичный» не меняет цифры расчёта DevelopAid.

### Состав архива

- единый расчёт — один файл шаблона;
- при очерёдности — `00_Консолидация_<проект>.xlsx` и по файлу на каждую очередь, каждый из них полноценная модель ПЛАТО;
- `README.txt` с составом и напоминанием: если Excel показывает старые значения, пересчёт по Ctrl+Alt+F9.

Новые эндпоинты `POST /report/plato` и `GET /report/plato/status`. Шаблон лежит в `templates/PLATO_template.xlsx`; без него эндпоинт отвечает понятной ошибкой, остальное приложение работает как раньше.

### Зависимости

Появился `openpyxl` в `requirements.txt` — он нужен, чтобы записать значения и не потерять формулы шаблона. Собственный генератор XLSX для выгрузки модели остался прежним и зависимостей не требует.

### Что проверить на первом запуске

Excel пересчитывает книгу при открытии. Сверьте выручку, CAPEX и LLCR в шаблоне с теми, что показывает сайт: движок DevelopAid и формулы шаблона — две независимые реализации одной методики, и расхождение будет означать, что где-то они разошлись.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.44`.

# DevelopAid v0.12.43 — помесячная детализация финмодели

## Что добавлено в v0.12.43

Раньше выгрузка показывала расходы проекта одной суммой за месяц. Теперь видно, из чего эта сумма состоит.

- движок отдаёт `capex_by_article` — каждая статья CAPEX разложена по месяцам тем же графиком, каким она и так расходовалась внутри расчёта: покупка, смена ВРИ, ИРД, П, РД, подготовка, СМР наземной и подземной части, сети, содержание площадки, авторский надзор, технадзор, управление проектом, благоустройство, ввод, социальная нагрузка, МФОЦ, ТЦ, наземный паркинг, генподряд, резерв;
- в результате расчёта появился блок `monthly`: расходы по статьям, выручка по продуктам, реализованные объёмы, коммерческие расходы и налог — всё помесячно;
- в выгрузке два новых листа: **«Расходы помесячно»** (статьи × месяцы) и **«Продажи помесячно»** (выручка и объёмы по продуктам × месяцы);
- итог по каждой строке и по каждому месяцу — формула `SUM` с посчитанным значением, поэтому лист живой после правки;
- при очерёдности детализация попадает в файл каждой очереди.

Новой математики нет: суммы по статьям сходятся с `capex.total`, помесячные суммы — с общим рядом расходов, выручка по продуктам — с итоговой выручкой. Сценарный множитель себестоимости применяется к детализации так же, как к итогам.

# DevelopAid v0.12.42 — видно, из какого тура кадастровой оценки взяты УПКС

## Что добавлено в v0.12.42

Вопрос «а не устарела ли кадастровая стоимость» теперь виден прямо в расчёте.

- в карточке смены ВРИ у каждой строки УПКС указана дата оценки: земля — на 01.01.2022, ОКС — на 01.01.2023;
- добавлена таблица «Кадастровая стоимость участков (ЕГРН)»: по каждому участку кадастровая стоимость, дата её определения и переход «текущий УПКС → целевой УПКС»;
- реквизиты туров оценки отдаются в `GET /mo/reference` (`upks_source`) и в ответе `POST /mo/calculate` (`upks.source`): отчёт, дата оценки, дата применения и дата следующего тура.

### Что откуда берётся и что стареет

| Величина | Источник | Актуальность |
| --- | --- | --- |
| Кадастровая стоимость участка (КС1) | ЕГРН через НСПД, живьём на момент запроса | всегда текущая |
| УПКС земли под жильё (целевая КС2) | Отчёт № 01/2022 об итогах ГКО Московской области | оценка на 01.01.2022, применяется с 01.01.2023 |
| УПКС ОКС многоквартирных домов (УПКСср) | Отчёт № 01/2023 | оценка на 01.01.2023, применяется с 01.01.2024 |
| Кср | Распоряжение Комитета по ценам и тарифам МО | III, IV кварталы 2026 года |

Государственная кадастровая оценка проводится раз в четыре года: следующий тур по земельным участкам — по состоянию на 01.01.2026 с применением с 01.01.2027, по ОКС — на 01.01.2027 с применением с 01.01.2028. После утверждения новых отчётов таблицы УПКС в `main_legacy.py` нужно заменить.

Важно: на итоговую плату за смену ВРИ возраст таблиц не влияет — формула сворачивается в Кср × площадь квартир × Кд. Таблицы УПКС определяют только промежуточные ∆КС, К1 и К.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.42`.

# DevelopAid v0.12.41 — справочник Кср заполнен официальными данными

## Что добавлено в v0.12.41

Справочник средней рыночной стоимости 1 м² жилья больше не пустой: в поставку включена таблица из **распоряжения Комитета по ценам и тарифам Московской области от 28.04.2026 № 114-Р** (зарегистрировано 30.04.2026 № 32-443/2026, действует с 01.07.2026) на III–IV кварталы 2026 года.

- `data/mo_market_price.csv` содержит все 56 муниципальных образований из приложения к распоряжению плюс среднее значение по области — 198 907 ₽/м²;
- Кср подставляется по округу автоматически: например, Мытищи — 238 052 ₽/м², Красногорск — 251 426, Реутов — 242 817, Серебряные Пруды — 77 041;
- если округа нет в распоряжении (упразднённые Протвино, Пущино, Дзержинский, Электрогорск), берётся среднее по области с отдельным предупреждением;
- в карточке ВРИ видно, что Кср пришёл из распоряжения и за какой период; ручное значение в поле по-прежнему приоритетнее;
- добавлены синонимы названий: «Павлово-Посадский городской округ» из распоряжения и «Городской округ Павловский Посад» из таблиц УПКС — один и тот же округ;
- из справочника УПКС убрана строка «Итого по Московской области»: это итог таблицы, а не муниципальное образование, теперь она хранится отдельной константой. Округов в справочнике 60.

Обновление на следующий период — загрузкой нового приложения через кнопку «Загрузить таблицу Кср»: распоряжения выходят раз в полугодие.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.41`.

# DevelopAid v0.12.40 — справочник средней рыночной стоимости жилья (Кср)

## Что добавлено в v0.12.40

Кср — средняя рыночная стоимость 1 м² жилья — перестал быть значением «из головы». Он берётся из официального источника: распоряжений Комитета по ценам и тарифам Московской области (например, от 22.04.2025 № 89-Р на III–IV кварталы 2025 года).

- в блоке «Калькулятор Подмосковья» появилась загрузка приложения к распоряжению: `.xlsx` с таблицей «муниципальное образование — стоимость»;
- разбор терпим к формату: ищутся пары «название — число в диапазоне 20 000…1 000 000 ₽», названия сопоставляются с 61 округом справочника, строка «в целом по Московской области» сохраняется отдельно как среднее по региону;
- после загрузки Кср подставляется по округу автоматически, в карточке ВРИ видно источник и период;
- порядок выбора Кср: значение из поля запроса → справочник Комитета → УПКС ОКС округа (последнее только как заглушка, с предупреждением, что К1 = 1,00);
- нераспознанные строки и округа без цены возвращаются списком, чтобы таблицу можно было проверить;
- новые эндпоинты: `GET /mo/market-price`, `POST /mo/market-price` (список строк) и `POST /mo/market-price/import` (файл распоряжения);
- справочник хранится в `data/mo_market_price.csv` и переопределяется переменной `MO_MARKET_PRICE_PATH`; ответ импорта содержит готовый CSV, чтобы положить его в репозиторий — иначе загруженная таблица живёт только до перезапуска сервиса.

Формат файла справочника:

```
municipality,price_rub_per_sqm,period,document
Городской округ Мытищи,208733.0,III–IV кварталы 2025,Распоряжение № 89-Р от 22.04.2025
```

Автоматической загрузки прямо с `mosreg.ru` нет: официальный источник публикует таблицу приложением к распоряжению, без API, поэтому файл загружается вручную раз в полугодие.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.40`.

# DevelopAid v0.12.39 — калькулятор Подмосковья

## Что добавлено в v0.12.39

Отдельный блок для Московской области — аналог калькулятора ГлавАПУ, который работает только по Москве.

- на вкладке «Вводные» появился блок **«Калькулятор Подмосковья»**: кадастровый номер, адрес или площадь участка вручную;
- принимаются только участки кадастрового округа 50 — номер вне Московской области отклоняется с понятным сообщением;
- площадь участка берётся из ЕГРН через НСПД, по ней и заданной плотности считается площадь квартир;
- **плотность — параметр**, по умолчанию 30 000 м² квартир на 1 га; в поле рядом можно поставить любую;
- социальная нагрузка считается по нормативам РНГП Московской области: население (28 м²/чел.), ДОО (65 мест на 1000), СОШ (135 мест на 1000, кратно 25), поликлиника (17,75 пос./смену на 1000), паркинг постоянного и временного хранения (356 м/м на 1000, 90% и 18%), озеленение (6,5 и 4,4 м²/чел.), нежилые помещения общественных объектов, офисы под дефицит рабочих мест, компенсация в бюджет (стационар, скорая, пожарное депо);
- **любой норматив можно переопределить** в запросе — все они собраны в одном словаре;
- плата за смену ВРИ считается по методике приложенного расчёта: разница кадастровых стоимостей участков и целевого УПКС жилой застройки, коэффициенты К1 = Кср / УПКСср и К = S × УПКСср × Кд / ∆КС;
- справочники УПКС: земли жилой застройки и ОКС многоквартирных домов по 61 городскому округу, плюс реестр УПКС ОКС по 30 839 кадастровым кварталам;
- округ определяется по адресу ЕГРН, при неудаче выбирается вручную из списка;
- показывается упрощённый баланс территории: сколько гектаров занимает нормативная социалка и сколько остаётся под жильё и УДС;
- кнопка «Применить к Вводным и ТЭП» переносит в модель ТЭП, социальные мощности и стоимость смены ВРИ;
- в Telegram кадастровые номера вида `50:*` автоматически уходят в подмосковный расчёт, а не в московский ГлавАПУ, и приходят обычной карточкой ТЭП — с кнопкой «Прокомментировать ТЭП» от Платона;
- новые эндпоинты `POST /mo/calculate` и `GET /mo/reference`.

### Сверка с исходными расчётами

Расчёт воспроизводит приложенные образцы:

- ППТ на 200 000 м² квартир — население 7 143, ДОО 465 мест, СОШ 975 мест, поликлиника 127 пос./смену, паркинг 2 289 и 458 м/м, подземный паркинг 80 115 м², озеленение 46 429,5 и 31 429,2 м², СПП ГНС 314 911 м²;
- смена ВРИ по 22 участкам в Мытищах — ∆КС 630 964 445,36 ₽, К1 1,8302, К 3,615, плата 4 174 618 253,82 ₽ (совпадение до копейки).

Два отличия от исходных таблиц, оба намеренные и объяснимые:

1. нежилые помещения общественных объектов считаются строго по нормативу (20 348 м² против 20 719 м² в ППТ) — в ППТ часть позиций округлена вверх проектировщиком;
2. методика платы за ВРИ алгебраически сворачивается: ∆КС и УПКСср сокращаются, и П = Кср × площадь квартир × Кд. То есть плата зависит только от средней цены м², площади квартир и коэффициента Кд, а таблицы УПКС влияют лишь на промежуточные ∆КС, К1 и К. Расчёт по участкам сохранён полностью, но об этом стоит знать.

### Новый файл данных

Появилась папка `data/` с файлом `upks_oks_quarters.csv.gz` — реестр УПКС ОКС по кадастровым кварталам. Файл необязательный: без него калькулятор работает на окружных справочниках, теряется только справочное значение УПКС по кварталу.

Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.39`.

# DevelopAid v0.12.38 — выгрузка полной модели в Excel

## Что добавлено в v0.12.38

- рядом с «Экспорт PDF» на вкладке «Отчёт» появилась кнопка **«Скачать модель (ZIP)»** — выгружается вся модель, а не только отчётные цифры;
- новый эндпоинт `POST /report/model` возвращает ZIP; расчёт выполняется на сервере штатным движком, клиентские числа не принимаются на веру;
- в Telegram модель приходит вместе с PDF: после итоговой карточки бот присылает тот же ZIP;
- книга модели содержит листы: `Сводка`, `Вводные`, `ТЭП`, `Выручка`, `Расходы`, `Помесячно`, `Денежный поток`, `Календарь`;
- лист `Помесячно` — полная помесячная модель: продажи, расходы, БРИДЖ, ПФ, эскроу, покрытие, ставки, проценты, комиссии и налог по 23 колонкам;
- итоги, доли и накопленный поток заданы формулами Excel с посчитанными значениями, поэтому файл читается и до пересчёта, и живёт после правки;
- по месяцам суммируются только потоки: остатки долга, ставки и покрытие эскроу в строке «Итого» намеренно пустые;
- лист `Вводные` выгружает все параметры модели вместе с ключами — по ним значения переносятся обратно;
- денежные показатели выгружаются в млн ₽, площади в м², ставки и доли в процентах.

### Очередность и консолидатор

- при включённой очерёдности ZIP содержит `00_Консолидация.xlsx` и по файлу на каждую очередь;
- консолидатор держит очереди отдельными листами и лист `Консолидация помесячно`, где суммы собираются формулами `SUMIF` с этих листов: правка месяца в очереди сразу меняет свод;
- в консолидаторе есть лист `Сравнение очередей` — площади, выручка, CAPEX, общие расходы кассой и по аллокации, пики БРИДЖ и ПФ, LLCR, прибыль и маржинальность по каждой очереди с итогом;
- отдельные файлы очередей — та же модель по одной очереди, если нужен изолированный расчёт;
- проект с одной очередью выгружается как единая модель, без лишнего консолидатора;
- в `README.txt` внутри архива описан состав файлов, единицы измерения и ограничение по суммированию остатков.

Внешних зависимостей не добавилось: XLSX собирается тем же встроенным механизмом, что и шаблон ТЭП. Версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.38`.

# DevelopAid v0.12.37 — Платон комментирует ТЭП

## Структура файлов

Начиная с 0.12.36 приложение разложено на два файла:

- `main_legacy.py` — расчётное ядро, веб-интерфейс и базовый Telegram-слой (бывший `main.py`);
- `main.py` — тонкая обёртка: загружает ядро как модуль `core`, поднимает то же приложение FastAPI и надстраивает Telegram-слой Платона.

Обе части деплоятся вместе, команда запуска не меняется: `uvicorn main:app --host 0.0.0.0 --port $PORT`.

## Что добавлено в v0.12.37

- Платон Сергеевич комментирует ТЭП сразу после того, как бот его собрал, — не дожидаясь открытия мини-приложения и отправки результата;
- под карточкой «Проверьте ТЭП перед созданием проекта» и под итоговой карточкой расчёта появилась кнопка **«Прокомментировать ТЭП»**; та же команда — `/comment`;
- разбор идёт по фиксированной структуре: состав ТЭП, посадка и плотность, экономика при текущих ценах, риски и слабые места, что уточнить до сделки;
- ТЭП бота разворачивается в полный контекст модели ровно так же, как это делает мини-приложение: частичные вводные накладываются на `DEFAULT_INPUTS`, частичный ТЭП — на `TEP_DEFAULT`, после чего расчёт выполняет штатный движок, а не языковая модель;
- кнопка «Спросить Платона» больше не отвечает «модель не передана» после ТЭП, собранного ботом: диалог работает и на этом контексте;
- если Платон предложил изменение вводных, кнопка «Применить в модель» работает и для ТЭП бота — ссылка на мини-приложение пересобирается с учётом правки;
- разбор ТЭП не переводит чат в режим диалога: обычный текст по-прежнему обрабатывает основной сценарий бота;
- если `OPENAI_API_KEY` не задан, бот прямо говорит об этом вместо непонятной ошибки; то же состояние видно в `/status`;
- `/status` показывает источник контекста: ТЭП, собранный ботом, или полный расчёт из мини-приложения;
- федеральный поиск участка из 0.12.35 доступен в этой сборке без изменений;
- версия приложения, `/health`, заголовок страницы, User-Agent и версия бота обновлены до `0.12.37`.

### Известное ограничение сборки

Пакет `./developaid_hotfix` из `requirements.txt` патчит файл `main.py`, находя его по строке `DevelopAid Development Investment Model`. После разделения на `main.py` и `main_legacy.py` он этот файл не находит и при старте пишет в stderr traceback, ничего не меняя. Патч закрытия Mini App из 0.12.33–0.12.34 в этой сборке не применяется. Решение — отдельной задачей: либо убрать `./developaid_hotfix` из `requirements.txt`, либо перенести его правки внутрь обёртки.

# DevelopAid v0.12.35 — поиск участка по кадастровому номеру и адресу (вся Россия)

## Что добавлено в v0.12.35

- новый блок «Поиск участка по кадастровому номеру или адресу» на вкладке «Вводные»: работает по всей стране, а не только по Москве;
- запрос принимает кадастровый номер (можно несколько сразу), почтовый адрес и координаты «широта, долгота»;
- источник сведений — НСПД (`nspd.gov.ru`, ППК «Роскадастр»), открытые данные ЕГРН: адрес, площадь (м² и га), категория земель, ВРИ, кадастровая стоимость и удельная стоимость ₽/м², форма собственности, статус, дата постановки на учёт, кадастровый квартал, субъект РФ, координаты центра и ссылка на публичную карту;
- если адрес не находится в НСПД напрямую, он геокодируется и участок определяется по точке: Яндекс (`YANDEX_GEOCODER_API_KEY`) → DaData (`DADATA_API_KEY`) → OpenStreetMap Nominatim без ключа;
- если НСПД недоступен или номера нет в ЕГРН, карточка всё равно показывает субъект РФ по коду кадастрового округа и кадастровый квартал, разобранные из самого номера;
- кнопка «Подставить номера в расчёт ТЭП» переносит найденные земельные участки в существующий блок ГлавАПУ; кнопка «Сохранить участок в проект» кладёт сведения в `inputs._land_lookup` вместе с проектом;
- новые эндпоинты `POST /land/lookup` и `GET /land/providers` (диагностика подключённых источников);
- ответы НСПД кешируются на 15 минут (`LAND_LOOKUP_CACHE_TTL`), запросы к Nominatim ограничены одним в секунду по правилам OSM;
- на внешние сервисы уходит только строка поиска — финансовая модель и вводные не передаются;
- расчётное ядро, ТЭП, кредитный блок и PDF не затронуты;
- версия приложения, `/health`, заголовок страницы и User-Agent обновлены до `0.12.35`.

### Переменные окружения (все необязательные)

| Переменная | Назначение |
| --- | --- |
| `NSPD_BASE_URL` | адрес геопортала НСПД, по умолчанию `https://nspd.gov.ru` |
| `NSPD_TIMEOUT_SECONDS` | таймаут запроса к НСПД, по умолчанию 25 с |
| `YANDEX_GEOCODER_API_KEY` | ключ геокодера Яндекса (приоритетный геокодер) |
| `DADATA_API_KEY` | токен DaData; если DaData вернёт кадастровый номер, он используется напрямую |
| `NOMINATIM_BASE_URL` | свой инстанс Nominatim вместо публичного |
| `LAND_LOOKUP_GEOCODER` | принудительно один геокодер: `yandex`, `dadata` или `nominatim` |
| `LAND_LOOKUP_CACHE_TTL` | время жизни кеша ответов, секунды |

# DevelopAid v0.12.24 — ставка ЦБ, БРИДЖ, очередность и PDF

## Что изменено в v0.12.24

- кнопка обновления ставки использует официальный RSS пресс-релизов Банка России, поэтому решение от 24.07.2026 подхватывается сразу как 14,00%, без ожидания даты вступления в силу в исторической таблице;
- ставка БРИДЖ раскрыта на четыре составляющие: текущая ключевая, спред, ставка на текущей ключевой и средневзвешенная ставка за период;
- средняя ключевая ставка за период БРИДЖ показывается отдельно, поэтому прогнозная средняя больше не выглядит как текущая ставка кредита;
- новый и импортируемый проект одноочередный по умолчанию; очередность включается только явным действием пользователя или проектным preset;
- старое состояние браузера без признака явного включения очередности больше не переносит три очереди в новый небольшой проект;
- из PDF удалены методологические примечания;
- в PDF добавлены графики структуры расходов, динамики БРИДЖ/ПФ/эскроу, ставок финансирования и месячного темпа продаж;
- версия приложения, `/health`, заголовок страницы и User-Agent обновлены до `0.12.24`.

# PLATO v0.12.5 — единый блок пояснений в шапке

## Что изменено в v0.12.5

- пояснения о классе проекта, сценарии и связанных расходах объединены в один блок непосредственно под верхней шапкой;
- дублирующий блок под меню вкладок удалён;
- меню вкладок больше не разрезает единое пояснение;
- отступы блока приведены к общей сетке интерфейса, включая мобильную версию;
- расчётная логика не менялась.

## Изменения, сохранённые из v0.12.4

### Понятные ставки ПФ и динамические сценарии ключевой ставки

## Что изменено в v0.12.4

### 1. Названия ставок ПФ

Во всех KPI и детальных таблицах заменены двусмысленные подписи:

- `Базовая ставка ПФ до эскроу` → `Средняя ставка ПФ без эффекта эскроу`;
- `Эффективная ставка ПФ после эскроу` → `Средняя фактическая ставка ПФ с учётом эскроу`;
- `Спецставка ПФ` → `Ставка ПФ при покрытии эскроу 1×`.

Расчёт ставок не менялся — изменено только их корректное экономическое наименование.

### 2. Динамические названия сценариев ключевой ставки

Значения `11% / 9% / 7%` больше не зашиты в выпадающий список. Названия сценариев автоматически подтягивают текущие целевые ориентиры из полей `Консервативная цель`, `Базовая цель` и `Оптимистичная цель`.

Например, после изменения базовой цели на `10,5%` интерфейс и отчёт показывают `Базовый → 10,5%`. Значения также синхронизированы с легендой графика.

### 3. Техническая синхронизация версии

Версия приложения, `/health`, заголовок страницы и User-Agent приведены к `0.12.4`.

## Изменения, сохранённые из v0.12.3

## Что исправлено

### 1. Отдельная налоговая маржа объектов КРТ

МФОЦ / офисы, ТЦ / ОСЗ и наземный паркинг теперь рассчитываются как самостоятельные продукты:

- МФОЦ и ОСЗ — реализованные м² × (фактическая цена месяца − себестоимость на продаваемый м²);
- наземный паркинг — реализованные машино-места × (фактическая цена места − себестоимость места);
- каждый объект включается в общую налоговую базу один раз.

### 2. Налог начисляется накопительно и помесячно

Логика приведена к Excel-эталону `PLATO_template(1).xlsx`:

1. суммируется маржа реализованных основных продуктов;
2. добавляются отдельные маржи объектов КРТ;
3. вычитаются выплаченные проценты и банковские комиссии;
4. налог 25% начисляется на накопленную положительную базу не ранее РВЭ;
5. ранее начисленный налог повторно не учитывается.

В ответе расчётного API появились:

- `finance.profit_tax_schedule`;
- `finance.tax_rows`;
- `finance.tax_margin_by_product`;
- `finance.tax_cost_by_product`;
- `cashflow.profit_tax`.

### 3. Исправлен горизонт отдельных объектов

Если строительство или продажи объекта КРТ заканчиваются позже жилой части, горизонт проекта автоматически продлевается до последнего денежного потока. Поздняя выручка теперь одновременно попадает в:

- финансирование;
- налоговый расчёт;
- NPV / IRR;
- проектный и акционерный cash flow.

### 4. Убран двойной учёт платы за лимит в NPV

Плата за лимит уже капитализируется в задолженность по процентам и входит в `interest_payment` при фактической выплате. В cash flow она больше не вычитается второй раз отдельной строкой.

### 5. Техническая синхронизация версии v0.12.3

В базовой сборке версия приложения, `/health`, заголовок страницы и User-Agent были приведены к `0.12.3`. Устаревшая версия `0.8.4` в `/health` удалена.

### 6. Платон Сергеевич видит новый налоговый блок

Для `explain_metric` и `trace_metric` добавлена метрика `profit_tax`: агент показывает маржу основных продуктов и КРТ, признанную себестоимость, расходы финансирования, формулу и помесячные платежи.

## Что не менялось

- логика принятия решений Платона Сергеевича и `evaluate_purchase_offer` из v0.12.2;
- защита от повторных tool calls и финальный synthesis-pass;
- базовая логика БРИДЖ / ПФ / эскроу;
- одноочередная и многоочередная архитектура;
- пользовательские вводные и визуальная структура интерфейса.

## Развёртывание

Заменить в репозитории `warstyle-plato/plato` файл `main.py` на новую версию и выполнить commit. Render подхватит обновление автоматически.
