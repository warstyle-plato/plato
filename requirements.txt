fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
reportlab>=4.2
openpyxl>=3.1
pypdf>=4.3
# Штатный калькулятор ГлавАПУ запускается браузером без экрана: копия его
# методики отставала от города, и расхождение находил человек, а не мы.
# Включается переменной GLAVAPU_HEADLESS=1 на той машине, где есть Chromium.
playwright>=1.44
# Картинка территории в чате бота: контуры ЕГРН рисуются поверх подложки НСПД.
Pillow>=10
# QR для входа с телефона: на компьютере без Telegram ссылка t.me открывает
# страницу «поставьте приложение», и человек упирается в тупик. Чистый Python,
# без зависимостей — рисовать QR своим кодом значит писать Рида — Соломона,
# которого некому проверять.
segno>=1.6
# Отчёт о продажах приходит и в двоичном .xlsb: рабочая финмодель на
# двадцать семь листов в нём весит вдвое меньше, а openpyxl его не читает вовсе.
pyxlsb>=1.0
# Свод продаж презентацией: слайд — раздел того же отчёта. Второй вёрстки нет,
# на слайд уходит снимок раздела, а собрать файл .pptx своими руками — это ZIP
# с полудюжиной XML по спецификации OOXML, которую некому проверять.
python-pptx>=1.0
